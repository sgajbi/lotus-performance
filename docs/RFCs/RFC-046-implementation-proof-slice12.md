# RFC-046 Slice 12 Implementation Proof

| Field | Value |
| --- | --- |
| RFC | RFC-046 - TWR Industry Methodology Alignment, Evidence Contract, and Data Product Hardening |
| Slice | 12 - Implementation Proof |
| `lotus-performance` branch | `feat/rfc-046-twr-industry-evidence` |
| Proof artifact | `output/rfc-046/slice12-implementation-proof.json` |

## Proof Scope

Slice 12 proves the RFC-046 implementation through a local application execution path and records a
machine-readable proof artifact under non-git-tracked `output/`.

The proof exercises:

- portfolio TWR through `POST /performance/twr`
- daily calculation evidence
- geometric linking and non-arithmetic cumulative return
- beginning-of-day and end-of-day flow evidence
- benchmark supportability evidence with a calendar gap
- calculation supportability and bounded metric-label posture
- stateful input mode with mocked `lotus-core` source rows
- stateful source-quality evidence preserving `source_owner="lotus-core"` and
  `source_product="PortfolioTimeseriesInput"`
- degraded source warnings for missing valuation points, unsupported cash-flow labels, duplicate
  source-date conflicts, and stale source observations

## Proof Artifact Summary

`output/rfc-046/slice12-implementation-proof.json` was generated by running the application through
`TestClient` and writing only the summarized evidence required for review.

Critical values:

- stateless TWR response status: `200`
- stateful TWR response status: `200`
- daily returns: `+10%`, `-10%`, and `0.454545%`
- cumulative return after day 2: `-1%`, proving geometric linking rather than arithmetic summing
- final period return: `-0.5500004499999989%`
- daily evidence status: `calculated`
- daily linkability status: `linkable`
- benchmark calendar alignment: `partial_overlap`
- benchmark warning codes:
  - `BENCHMARK_CALENDAR_GAP`
  - `BENCHMARK_VENDOR_SERIES_BASE_ONLY`
- stateful source owner: `lotus-core`
- stateful source product: `PortfolioTimeseriesInput`
- source quality state: `stale`
- source-quality warnings:
  - `MISSING_VALUATION_POINTS`
  - `UNSUPPORTED_CASHFLOW_LABELS`
  - `SOURCE_DATE_CONFLICTS`
  - `STALE_SOURCE_OBSERVATIONS`

The script asserted these values before writing the proof artifact.

## Critical Review

The proof shows the RFC-046 evidence contract is internally coherent:

- calculation evidence explains the returned daily figures and flow treatment
- geometric linking is visible in cumulative return
- benchmark supportability does not hide missing benchmark dates
- stateful source-quality evidence preserves source ownership instead of flattening source posture
- degraded source state is represented as supportability metadata rather than a silent clean result

No material implementation gap was found in this proof pass.

Known governed publication note: Slice 10 changed repo-local wiki source. Live wiki publication is
deferred until the RFC branch is merged to `main`, so published wiki truth does not get ahead of
merged repository truth.

## Validation

Proof generation command:

- inline Python `TestClient` proof script writing
  `output/rfc-046/slice12-implementation-proof.json`
  - Result: passed; artifact generated

Post-proof validation:

- `python -m pytest tests/unit/docs/test_public_docs_contract.py -q`
  - Result: `41 passed`
- `make lint`
  - Result: passed, including monetary-float guard with `135` findings and `135` allowlisted findings
- `make typecheck`
  - Result: `Success: no issues found in 159 source files`
- `git diff --check`
  - Result: passed, with line-ending warnings only

The proof artifact is intentionally not tracked by git. This slice evidence document records the
artifact path and key reviewed values.
