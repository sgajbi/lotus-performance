# Codebase Review Ledger

This ledger tracks systematic review work across `lotus-performance`.

Companion process document:

- [CODEBASE-REVIEW-PLAYBOOK.md](./CODEBASE-REVIEW-PLAYBOOK.md)

## Review entries

| Review ID | Date | Scope / Pattern | Status | Findings | Actions Taken | Follow-up | Evidence / Sign-off |
|---|---|---|---|---|---|---|---|
| LP-CR-001 | 2026-03-13 | Async orchestration, compute-job leasing, and worker fencing | Hardened | Initial review found a correctness risk in `analytics_compute_job` claiming: `lease_pending_jobs(...)` and stale-job reconciliation both selected rows and then mutated them without a row-level claim fence. On PostgreSQL, competing compute workers could observe the same pending or stale row before either transaction committed. | Added explicit PostgreSQL `FOR UPDATE SKIP LOCKED` statement builders for pending-job leasing and stale-job reconciliation in `ComputeJobStore`, and added lower-level unit proofs for both statement shapes while retaining existing lifecycle tests. | Continue this pattern review with worker quiescence, queue observability, and documentation drift around the old background-task runtime description. | Branch `review-ledger-async-orchestration`; files: `app/services/compute_job_store.py`, `tests/unit/services/test_compute_job_store.py`; validation: `python -m pytest tests/unit/services/test_compute_job_store.py -q`, `python -m pytest tests/unit/services/test_compute_executor_worker.py -q`, `python -m ruff check app/services/compute_job_store.py tests/unit/services/test_compute_job_store.py`, `python -m mypy --config-file mypy.ini app/services/compute_job_store.py`. |
| LP-CR-002 | 2026-03-13 | Runtime readiness and durable metadata dependency health | Hardened | The readiness endpoint only checked an in-process draining flag. The API could therefore return `{"status":"ready"}` even when the durable metadata store was unreachable, leaving async execution, lineage persistence, and execution polling effectively broken behind a false-green readiness contract. | Added `ExecutionRegistry.ping()`, introduced a shared durability-health service, and hardened `/health/ready` to fail closed with `503` and a concrete reason when the durable metadata store is unreachable. Added service-level and integration proofs for the readiness contract. | Next: align architecture/runtime docs with the executor-backed model and review whether queue-pressure visibility should be surfaced beyond readiness. | Files: `app/services/execution_registry.py`, `app/services/durability_health_service.py`, `app/api/endpoints/health.py`, `tests/unit/services/test_durability_health_service.py`, `tests/integration/test_integration_capabilities_api.py`; validation: `python -m pytest tests/unit/services/test_durability_health_service.py tests/integration/test_integration_capabilities_api.py -q`, `python -m ruff check app/api/endpoints/health.py app/services/durability_health_service.py app/services/execution_registry.py tests/unit/services/test_durability_health_service.py tests/integration/test_integration_capabilities_api.py`, `python -m mypy --config-file mypy.ini app/services/durability_health_service.py app/services/execution_registry.py`. |
| LP-CR-003 | 2026-03-13 | Runtime documentation and RFC status drift | Hardened | The living docs still described the pre-RFC-041 model: in-process background-task lineage capture, no explicit runtime-topology document, and RFC status artifacts that still classified RFC-041 as draft/partially implemented. | Rewrote the technical architecture doc around the implemented API-orchestrator/compute-executor/lineage-worker/database split, added a dedicated runtime-topology document, updated reproducibility guidance to describe durable lineage metadata plus worker materialization, and corrected RFC status artifacts to mark RFC-041 implemented. | Separate docs-quality review still needed for `docs/guides/api_reference.md`, which remains a broader stale-contract/document-structure issue rather than a runtime-truth issue. | Files: `docs/technical/architecture.md`, `docs/technical/runtime_topology.md`, `docs/guides/reproducibility.md`, `docs/RFCs/RFC-INDEX.md`, `docs/RFCs/RFC_IMPLEMENTATION_STATUS.md`; characterization evidence: targeted content review against `main.py`, `docker-compose.yml`, `app/workers/compute_executor_worker.py`, and `app/workers/lineage_worker.py`. |
| LP-CR-004 | 2026-03-13 | Human API reference drift and async contract clarity | Hardened | The existing `docs/guides/api_reference.md` was structurally broken, mixed obsolete request shapes, and did not reflect the executor-backed async result pattern now used by returns-series, contribution, and attribution. | Replaced the file with a current endpoint map covering synchronous analytics, async accepted/result flows, execution polling, lineage retrieval, health/readiness semantics, and the recommendation to use Swagger/OpenAPI for exact field-level contract detail. | OpenAPI-generated examples remain the canonical field-level source. Keep this guide at the endpoint-map level and do not hand-maintain duplicated field catalogs here. | File: `docs/guides/api_reference.md`; characterization evidence: reviewed against router surfaces in `app/api/endpoints/performance.py`, `app/api/endpoints/contribution.py`, `app/api/endpoints/returns_series.py`, `app/api/endpoints/executions.py`, and `app/api/endpoints/health.py`. |
| LP-CR-005 | 2026-03-13 | Durable queue-pressure observability | Hardened | The service exposed generic HTTP metrics but no first-class view of compute backlog, failed durable jobs, or oldest pending lineage age. Operators had durable state, but not a durable-state metrics contract for executor and lineage queues. | Added queue stats methods to the compute and lineage stores, introduced a Prometheus collector that exports durable queue gauges, added store-level tests plus collector proof, and added an integration assertion that `/metrics` exposes the new queue-pressure signals. Updated runtime-topology docs to include the metrics contract. | Next: consider whether queue-pressure visibility also needs a control-plane/read API beyond Prometheus. The metrics surface is now in place, but operator drill-down is still limited to execution-specific polling. | Files: `app/services/compute_job_store.py`, `app/services/lineage_metadata_store.py`, `app/services/queue_metrics_service.py`, `app/observability.py`, `tests/unit/services/test_compute_job_store.py`, `tests/unit/services/test_lineage_metadata_store.py`, `tests/unit/services/test_queue_metrics_service.py`, `tests/integration/test_integration_capabilities_api.py`, `docs/technical/runtime_topology.md`; validation: `python -m pytest tests/unit/services/test_compute_job_store.py tests/unit/services/test_lineage_metadata_store.py tests/unit/services/test_queue_metrics_service.py tests/integration/test_integration_capabilities_api.py -q`, `python -m ruff check app/services/compute_job_store.py app/services/lineage_metadata_store.py app/services/queue_metrics_service.py app/observability.py tests/unit/services/test_compute_job_store.py tests/unit/services/test_lineage_metadata_store.py tests/unit/services/test_queue_metrics_service.py tests/integration/test_integration_capabilities_api.py`, `python -m mypy --config-file mypy.ini app/services/compute_job_store.py app/services/lineage_metadata_store.py app/services/queue_metrics_service.py app/observability.py`. |
| LP-CR-006 | 2026-03-13 | Control-plane visibility for durable runtime queues | Hardened | After queue metrics were added, operators still had no direct JSON read surface for current queue backlog, draining state, or degradation reasons. The only durable drill-down path was execution-by-execution polling, which is too narrow for runtime support. | Added a shared runtime-status service that aggregates draining state, durable metadata-store readiness, and current compute/lineage queue status, then exposed it as `GET /integration/runtime-status`. Added unit tests for ready/draining/degraded service behavior and integration tests for live API responses. Updated runtime and API-reference docs to include the control-plane endpoint. | Next: review whether queue snapshot payloads should include worker throughput and retry-rate counters or whether Prometheus remains the right surface for time-series behavior. Current support need for point-in-time backlog inspection is closed. | Files: `app/services/runtime_status_service.py`, `app/api/endpoints/runtime_status.py`, `main.py`, `tests/unit/services/test_runtime_status_service.py`, `tests/integration/test_runtime_status_api.py`, `docs/guides/api_reference.md`, `docs/technical/runtime_topology.md`; validation: `python -m pytest tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py -q`, `python -m ruff check app/services/runtime_status_service.py app/api/endpoints/runtime_status.py main.py tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py`, `python -m mypy --config-file mypy.ini app/services/runtime_status_service.py app/api/endpoints/runtime_status.py`. |
| LP-CR-007 | 2026-03-13 | Hot-path durable query indexing for compute and lineage queues | Hardened | The executor and lineage worker query shapes were reasonable, but the durable tables did not declare indexes aligned with their primary access paths. Pending-job lease scans, stale-lease reconciliation, pending lineage payload scans, and oldest-pending calculations would all degrade into avoidable table scans as backlog grows. | Added explicit SQL indexes for compute job status/created-at, compute job status/type/created-at, compute job status/lease-expiry, lineage record status, and lineage payload created-at. Added lower-level schema tests that assert those indexes are actually created. | Next: if PostgreSQL becomes the only supported durable runtime store, consider validating `EXPLAIN` plans or adding partial-index migrations tuned to the final production dialect. Current repo-level hot-path index gap is closed. | Files: `app/services/compute_job_store.py`, `app/services/lineage_metadata_store.py`, `tests/unit/services/test_compute_job_store.py`, `tests/unit/services/test_lineage_metadata_store.py`; validation: `python -m pytest tests/unit/services/test_compute_job_store.py tests/unit/services/test_lineage_metadata_store.py -q`, `python -m ruff check app/services/compute_job_store.py app/services/lineage_metadata_store.py tests/unit/services/test_compute_job_store.py tests/unit/services/test_lineage_metadata_store.py`, `python -m mypy --config-file mypy.ini app/services/compute_job_store.py app/services/lineage_metadata_store.py`. |
| LP-CR-008 | 2026-03-13 | Worker quiescence and shutdown semantics | Hardened | Both worker runtimes were open-ended `while True` poll loops with no explicit stop contract. That made graceful shutdown, controlled test orchestration, and replay-safe draining depend on external process kill behavior instead of an owned runtime boundary. | Added explicit `stop_event` support to the compute executor and lineage worker loops, including idle-wait interruption helpers. Added lower-level tests proving immediate stop and stop-during-idle-wait behavior, and updated runtime topology docs to reflect explicit quiescence support. | Next: if the platform adds a process supervisor contract, thread that stop signal through the container entrypoints so runtime quiescence is exercised in deployment, not only in-process tests. | Files: `app/workers/compute_executor_worker.py`, `app/workers/lineage_worker.py`, `tests/unit/services/test_compute_executor_worker.py`, `tests/unit/services/test_lineage_worker.py`, `docs/technical/runtime_topology.md`; validation: `python -m pytest tests/unit/services/test_compute_executor_worker.py tests/unit/services/test_lineage_worker.py -q`, `python -m ruff check app/workers/compute_executor_worker.py app/workers/lineage_worker.py tests/unit/services/test_compute_executor_worker.py tests/unit/services/test_lineage_worker.py`, `python -m mypy --config-file mypy.ini app/workers/compute_executor_worker.py app/workers/lineage_worker.py`. |
| LP-CR-009 | 2026-03-13 | Executor failure-handling duplication | Hardened | The compute executor repeated terminal-failure side effects across reconciled-job failure, exhausted-retry failure, and non-retryable failure branches. That duplication meant async-result recording and execution-registry failure transitions could drift independently over time. | Extracted a shared terminal-failure helper in the compute worker and added a lower-level unit proof that missing execution records still yield durable async failure state plus operator-visible logging. | Next: if more executor job types or failure policies are added, consider moving terminal/retry transition handling into a dedicated runtime service rather than growing worker-local helpers further. | Files: `app/workers/compute_executor_worker.py`, `tests/unit/services/test_compute_executor_worker.py`; validation: `python -m pytest tests/unit/services/test_compute_executor_worker.py -q`, `python -m ruff check app/workers/compute_executor_worker.py tests/unit/services/test_compute_executor_worker.py`, `python -m mypy --config-file mypy.ini app/workers/compute_executor_worker.py`. |
| LP-CR-010 | 2026-03-13 | Public documentation contract drift and broken methodology navigation | Hardened | Public-facing docs had drifted materially from the shipped contract. `README.md` still showed older request shapes, the user guide omitted async execution and runtime support surfaces, `twr.md` still documented `period_type`/`daily_data`, `mwr.md` contained citation debris and overstated method reality, the standalone-engine guide no longer matched `run_calculations(...)`, and the methodology index referenced a documentation structure that no longer served as the canonical navigation model. | Rewrote the README, user guide, TWR guide, MWR guide, standalone-engine guide, and methodology index around the current contract and runtime model. Added a docs regression test pack that checks these pages for current request shape, async/runtime-support visibility, and engine-API alignment. | Next: continue with deeper methodology-quality review for contribution and attribution guides if those pages need to be upgraded to the stricter metric-doc standard rather than endpoint-guide level. | Files: `README.md`, `docs/Portfolio Performance Analytics - A User Guide.md`, `docs/guides/twr.md`, `docs/guides/mwr.md`, `docs/guides/standalone_engine_usage.md`, `docs/technical/methodology_index.md`, `tests/unit/docs/test_public_docs_contract.py`; validation: `python -m pytest tests/unit/docs/test_public_docs_contract.py -q`, `python -m ruff check tests/unit/docs/test_public_docs_contract.py`, `python scripts/openapi_quality_gate.py`. |
| LP-CR-011 | 2026-03-13 | Contribution and attribution guide contract drift | Hardened | After the first docs pass, the contribution and attribution guides were still on older request structures, including nested `daily_data`, request-level `period_type`, and response narratives that did not reflect current async execution behavior or `results_by_period` output shape. | Rewrote the contribution and attribution guides around the current request and response contracts, including async accepted/result flows and current grouping inputs. Extended the docs regression test pack so these two guides are checked alongside README, TWR, MWR, and methodology navigation. | Next: if we want audit-grade methodology docs rather than endpoint guides, promote the deeper formulas and variable dictionaries into the metric methodology set rather than re-expanding endpoint guides into duplicated field catalogs. | Files: `docs/guides/contribution.md`, `docs/guides/attribution.md`, `tests/unit/docs/test_public_docs_contract.py`; validation: `python -m pytest tests/unit/docs/test_public_docs_contract.py -q`, `python -m ruff check tests/unit/docs/test_public_docs_contract.py`. |
| LP-CR-012 | 2026-03-14 | Metric methodology rigor and anti-drift guardrails | Hardened | The metric methodology set had become a high-value but weakly-governed artifact. Most metric docs were already close to the v3 structure, but the repository had no regression guard for section order, worked-example completeness, or master-index coverage. That meant future drift could silently erode auditability even if a few individual docs looked good at review time. | Tightened the two foundational methodology documents for TWR base return and MWR XIRR around exact engine behavior, including clearer symbol definitions, explicit fallback and solver semantics, and more precise output mapping. Added a dedicated methodology-doc regression pack that enforces the v3 section order, requires worked-example output mapping on every metric doc, and checks that the master index covers the full metric-doc set. Updated the methodology master index to describe the enforced v3 standard instead of the older generic template language. | Next: continue the same methodology-depth review on contribution and attribution metrics if we want formula-by-formula audit narration that is as explicit as the foundational return metrics. The anti-drift guard is now in place for the whole metric-doc corpus. | Files: `docs/methodologies/metrics/metric-twr-base-return.md`, `docs/methodologies/metrics/metric-mwr-xirr.md`, `docs/methodologies/metrics/master-index.md`, `tests/unit/docs/test_metric_methodology_docs.py`; validation: `python -m pytest tests/unit/docs/test_metric_methodology_docs.py -q`, `python -m ruff check tests/unit/docs/test_metric_methodology_docs.py`, `python scripts/openapi_quality_gate.py`. |
| LP-CR-013 | 2026-03-14 | Contribution weight division fencing on zero portfolio capital | Hardened | Reviewing contribution methodology against the engine exposed a real correctness gap: `_calculate_daily_instrument_contributions(...)` documented zero-capital weight division as mapping to `0`, but the implementation only filled `NaN`. When portfolio capital was `0` and instrument capital was non-zero, the division could produce `inf`, contaminating raw and smoothed contribution totals. | Normalized the daily weight series by replacing `+/-inf` with `NaN` before `fillna(0.0)`, ensuring zero-capital portfolio days cannot leak infinite weights or contributions into flat or hierarchical aggregation paths. Added a focused unit test that constructs the zero-portfolio-capital edge case and proves weight and contribution outputs are forced to zero. | Next: continue the methodology-depth review for contribution and attribution docs now that the documented zero-weight behavior is truly implementation-backed. | Files: `engine/contribution.py`, `tests/unit/engine/test_contribution.py`; validation: `python -m pytest tests/unit/engine/test_contribution.py -q`, `python -m ruff check engine/contribution.py tests/unit/engine/test_contribution.py`. |
| LP-CR-014 | 2026-03-14 | Hierarchical contribution period-collapse bug | Hardened | Hierarchical contribution requests were contractually modeled as `results_by_period`, but the implementation aggregated across the full master window once and then attached that aggregate only to the first resolved period. Multi-period hierarchy requests therefore returned incorrect results and silently dropped later periods. | Refactored hierarchical aggregation into a reusable period-slice helper, extracted shared portfolio-period return computation in the contribution service, and changed hierarchy mode to iterate resolved periods the same way as the flat path. Added an integration test that proves `MTD` and `YTD` hierarchy results are both returned with distinct values. | Next: align the contribution methodology docs to state the per-period hierarchy behavior explicitly now that the runtime path is fixed and test-backed. | Files: `app/services/contribution_service.py`, `engine/contribution.py`, `tests/integration/test_contribution_api.py`; validation: `python -m pytest tests/integration/test_contribution_api.py -q`, `python -m ruff check app/services/contribution_service.py engine/contribution.py tests/integration/test_contribution_api.py`. |
| LP-CR-015 | 2026-03-14 | Dead attribution response contract surface | Hardened | `currency_attribution_totals` and its companion model existed in the attribution response schema, but nothing in runtime, tests, or docs ever populated it. Keeping an unimplemented response field in a banking analytics API is avoidable contract noise and invites false expectations in downstream clients and Swagger. | Removed the dead response model and field from the attribution schema and added a model-schema test that proves the field is absent. This narrows the public contract to the attribution outputs the service actually computes today. | Next: if aggregate currency-attribution totals are needed later, add them only with an implemented computation path, reconciliation semantics, and contract tests. | Files: `app/models/attribution_responses.py`, `tests/unit/models/test_attribution_models.py`; validation: `python -m pytest tests/unit/models/test_attribution_models.py -q`, `python -m ruff check app/models/attribution_responses.py tests/unit/models/test_attribution_models.py`, `python scripts/openapi_quality_gate.py`. |
| LP-CR-016 | 2026-03-14 | Attribution zero-weight and zero-capital division fencing | Hardened | Attribution instrument preparation still had two non-finite division paths inside the derived `by_instrument` path: instrument `weight_bop` could go infinite when portfolio beginning capital was zero, and grouped portfolio returns could go non-finite when a derived group's summed weight was zero. Both cases would pollute aligned panels and create brittle downstream effect calculations. | Hardened `_prepare_data_from_instruments(...)` by replacing `+/-inf` with `NaN` and then forcing deterministic `0.0` weights/returns on the derived instrument/group rollup path. Added a lower-level unit test proving the zero-portfolio-capital path produces finite zero outputs instead of leaking non-finite values into attribution. Explicitly did not coerce caller-supplied `by_group` returns, because those are authoritative request inputs rather than derived values. | Next: continue the attribution methodology pass now that the derived zero-weight runtime behavior is deterministic and test-backed. | Files: `engine/attribution.py`, `tests/unit/engine/test_attribution.py`; validation: `python -m pytest tests/unit/engine/test_attribution.py -q`, `python -m ruff check engine/attribution.py tests/unit/engine/test_attribution.py`. |
| LP-CR-017 | 2026-03-14 | Attribution guide and methodology response-ownership drift | Hardened | The attribution guide still implied `model` and `linking` were period-level fields, even though the shipped response keeps them at the top level. That kind of misplaced field ownership is small but expensive drift in consumer-facing docs because it pushes clients toward the wrong response shape. | Updated the attribution guide example and response-shape section to show `model` and `linking` at top level and `currency_attribution` as the optional per-period extension. Tightened the active-return methodology doc to note that `model` and `linking` are response-level context, not period-level fields. Extended the docs regression pack so this shape stays enforced. | Next: continue the attribution methodology-depth pass on allocation/selection/interaction docs if we want the same audit-grade precision now present in the TWR/MWR/contribution set. | Files: `docs/guides/attribution.md`, `docs/methodologies/metrics/metric-attribution-active-return.md`, `tests/unit/docs/test_public_docs_contract.py`; validation: `python -m pytest tests/unit/docs/test_public_docs_contract.py tests/unit/engine/test_attribution.py tests/unit/models/test_attribution_models.py -q`, `python scripts/openapi_quality_gate.py`. |
| LP-CR-018 | 2026-03-14 | Attribution methodology activation-rule drift and dead fallback cleanup | Hardened | The attribution engine still carried a dead fallback for `analyses`, even though the request model forbids an empty list. Separately, the currency-attribution docs described the feature as a generic `currency_mode=BOTH` path without making the real activation rule explicit: the engine only emits currency attribution when `group_by` includes `currency` and the aligned panel carries the required local/FX columns. | Removed the dead `request.analyses` fallback branch in the instrument-preparation path and updated the attribution guide plus currency-attribution methodology docs to state the exact activation conditions for currency attribution. Extended the docs regression pack so the `group_by`/`currency` requirement is now explicit and test-backed. | Next: continue tightening allocation/selection/interaction methodology wording around the top-down linking factor and hierarchy aggregation if we want full v3-style audit precision across the attribution set. | Files: `engine/attribution.py`, `docs/guides/attribution.md`, `docs/methodologies/metrics/metric-currency-allocation.md`, `docs/methodologies/metrics/metric-currency-selection.md`, `docs/methodologies/metrics/metric-currency-local-allocation.md`, `docs/methodologies/metrics/metric-currency-local-selection.md`, `tests/unit/docs/test_public_docs_contract.py`; validation: `python -m pytest tests/unit/docs/test_public_docs_contract.py tests/unit/engine/test_attribution.py -q`, `python -m ruff check engine/attribution.py tests/unit/docs/test_public_docs_contract.py tests/unit/engine/test_attribution.py`. |
| LP-CR-019 | 2026-03-14 | Attribution formula auditability and linking-proof coverage | Hardened | The attribution engine had correct core formulas, but the lower-level tests mostly asserted reconciled totals rather than pinning the exact Brinson formulas and top-down linking behavior row by row. The methodology docs also described linking only generically, which is weaker than an audit-grade explanation for a banking analytics codebase. | Added explicit unit tests for exact Brinson-Fachler and Brinson-Hood-Beebower single-period formulas and for `_link_effects_top_down(...)` scaling only the effect columns. Upgraded the allocation, selection, and interaction methodology docs to define `AR_geo`, `AR_arith`, and `scale`, show the exact per-model formulas with period/group symbols, and describe the resample-plus-link flow used by the engine. Added a docs regression guard so those three metric docs must continue to name the top-down linking factor explicitly. | Next: continue the same audit-grade uplift on the four currency-attribution metric docs if we want their methodology set to match the same precision as the core attribution effects. | Files: `tests/unit/engine/test_attribution.py`, `docs/methodologies/metrics/metric-attribution-allocation.md`, `docs/methodologies/metrics/metric-attribution-selection.md`, `docs/methodologies/metrics/metric-attribution-interaction.md`, `tests/unit/docs/test_metric_methodology_docs.py`; validation: `python -m pytest tests/unit/engine/test_attribution.py tests/unit/docs/test_metric_methodology_docs.py -q`, `python -m ruff check tests/unit/engine/test_attribution.py tests/unit/docs/test_metric_methodology_docs.py`, `python scripts/openapi_quality_gate.py`. |
| LP-CR-020 | 2026-03-14 | Currency attribution formula auditability and aggregation semantics | Hardened | Currency attribution still relied mainly on one integration happy path. That proved the end-to-end total once, but it did not pin the exact four Karnosky-Singer formulas at the engine level or require the methodology docs to define how each effect contributes to per-currency `total_effect`. | Added a lower-level unit test for `_calculate_currency_attribution_effects(...)` covering all four exact formulas. Upgraded the four currency-attribution methodology docs to define aggregated symbols like `LA_c`, `LS_c`, `CA_c`, `CS_c`, and `TE_c`, and to state explicitly that each effect contributes to `currency_attribution[].effects.total_effect`. Added a docs regression guard that requires those docs to describe the total-effect relationship. | Next: if we want one more high-value pass, it should be removing the remaining stale `START FIX` comment markers scattered across the engine/tests where the fix is long since normalized and the comments now add noise rather than clarity. | Files: `tests/unit/engine/test_attribution.py`, `docs/methodologies/metrics/metric-currency-local-allocation.md`, `docs/methodologies/metrics/metric-currency-local-selection.md`, `docs/methodologies/metrics/metric-currency-allocation.md`, `docs/methodologies/metrics/metric-currency-selection.md`, `tests/unit/docs/test_metric_methodology_docs.py`; validation: `python -m pytest tests/unit/engine/test_attribution.py tests/unit/docs/test_metric_methodology_docs.py -q`, `python -m ruff check tests/unit/engine/test_attribution.py tests/unit/docs/test_metric_methodology_docs.py`, `python scripts/openapi_quality_gate.py`. |
| LP-CR-021 | 2026-03-14 | Stale fix-marker comment cleanup in reviewed attribution scope | Hardened | After the attribution runtime and methodology passes, several files still carried `START FIX` / `END FIX` and similar migration markers even though the behavior is now the steady-state implementation. Those comments add noise, imply temporary code where none exists, and make reviewed files harder to read. | Removed stale fix-marker comments from the attribution request model and attribution unit/model tests, keeping only comments that still explain real domain intent. This is cleanup only; no behavior changed. | Next: if we want to continue this cleanup pattern, apply it surgically in other reviewed areas rather than globally to avoid churn. | Files: `app/models/attribution_requests.py`, `tests/unit/engine/test_attribution.py`, `tests/unit/models/test_attribution_models.py`; validation: `python -m pytest tests/unit/engine/test_attribution.py tests/unit/models/test_attribution_models.py -q`, `python -m ruff check app/models/attribution_requests.py tests/unit/engine/test_attribution.py tests/unit/models/test_attribution_models.py`. |
| LP-CR-022 | 2026-03-14 | Stale refactor-marker cleanup in shared analytics contract models | Hardened | Shared request/response models and one attribution integration assertion still carried `START REFACTOR` / `START FIX` style comments even though the unified `analyses` model and period-prefixed lineage artifact behavior are now the normal contract. Those markers add noise in high-traffic contract files and make the code read like a diff instead of a maintained system. | Removed stale refactor/fix markers from the shared performance request model, contribution request model, attribution response model, and the attribution integration test. This is behavior-preserving cleanup only. | Next: if we continue this pattern, keep it limited to reviewed files with stabilized behavior rather than bulk-editing the whole repo. | Files: `app/models/requests.py`, `app/models/contribution_requests.py`, `app/models/attribution_responses.py`, `tests/integration/test_attribution_api.py`; validation: `python -m pytest tests/integration/test_attribution_api.py tests/unit/models/test_attribution_models.py -q`, `python -m ruff check app/models/requests.py app/models/contribution_requests.py app/models/attribution_responses.py tests/integration/test_attribution_api.py tests/unit/models/test_attribution_models.py`. |
| LP-CR-023 | 2026-03-14 | Duplicate-submission fencing and async replay correctness | Hardened | The executor-backed submission path still used `merge` semantics at the durable boundary. Reusing a `calculation_id` could silently upsert execution or compute-job rows instead of distinguishing a safe idempotent replay from a conflicting payload drift. That is an operational correctness risk in a banking analytics service because replay, reconciliation, and operator polling all depend on `calculation_id` being a trustworthy execution handle rather than an ambient overwrite key. | Added durable registration outcomes to `ExecutionRegistry` and `ComputeJobStore` so the system now distinguishes `created`, `replay`, and `conflict` at the database boundary. Extracted a shared submission-fencing service used by returns-series, contribution, and attribution async endpoints, and used the same fence to reject reused `calculation_id` values on synchronous submission paths. Added unit proofs for registry/job registration semantics, integration tests proving exact async replay returns the same accepted handle while payload drift yields `409 Conflict`, an E2E executor replay journey, and public API guidance that now documents the `calculation_id` replay contract explicitly. | Next: continue the gold-standard review on executor operational recovery and backlog behavior under multi-job load; duplicate-submission correctness and client guidance are now both covered. | Files: `app/services/execution_registry.py`, `app/services/compute_job_store.py`, `app/services/submission_fencing_service.py`, `app/api/endpoints/returns_series.py`, `app/api/endpoints/contribution.py`, `app/api/endpoints/performance.py`, `tests/unit/services/test_execution_registry.py`, `tests/unit/services/test_compute_job_store.py`, `tests/integration/test_returns_series_api.py`, `tests/integration/test_contribution_api.py`, `tests/integration/test_attribution_api.py`, `tests/e2e/test_workflow_journeys.py`, `docs/guides/api_reference.md`, `README.md`; validation: `python -m pytest tests/unit/services/test_execution_registry.py tests/unit/services/test_compute_job_store.py tests/integration/test_returns_series_api.py tests/integration/test_contribution_api.py tests/integration/test_attribution_api.py tests/e2e/test_workflow_journeys.py -q`, `python -m ruff check app/services/execution_registry.py app/services/compute_job_store.py app/services/submission_fencing_service.py app/api/endpoints/returns_series.py app/api/endpoints/contribution.py app/api/endpoints/performance.py tests/unit/services/test_execution_registry.py tests/unit/services/test_compute_job_store.py tests/integration/test_returns_series_api.py tests/integration/test_contribution_api.py tests/integration/test_attribution_api.py tests/e2e/test_workflow_journeys.py`, `python scripts/openapi_quality_gate.py`. |
| LP-CR-024 | 2026-03-14 | Runtime backlog visibility for stuck compute work | Hardened | The runtime control plane and Prometheus metrics exposed queue counts plus oldest pending age, but they could not distinguish "healthy backlog" from "stuck in-flight work." Under multiple executor-backed jobs, operators had no first-class signal for the oldest leased or oldest running compute job, which weakens incident triage for hung workers and slow drains. | Extended `ComputeQueueStats` with oldest leased and oldest running ages, surfaced those fields through `/integration/runtime-status`, and exported matching Prometheus gauges. Added lower-level store, service, and metrics proofs plus integration assertions on runtime-status and `/metrics`. Updated runtime-topology and API-reference docs so operator guidance matches the shipped control-plane contract. | Next: if we keep pushing this pattern, the next operational step is explicit stale-job counters or degraded-state thresholds once real SLOs are set. The core observability gap around in-flight compute age is now closed. | Files: `app/services/compute_job_store.py`, `app/services/queue_metrics_service.py`, `app/api/endpoints/runtime_status.py`, `tests/unit/services/test_compute_job_store.py`, `tests/unit/services/test_runtime_status_service.py`, `tests/unit/services/test_queue_metrics_service.py`, `tests/integration/test_runtime_status_api.py`, `tests/integration/test_integration_capabilities_api.py`, `docs/technical/runtime_topology.md`, `docs/guides/api_reference.md`; validation: `python -m pytest tests/unit/services/test_compute_job_store.py tests/unit/services/test_runtime_status_service.py tests/unit/services/test_queue_metrics_service.py tests/integration/test_runtime_status_api.py tests/integration/test_integration_capabilities_api.py -q`, `python -m ruff check app/services/compute_job_store.py app/services/queue_metrics_service.py app/api/endpoints/runtime_status.py tests/unit/services/test_compute_job_store.py tests/unit/services/test_runtime_status_service.py tests/unit/services/test_queue_metrics_service.py tests/integration/test_runtime_status_api.py tests/integration/test_integration_capabilities_api.py`, `python -m mypy --config-file mypy.ini app/services/compute_job_store.py app/services/queue_metrics_service.py app/api/endpoints/runtime_status.py`. |
| LP-CR-025 | 2026-03-14 | Proactive runtime degradation on queue-age thresholds | Hardened | Even after adding in-flight age signals, runtime support still depended on humans interpreting raw ages manually. The control plane would remain `ready` while a compute or lineage queue silently exceeded the backlog or stuck-work age that operations would treat as unhealthy. | Added configurable age thresholds for compute pending, leased, running, and lineage pending work, and taught `build_runtime_status_snapshot(...)` to mark queue state and aggregate runtime state as `degraded` when those limits are crossed. Added unit proofs for compute and lineage threshold degradation and an integration test that manipulates durable compute-job age and proves `/integration/runtime-status` degrades automatically. Updated operator docs to state that runtime status can degrade proactively on configured queue-age thresholds. | Next: the next operational hardening step is exposing stale-job counters and, if we define production SLOs, alert recommendations or defaults for these thresholds. The core shift from passive reporting to actionable runtime-state degradation is now implemented. | Files: `app/core/config.py`, `app/services/runtime_status_service.py`, `tests/unit/services/test_runtime_status_service.py`, `tests/integration/test_runtime_status_api.py`, `docs/technical/runtime_topology.md`, `docs/guides/api_reference.md`; validation: `python -m pytest tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py -q`, `python -m ruff check app/core/config.py app/services/runtime_status_service.py tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py`, `python -m mypy --config-file mypy.ini app/core/config.py app/services/runtime_status_service.py`. |
| LP-CR-026 | 2026-03-14 | Failure-pressure visibility for executor-backed runtime support | Hardened | Queue age alone still left a blind spot: operators could see that work was old, but not whether the system was building retry backlog, accumulating lease-expiry recoveries, or collecting terminal compute failures. That makes support slower because "slow queue" and "failing queue" require different responses. | Extended `ComputeQueueStats`, `/integration/runtime-status`, and Prometheus metrics to expose retry-backlog, lease-expiry, and terminal-failure counts. Added lower-level queue-stat proofs plus integration assertions that runtime-status and `/metrics` carry those failure-pressure signals. Updated runtime and API docs so operators know where to read the new counters. | Next: the next gold-standard operational step is deciding whether specific failure-pressure thresholds should also degrade runtime status or trigger explicit alert guidance once production SLOs are agreed. The visibility gap itself is now closed. | Files: `app/services/compute_job_store.py`, `app/api/endpoints/runtime_status.py`, `app/services/queue_metrics_service.py`, `tests/unit/services/test_compute_job_store.py`, `tests/unit/services/test_runtime_status_service.py`, `tests/unit/services/test_queue_metrics_service.py`, `tests/integration/test_runtime_status_api.py`, `tests/integration/test_integration_capabilities_api.py`, `docs/technical/runtime_topology.md`, `docs/guides/api_reference.md`; validation: `python -m pytest tests/unit/services/test_compute_job_store.py tests/unit/services/test_runtime_status_service.py tests/unit/services/test_queue_metrics_service.py tests/integration/test_runtime_status_api.py tests/integration/test_integration_capabilities_api.py -q`, `python -m ruff check app/services/compute_job_store.py app/api/endpoints/runtime_status.py app/services/queue_metrics_service.py tests/unit/services/test_compute_job_store.py tests/unit/services/test_runtime_status_service.py tests/unit/services/test_queue_metrics_service.py tests/integration/test_runtime_status_api.py tests/integration/test_integration_capabilities_api.py`, `python -m mypy --config-file mypy.ini app/services/compute_job_store.py app/api/endpoints/runtime_status.py app/services/queue_metrics_service.py`. |
| LP-CR-027 | 2026-03-14 | Lineage worker bounded retries and lineage failure-pressure visibility | Hardened | The lineage worker incremented attempt count but had no bounded retry policy, so a bad lineage payload could churn indefinitely. At the same time, runtime support could not distinguish healthy lineage backlog from retried or terminally failed lineage materialization. | Added `LINEAGE_WORKER_MAX_ATTEMPTS`, changed lineage materialization to return explicit success/failure to the worker, and taught the worker to retry within budget and mark terminal failure once the retry budget is exhausted. Extended lineage queue stats, runtime-status, and Prometheus metrics with retry-backlog and terminal-failure counts. Added lower-level store/service/worker proofs and integration assertions for the new lineage support signals. Updated runtime and API docs so lineage support behavior matches the shipped runtime. | Next: the next strong operational step is deciding whether lineage failure-pressure should also participate in active degradation thresholds or alert guidance once production expectations are fixed. The infinite-retry and hidden-failure-pressure gap is now closed. | Files: `app/core/config.py`, `app/services/lineage_metadata_store.py`, `app/services/lineage_service.py`, `app/workers/lineage_worker.py`, `app/api/endpoints/runtime_status.py`, `app/services/queue_metrics_service.py`, `tests/unit/services/test_lineage_metadata_store.py`, `tests/unit/services/test_lineage_service.py`, `tests/unit/services/test_lineage_worker.py`, `tests/unit/services/test_runtime_status_service.py`, `tests/unit/services/test_queue_metrics_service.py`, `tests/integration/test_runtime_status_api.py`, `tests/integration/test_integration_capabilities_api.py`, `docs/technical/runtime_topology.md`, `docs/guides/api_reference.md`; validation: `python -m pytest tests/unit/services/test_lineage_metadata_store.py tests/unit/services/test_lineage_service.py tests/unit/services/test_lineage_worker.py tests/unit/services/test_runtime_status_service.py tests/unit/services/test_queue_metrics_service.py tests/integration/test_runtime_status_api.py tests/integration/test_integration_capabilities_api.py -q`, `python -m ruff check app/core/config.py app/services/lineage_metadata_store.py app/services/lineage_service.py app/workers/lineage_worker.py app/api/endpoints/runtime_status.py app/services/queue_metrics_service.py tests/unit/services/test_lineage_metadata_store.py tests/unit/services/test_lineage_service.py tests/unit/services/test_lineage_worker.py tests/unit/services/test_runtime_status_service.py tests/unit/services/test_queue_metrics_service.py tests/integration/test_runtime_status_api.py tests/integration/test_integration_capabilities_api.py`, `python -m mypy --config-file mypy.ini app/core/config.py app/services/lineage_metadata_store.py app/services/lineage_service.py app/workers/lineage_worker.py app/api/endpoints/runtime_status.py app/services/queue_metrics_service.py`. |
| LP-CR-028 | 2026-03-14 | Active runtime degradation on failure-pressure thresholds | Hardened | Queue age and failure-pressure counters were visible, but runtime-status still treated them as passive telemetry. Operators could see retry backlog, lease-expiry recoveries, or terminal failures growing without the control plane itself reflecting that the runtime had crossed unhealthy operational conditions. | Added configurable degradation thresholds for compute retry backlog, compute lease-expiry recoveries, compute terminal failures, lineage retry backlog, and lineage terminal failures. Taught `build_runtime_status_snapshot(...)` to degrade queue state and aggregate runtime state when those thresholds are crossed, and added unit plus integration proofs for both compute and lineage failure-pressure degradation. Updated runtime and API docs so operator guidance matches the active degradation behavior. | Next: the next operational step is to decide production default thresholds and alert-routing guidance so these policies are not just configurable but operationally standardized. The control plane now escalates failure pressure instead of only surfacing raw counters. | Files: `app/core/config.py`, `app/services/runtime_status_service.py`, `tests/unit/services/test_runtime_status_service.py`, `tests/integration/test_runtime_status_api.py`, `docs/technical/runtime_topology.md`, `docs/guides/api_reference.md`; validation: `python -m pytest tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py -q`, `python -m ruff check app/core/config.py app/services/runtime_status_service.py tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py`, `python -m mypy --config-file mypy.ini app/core/config.py app/services/runtime_status_service.py`. |
| LP-CR-029 | 2026-03-14 | Runtime-status actionability under multiple simultaneous degradations | Hardened | Even after active degradation was added, the control plane still surfaced only one `reason` per queue. When several thresholds were breached together, operators had to infer the full failure picture from raw counters, which is weaker than an actionable support contract. | Extended the runtime-status service and API contract so each queue now exposes a `degradation_reasons` list while retaining `reason` as the primary summary, and added a top-level `runtime_degradation_reasons` list that aggregates all queue-level degradation and unavailability signals. Added unit and integration proofs for multiple simultaneous compute and lineage degradations, and updated runtime/API docs to reflect the richer operator contract. | Next: the next worthwhile step in this area is standardizing production threshold defaults and alert-routing guidance so the richer control plane maps directly to platform operations policy. | Files: `app/services/runtime_status_service.py`, `app/api/endpoints/runtime_status.py`, `tests/unit/services/test_runtime_status_service.py`, `tests/integration/test_runtime_status_api.py`, `docs/technical/runtime_topology.md`, `docs/guides/api_reference.md`; validation: `python -m pytest tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py -q`, `python -m ruff check app/services/runtime_status_service.py app/api/endpoints/runtime_status.py tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py`, `python -m mypy --config-file mypy.ini app/services/runtime_status_service.py app/api/endpoints/runtime_status.py`. |
| LP-CR-030 | 2026-03-14 | Hot-path queue-stat query shape for runtime status and metrics | Hardened | Once runtime-status and Prometheus started exposing richer queue pressure, the underlying store methods became a hot path. Both `get_queue_stats(...)` and `get_pending_payload_stats(...)` were pulling row sets into Python and deriving retry/failure pressure there, which creates avoidable table-scan pressure on the exact support endpoints operators depend on during backlog incidents. | Reworked compute and lineage queue-stat retrieval into SQL-side aggregate queries using conditional sums and conditional minimum timestamps, preserving the existing contract while removing Python-side row walks from the hot path. Added lower-level query-shape tests that prove each stats read executes as a single `SELECT`, and updated runtime-topology docs to note the bounded-query behavior. | Next: if this control-plane area keeps growing, consider adding repository-level perf characterization or `EXPLAIN` plan checks for PostgreSQL specifically. The immediate hot-path shape problem is closed. | Files: `app/services/compute_job_store.py`, `app/services/lineage_metadata_store.py`, `tests/unit/services/test_compute_job_store.py`, `tests/unit/services/test_lineage_metadata_store.py`, `docs/technical/runtime_topology.md`; validation: `python -m pytest tests/unit/services/test_compute_job_store.py tests/unit/services/test_lineage_metadata_store.py tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py -q`, `python -m ruff check app/services/compute_job_store.py app/services/lineage_metadata_store.py tests/unit/services/test_compute_job_store.py tests/unit/services/test_lineage_metadata_store.py`, `python -m mypy --config-file mypy.ini app/services/compute_job_store.py app/services/lineage_metadata_store.py`. |
| LP-CR-031 | 2026-03-14 | Runtime degradation-policy discoverability | Hardened | Even with degradation reasons and counters exposed, operators still had to know the active configured thresholds out of band. That weakens incident handling because the control plane could say "threshold exceeded" without telling support what threshold was actually in force on that runtime. | Extended the runtime-status snapshot and API contract to expose active compute and lineage degradation-policy thresholds alongside current queue state. Added unit and integration proofs that the live control plane returns those policy values, and updated runtime/API docs so support guidance matches the shipped contract. | Next: the remaining gap in this area is platform-level standardization of default thresholds and alert routing. The service-level control plane now exposes both live queue state and the policy used to interpret it. | Files: `app/services/runtime_status_service.py`, `app/api/endpoints/runtime_status.py`, `tests/unit/services/test_runtime_status_service.py`, `tests/integration/test_runtime_status_api.py`, `docs/technical/runtime_topology.md`, `docs/guides/api_reference.md`; validation: `python -m pytest tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py -q`, `python -m ruff check app/services/runtime_status_service.py app/api/endpoints/runtime_status.py tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py`, `python -m mypy --config-file mypy.ini app/services/runtime_status_service.py app/api/endpoints/runtime_status.py`. |
| LP-CR-032 | 2026-03-14 | Runtime degradation breach-detail quality | Hardened | Even after reasons and live policy were exposed, operators still had to join those two views mentally to understand severity. A degraded runtime could say which rule fired and what threshold existed, but not the observed value that actually crossed the line. | Extended runtime-status to expose detailed degradation payloads at queue and aggregate level, with `reason`, `observed_value`, and `threshold_value` for each active breach. Added unit and integration proofs for age-based and count-based breaches, including multi-breach snapshots, and updated runtime/API docs so incident support has one machine-readable control-plane source. | Next: the remaining work in this lane is platform-level defaulting and alert policy, not missing service-level evidence in the runtime contract. | Files: `app/services/runtime_status_service.py`, `app/api/endpoints/runtime_status.py`, `tests/unit/services/test_runtime_status_service.py`, `tests/integration/test_runtime_status_api.py`, `docs/technical/runtime_topology.md`, `docs/guides/api_reference.md`; validation: `python -m pytest tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py -q`, `python -m ruff check app/services/runtime_status_service.py app/api/endpoints/runtime_status.py tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py`, `python -m mypy --config-file mypy.ini app/services/runtime_status_service.py app/api/endpoints/runtime_status.py`. |
| LP-CR-033 | 2026-03-14 | Warning-clean enum contract hygiene in returns-series helper tests | Hardened | A full-suite run was green but still emitted Pydantic serializer warnings from test-only `model_construct(...)` paths that injected raw `"NET"` strings into the `MetricBasis` enum field. That left the test suite technically passing but not warning-clean, which weakens signal quality for future failures. | Replaced the raw string with the actual `MetricBasis.NET` enum in the affected returns-series helper tests so the tests exercise the same enum contract shape as production code while preserving the lightweight `model_construct(...)` path. Re-ran the targeted helper pack and then the full suite to confirm the warning path is gone. | Next: keep the suite warning-clean as part of the review program; future warning regressions should be treated as contract hygiene issues, not background noise. | Files: `tests/unit/app/test_returns_series_endpoint_helpers.py`; validation: `python -m pytest tests/unit/app/test_returns_series_endpoint_helpers.py -q`, `python -m ruff check tests/unit/app/test_returns_series_endpoint_helpers.py`, `python -m pytest -q`. |
| LP-CR-034 | 2026-03-14 | Ambient-state dependence in core engine calculation pipeline | Hardened | The core `run_calculations(...)` engine path mutated the caller-owned DataFrame in place, adding derived columns and coercing input types directly on the original object. That is avoidable ambient-state coupling in a banking analytics engine and makes downstream behavior depend on call order or reuse of shared frames. | Changed `run_calculations(...)` to operate on a deep working copy while preserving the existing calculation pipeline and outputs. Added a focused unit test that proves caller input frames remain unchanged after calculation, and revalidated the compute and characterization engine packs. | Next: continue the engine review on any remaining stale fix-marker sections where comments may still be hiding brittle logic or unused parameters. The caller-mutation risk itself is now closed. | Files: `engine/compute.py`, `tests/unit/engine/test_compute.py`; validation: `python -m pytest tests/unit/engine/test_compute.py tests/unit/engine/test_engine_characterization.py -q`, `python -m ruff check engine/compute.py tests/unit/engine/test_compute.py`, `python -m mypy --config-file mypy.ini engine/compute.py`. |
| LP-CR-035 | 2026-03-14 | Engine ownership cleanup around cumulative-return orchestration and breakdown helpers | Hardened | The core engine still carried historical fix-marker blocks in `compute.py`, `ror.py`, `rules.py`, and breakdown tests, and the breakdown summary helper still accepted dead parameters that no longer affected behavior. That left the analytics core reading like accumulated patches instead of owned engine structure, which raises maintenance cost and makes future correctness changes riskier. | Refactored cumulative-return orchestration into explicit helper functions for component selection, temp/final cumulative return generation, reset zeroing, and NIP handling. Removed stale fix-marker comments in the reviewed engine path, and removed the dead `full_history_df` and `rounding_precision` parameters from `_calculate_period_summary_dict(...)` while updating its tests and call sites. Revalidated the engine-focused packs and then the full suite. | Next: continue the engine review on any remaining fix-marker zones only where they still hide real modularity or correctness debt. This slice closed the design/ownership gap in the current cumulative-return and breakdown path. | Files: `engine/breakdown.py`, `engine/compute.py`, `engine/ror.py`, `engine/rules.py`, `tests/unit/engine/test_breakdown.py`, `tests/unit/engine/test_rules.py`; validation: `python -m pytest tests/unit/engine/test_compute.py tests/unit/engine/test_breakdown.py tests/unit/engine/test_rules.py tests/unit/engine/test_ror.py tests/unit/engine/test_engine_characterization.py -q`, `python -m ruff check engine/compute.py engine/breakdown.py engine/ror.py engine/rules.py tests/unit/engine/test_compute.py tests/unit/engine/test_breakdown.py tests/unit/engine/test_rules.py`, `python -m mypy --config-file mypy.ini engine/compute.py engine/breakdown.py engine/ror.py engine/rules.py`, `python -m pytest -q`. |
| LP-CR-036 | 2026-03-14 | Redundant defensive copying in contribution and attribution engine callers | Hardened | After `run_calculations(...)` was made caller-safe, higher-level analytics paths in contribution and attribution still copied per-position/per-instrument DataFrames before calling the engine. That was now dead duplication and added avoidable memory churn on hot analytical paths without buying additional safety. | Removed the redundant `.copy()` calls from contribution and attribution engine callers, relying on the now-owned engine copy boundary instead. Also removed a stale fix-marker note from the RoR unit tests. Revalidated targeted engine/integration packs and then the full suite, which stayed green and showed slightly improved benchmark throughput. | Next: continue selective engine cleanup only where there is another real leverage point on ownership, query shape, or correctness. The redundant copy churn in the reviewed hot paths is now removed. | Files: `engine/contribution.py`, `engine/attribution.py`, `tests/unit/engine/test_ror.py`; validation: `python -m pytest tests/unit/engine/test_compute.py tests/unit/engine/test_contribution.py tests/unit/engine/test_attribution.py tests/integration/test_contribution_api.py tests/integration/test_attribution_api.py -q`, `python -m ruff check engine/contribution.py engine/attribution.py tests/unit/engine/test_ror.py`, `python -m mypy --config-file mypy.ini engine/contribution.py engine/attribution.py`, `python -m pytest -q`. |
| LP-CR-037 | 2026-03-14 | Shared TWR runtime helper for higher-level analytics engines | Hardened | Contribution and attribution still each hand-rolled the same glue for `valuation_points -> engine dataframe -> optional BASE_ONLY override -> run_calculations(...)`, with local config reconstruction that risked drifting from the canonical engine configuration shape. That duplicated ownership on two hot analytics paths and could silently drop config like precision or rounding in override branches. | Added a shared engine runtime helper that runs TWR over valuation points and a `base_only_engine_config(...)` helper built via dataclass replacement so non-currency config is preserved. Rewired contribution and attribution to use the shared helper, and added a lower-level test proving the BASE_ONLY override keeps precision, rounding, and reporting currency settings intact. Revalidated targeted engine/integration packs and then the full suite. | Next: continue looking for duplicated orchestration only where it still crosses an important boundary; this slice closed one meaningful cross-engine duplication seam. | Files: `engine/runtime.py`, `engine/contribution.py`, `engine/attribution.py`, `tests/unit/engine/test_contribution.py`; validation: `python -m pytest tests/unit/engine/test_contribution.py tests/unit/engine/test_attribution.py tests/integration/test_contribution_api.py tests/integration/test_attribution_api.py -q`, `python -m ruff check engine/runtime.py engine/contribution.py engine/attribution.py tests/unit/engine/test_contribution.py`, `python -m mypy --config-file mypy.ini engine/runtime.py engine/contribution.py engine/attribution.py`, `python -m pytest -q`. |
| LP-CR-038 | 2026-03-14 | Shared synchronous execution lifecycle orchestration | Hardened | TWR, MWR, contribution, and attribution each owned their own execution-failure and lineage-enqueue sequencing. That duplication created a real correctness risk during refactor: if lineage enqueue failed after `execution` had been completed, the lifecycle could be left inconsistent or tests could silently keep patching the wrong owner. | Added `execution_lifecycle_service` as the explicit owner for synchronous execution completion and failure recording, rewired TWR, MWR, contribution, and attribution to use it, and hardened the helper to fail closed when lineage enqueue raises after the `execution` stage is complete. Added lower-level tests proving both the success path and the enqueue-failure path, and updated compute-worker isolation tests to patch the new lifecycle owner instead of the old per-service lineage handle. | Next: keep this shared owner as the only place that encodes synchronous execution-to-lineage transition semantics; if returns-series sync lifecycle is later folded into the same abstraction, do it deliberately rather than reintroducing local sequencing. | Files: `app/services/execution_lifecycle_service.py`, `app/api/endpoints/performance.py`, `app/services/contribution_service.py`, `app/services/attribution_service.py`, `tests/unit/services/test_execution_lifecycle_service.py`, `tests/unit/services/test_compute_executor_worker.py`; validation: `python -m pytest tests/unit/services/test_execution_lifecycle_service.py tests/integration/test_performance_api.py tests/integration/test_execution_api.py tests/integration/test_contribution_api.py tests/integration/test_attribution_api.py -q`, `python -m pytest tests/unit/services/test_compute_executor_worker.py tests/unit/services/test_execution_lifecycle_service.py -q`, `python -m ruff check app/services/execution_lifecycle_service.py app/services/contribution_service.py app/services/attribution_service.py app/api/endpoints/performance.py tests/unit/services/test_execution_lifecycle_service.py tests/unit/services/test_compute_executor_worker.py`, `python -m mypy --config-file mypy.ini app/services/execution_lifecycle_service.py app/services/contribution_service.py app/services/attribution_service.py app/api/endpoints/performance.py`, `python -m pytest -q`. |
| LP-CR-039 | 2026-03-14 | Robustness-policy hot-path ownership and copy discipline | Hardened | `run_calculations(...)` already owned the caller-protection copy boundary, but `engine/policies.py` still re-parsed the same `DataPolicy` model multiple times and created extra whole-frame copies for overrides and ignore-days. That duplicated ownership on a hot analytical path and made the policy layer do more ambient churn than necessary. | Added a parsed `PolicyInputs` view so overrides and ignored dates are derived once, changed policy application to allocate at most one mutable working frame for all pre-calculation transforms, and threaded ignored-date extraction directly into outlier flagging instead of recalculating it there. Added lower-level tests proving combined override + ignore-day policy still leaves the caller frame untouched and that ignored-date extraction stays canonical, then revalidated the TWR integration pack and the full suite. | Next: if policy complexity grows further, consider moving policy diagnostics to typed structures rather than nested dictionaries. The hot-path copy/re-parse duplication is now closed without changing the policy contract. | Files: `engine/policies.py`, `engine/compute.py`, `tests/unit/engine/test_policies.py`; validation: `python -m pytest tests/unit/engine/test_policies.py tests/unit/engine/test_compute.py tests/integration/test_performance_api.py -q`, `python -m ruff check engine/policies.py engine/compute.py tests/unit/engine/test_policies.py`, `python -m mypy --config-file mypy.ini engine/policies.py engine/compute.py`, `python -m pytest -q`. |
| LP-CR-040 | 2026-03-14 | Async submission fencing runtime ownership | Hardened | `register_async_submission_or_raise(...)` was still calling durable-store `create_schema()` on every async request even though application startup already owns schema bootstrap. That put runtime initialization side effects on a hot request path and hid a startup/runtime ownership mistake behind per-request behavior. The full-suite pass also exposed an E2E test that was asserting global queue cardinality instead of the replay workflow outcome. | Removed schema bootstrap from the async submission request path so startup remains the single owner of durable-store initialization. Added a focused submission-fencing unit pack that proves request handling no longer bootstraps schema and still preserves replay/conflict behavior. Tightened the E2E replay test to assert workflow success rather than global queue count, leaving exact queue-cardinality guarantees to the lower-level submission tests. | Next: keep exact replay/cardinality assertions in unit/integration layers and avoid reintroducing runtime bootstrap side effects into request handlers. | Files: `app/services/submission_fencing_service.py`, `tests/unit/services/test_submission_fencing_service.py`, `tests/e2e/test_workflow_journeys.py`; validation: `python -m pytest tests/unit/services/test_submission_fencing_service.py tests/integration/test_contribution_api.py tests/integration/test_attribution_api.py tests/integration/test_returns_series_api.py -q`, `python -m pytest tests/unit/services/test_submission_fencing_service.py tests/e2e/test_workflow_journeys.py -q`, `python -m ruff check app/services/submission_fencing_service.py tests/unit/services/test_submission_fencing_service.py tests/e2e/test_workflow_journeys.py`, `python -m mypy --config-file mypy.ini app/services/submission_fencing_service.py`, `python -m pytest -q`. |
| LP-CR-041 | 2026-03-14 | Characterization-suite ownership and stale marker cleanup | Hardened | The engine characterization layer still carried stale `START FIX` / `END FIX` marker noise in canonical expected outputs, and scenario identity was split between the scenario-data module and a second hard-coded registry in the test module. That makes characterization drift easier and weakens the role of the data module as the single owned source of scenario truth. | Removed the stale marker noise from the long-flip scenario and added a single `CHARACTERIZATION_SCENARIOS` registry in the characterization-data module so scenario identity now lives with the scenario definitions. Simplified the characterization test to derive its parametrization directly from that registry, reducing duplication without changing characterization behavior. Revalidated the characterization pack and then the full suite. | Next: keep future characterization additions in the shared registry rather than reintroducing a second scenario map in the test module. | Files: `tests/unit/engine/characterization_data.py`, `tests/unit/engine/test_engine_characterization.py`; validation: `python -m pytest tests/unit/engine/test_engine_characterization.py tests/unit/engine/test_compute.py -q`, `python -m ruff check tests/unit/engine/characterization_data.py tests/unit/engine/test_engine_characterization.py`, `python -m pytest -q`. |
| LP-CR-042 | 2026-03-14 | Async submission partial-state reconciliation | Hardened | Async submission still registered execution and compute-job state in separate durable stores without reconciling partial-create cases. That left two correctness seams: an orphan compute-job conflict could leave a newly created execution record behind, and an orphan async execution replay could return `202` without recreating the missing compute job or normalizing the `submission` stage. | Added `delete_execution(...)` to the execution registry, hardened async submission fencing so new execution records are cleaned up on compute-job conflict or registration failure, and taught replay handling to self-heal missing compute jobs by reopening and completing the `submission` stage exactly once. Added focused unit proofs for conflict cleanup and replay self-heal, plus integration tests with real stores that exercise both orphan-state cases through the contribution API. Revalidated the targeted packs and then the full suite. | Next: keep async durable-state registration logic centralized in submission fencing; if more async analytics types are added, they should inherit this reconciliation behavior rather than reimplementing registration order locally. | Files: `app/services/execution_registry.py`, `app/services/submission_fencing_service.py`, `tests/unit/services/test_submission_fencing_service.py`, `tests/integration/test_contribution_api.py`; validation: `python -m pytest tests/unit/services/test_submission_fencing_service.py tests/integration/test_contribution_api.py -q`, `python -m ruff check app/services/execution_registry.py app/services/submission_fencing_service.py tests/unit/services/test_submission_fencing_service.py tests/integration/test_contribution_api.py`, `python -m mypy --config-file mypy.ini app/services/execution_registry.py app/services/submission_fencing_service.py`, `python -m pytest -q`. |
| LP-CR-043 | 2026-03-14 | Runtime-status control-plane contract ownership | Hardened | The runtime-status control plane already had meaningful typed snapshot state in the service layer, but the endpoint still duplicated the full snapshot-to-response translation inline, including queue stats casts, degradation-detail projection, and queue-policy mapping. That split operator-facing contract ownership between service and transport code, which is how support surfaces drift as fields evolve. | Moved the runtime-status response schema and translation logic into a dedicated `app.models.runtime_status` module with a single `build_runtime_status_response(...)` path. Slimmed the endpoint back to transport-only behavior that delegates to the shared builder, and added focused unit proof for both degraded and unavailable queue serialization while preserving the existing runtime-status service and integration API packs. | Next: keep future runtime-status field additions in the shared response builder/module rather than reintroducing per-endpoint manual mapping. | Files: `app/models/runtime_status.py`, `app/api/endpoints/runtime_status.py`, `tests/unit/models/test_runtime_status_models.py`; validation: `python -m pytest tests/unit/models/test_runtime_status_models.py tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py -q`, `python -m ruff check app/models/runtime_status.py app/api/endpoints/runtime_status.py tests/unit/models/test_runtime_status_models.py`, `python -m mypy --config-file mypy.ini app/models/runtime_status.py app/api/endpoints/runtime_status.py`, `python -m pytest -q`. |
| LP-CR-044 | 2026-03-14 | Execution-polling contract ownership | Hardened | `/performance/executions/{calculation_id}` is a core operator and debugging surface for async analytics, but its endpoint still assembled stage, upstream snapshot, compute-job, and async-result payloads inline using raw dictionaries. That left the polling contract without a single owned translation path and made it more likely to drift as durable execution metadata evolves. | Added a dedicated `app.models.execution_polling` module with typed nested response models and a single `build_execution_response(...)` path. Slimmed the executions endpoint back to transport-only behavior that delegates to the shared builder, and added focused unit proof for both complete async metadata and the no-job/no-result case while preserving the existing execution integration pack. | Next: keep future execution-polling metadata additions in the shared builder/module rather than reintroducing raw nested dict assembly in the endpoint. | Files: `app/models/execution_polling.py`, `app/api/endpoints/executions.py`, `tests/unit/models/test_execution_polling_models.py`; validation: `python -m pytest tests/unit/models/test_execution_polling_models.py tests/integration/test_execution_api.py -q`, `python -m ruff check app/models/execution_polling.py app/api/endpoints/executions.py tests/unit/models/test_execution_polling_models.py`, `python -m mypy --config-file mypy.ini app/models/execution_polling.py app/api/endpoints/executions.py`, `python -m pytest -q`. |
| LP-CR-045 | 2026-03-14 | Engine diagnostics contract ownership between compute core and TWR transport | Hardened | The compute engine already produced structured diagnostics semantically, but it still surfaced them as loose nested dictionaries. The TWR endpoint then rebuilt the public `Diagnostics` payload with `.get(...)` lookups and locally expanded reset events from raw dictionaries. That left an important engine-to-API seam weakly typed, made defaults implicit, and duplicated contract knowledge across the engine, endpoint, and tests. | Added engine-local typed diagnostics models for policy overrides, outlier samples, reset events, and aggregate engine diagnostics. Rewired robustness policies and `run_calculations(...)` to populate those typed structures directly, and introduced a dedicated `app.models.performance_diagnostics` builder that maps the engine diagnostics into the public envelope and reset-event response models exactly once. Updated lower-level engine/policy tests to assert on typed diagnostics, added focused builder tests, reran the performance integration pack, and then reran the full suite plus the OpenAPI quality gate. | Next: if more analytics endpoints start consuming engine diagnostics directly, keep them on the shared builder path rather than reintroducing raw dict parsing or endpoint-local defaults. | Files: `engine/diagnostics.py`, `engine/policies.py`, `engine/compute.py`, `app/models/performance_diagnostics.py`, `app/api/endpoints/performance.py`, `tests/unit/engine/test_compute.py`, `tests/unit/engine/test_policies.py`, `tests/unit/models/test_performance_diagnostics_models.py`; validation: `python -m pytest tests/unit/engine/test_compute.py tests/unit/engine/test_policies.py tests/unit/models/test_performance_diagnostics_models.py tests/integration/test_performance_api.py -q`, `python -m ruff check app/api/endpoints/performance.py app/models/performance_diagnostics.py engine/compute.py engine/diagnostics.py engine/policies.py tests/unit/engine/test_compute.py tests/unit/engine/test_policies.py tests/unit/models/test_performance_diagnostics_models.py`, `python -m mypy --config-file mypy.ini app/api/endpoints/performance.py app/models/performance_diagnostics.py engine/compute.py engine/diagnostics.py engine/policies.py`, `python scripts/openapi_quality_gate.py`, `python -m pytest -q`. |
| LP-CR-046 | 2026-03-14 | Lineage worker lease fencing and additive schema upgrade safety | Hardened | The compute executor already used durable claim semantics, but the lineage worker still read naked pending payloads and incremented attempt count separately. Under multiple workers or crash/restart overlap, that allowed duplicate lineage materialization of the same calculation. The first broad validation pass also exposed a second production-grade gap: older SQLite lineage metadata stores were not upgraded by `create_all()`, so the new lease fields would fail against existing durable metadata tables. | Added durable lease metadata to lineage payloads and rewired the lineage worker to claim pending payloads with worker identity and lease expiry before materialization. Retry and terminal-failure paths now release lease state cleanly so work can be reclaimed safely after failure. Extended lineage schema bootstrap with an additive upgrade path that backfills the new lease columns and index into existing SQLite payload tables, preventing runtime breakage on older metadata stores. Added lower-level proofs for lease exclusivity, expired-lease reclaim, lease release on retry, and additive schema migration, then reran the full suite and OpenAPI gate. | Next: if lineage runtime visibility needs to go deeper later, the next logical step is exposing leased-lineage counts/age directly in runtime-status rather than inferring all backlog pressure from pending-only counts. The duplicate-processing and upgrade-path risks themselves are now closed. | Files: `app/core/config.py`, `app/services/lineage_metadata_store.py`, `app/workers/lineage_worker.py`, `tests/unit/services/test_lineage_metadata_store.py`, `tests/unit/services/test_lineage_worker.py`; validation: `python -m pytest tests/unit/services/test_lineage_metadata_store.py tests/unit/services/test_lineage_worker.py -q`, `python -m ruff check app/core/config.py app/services/lineage_metadata_store.py app/workers/lineage_worker.py tests/unit/services/test_lineage_metadata_store.py tests/unit/services/test_lineage_worker.py`, `python -m mypy --config-file mypy.ini app/core/config.py app/services/lineage_metadata_store.py app/workers/lineage_worker.py`, `python scripts/openapi_quality_gate.py`, `python -m pytest -q`. |
| LP-CR-047 | 2026-03-14 | Runtime-status visibility for leased lineage pressure | Hardened | After lineage moved to durable lease/claim semantics, runtime-status still treated lineage strictly as pending-only backlog. That created an operator blind spot: claimed lineage payloads and their in-flight age were invisible, so a stuck or overloaded lineage worker could look healthy as long as payloads had already been claimed. | Extended lineage queue stats with active leased-payload count and oldest leased age, exposed those fields through the runtime-status response contract, and added a lineage leased-age degradation policy/trigger so runtime can degrade when claimed lineage work stays in flight too long. Added lower-level queue-stats proof for active leased lineage payloads, unit proof for the new leased-age degradation path, updated runtime-status response model tests, and reran the targeted packs plus the full suite and OpenAPI gate. | Next: if we want to keep pushing on this lane, the next meaningful step is surfacing lineage lease expiry/recovery counts explicitly rather than only leased age and retry/terminal-failure pressure. The main leased-lineage visibility gap itself is now closed. | Files: `app/core/config.py`, `app/services/lineage_metadata_store.py`, `app/services/runtime_status_service.py`, `app/models/runtime_status.py`, `tests/unit/services/test_lineage_metadata_store.py`, `tests/unit/services/test_runtime_status_service.py`, `tests/unit/models/test_runtime_status_models.py`, `tests/integration/test_runtime_status_api.py`; validation: `python -m pytest tests/unit/models/test_runtime_status_models.py tests/unit/services/test_lineage_metadata_store.py tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py -q`, `python -m ruff check app/core/config.py app/services/lineage_metadata_store.py app/services/runtime_status_service.py app/models/runtime_status.py tests/unit/services/test_lineage_metadata_store.py tests/unit/services/test_runtime_status_service.py tests/unit/models/test_runtime_status_models.py tests/integration/test_runtime_status_api.py`, `python -m mypy --config-file mypy.ini app/core/config.py app/services/lineage_metadata_store.py app/services/runtime_status_service.py app/models/runtime_status.py`, `python scripts/openapi_quality_gate.py`, `python -m pytest -q`. |
| LP-CR-048 | 2026-03-14 | Durable schema migration governance aligned to shipped architecture | Hardened | The migration governance standard and smoke check still enforced a pre-RFC-041 assumption that lotus-performance had "no persistent schema". That was now false: the service owns durable execution, compute-job, async-result, and lineage metadata tables plus additive runtime schema upgrades. Leaving the old governance text in place would make CI certify the wrong architecture and weaken change control around future durable-store evolution. | Updated the migration contract standard to the real durable-schema model, codifying forward-only additive upgrades, rollback-runbook requirements for incompatible changes, and deterministic schema-upgrade proof expectations. Reworked the migration smoke checker and `Makefile` targets to validate the durable-schema contract instead of no-schema language, and added a focused unit test pack for the checker itself so this governance rule is test-backed. Reran migration-smoke, the full test suite, and the OpenAPI quality gate. | Next: if we continue the commercial-readiness lane, the next strong step is turning this governance into store-level migration inventory/runbook material instead of a single standards page plus smoke script. The repo-level governance contradiction itself is now closed. | Files: `docs/standards/migration-contract.md`, `scripts/migration_contract_check.py`, `Makefile`, `tests/unit/test_migration_contract_check.py`; validation: `python -m pytest tests/unit/test_migration_contract_check.py -q`, `make migration-smoke`, `python -m ruff check scripts/migration_contract_check.py tests/unit/test_migration_contract_check.py`, `python scripts/openapi_quality_gate.py`, `python -m pytest -q`. |
| LP-CR-049 | 2026-03-14 | Durable schema inventory and recovery-governance visibility | Hardened | After aligning migration governance to the real durable-schema architecture, the repo still lacked a single governed inventory of the tables it actually owns. Reviewers and operators could infer the durable schema from service modules, but there was no deterministic artifact listing owned tables, their runtime owner, their recovery role, and the expectation that backup/restore and restart runbooks cover them. That is a commercial-readiness governance gap because change control and operations should not depend on reading source files. | Added `docs/standards/durable-schema-inventory.md` as the authoritative inventory of owned durable tables, owners, purposes, upgrade rules, and recovery expectations. Added `scripts/durable_schema_inventory_check.py`, wired it into `make migration-smoke`, and added unit coverage for the checker so the inventory is enforced in CI rather than left as advisory text. Reran migration-smoke, the full test suite, and the OpenAPI quality gate. | Next: the natural follow-on is a concrete restore/restart runbook for the durable metadata stores and workers, since the inventory now states that requirement explicitly. The inventory/governance visibility gap itself is now closed. | Files: `docs/standards/durable-schema-inventory.md`, `scripts/durable_schema_inventory_check.py`, `Makefile`, `tests/unit/test_durable_schema_inventory_check.py`; validation: `python -m pytest tests/unit/test_durable_schema_inventory_check.py tests/unit/test_migration_contract_check.py -q`, `make migration-smoke`, `python -m ruff check scripts/durable_schema_inventory_check.py tests/unit/test_durable_schema_inventory_check.py scripts/migration_contract_check.py tests/unit/test_migration_contract_check.py`, `python scripts/openapi_quality_gate.py`, `python -m pytest -q`. |
| LP-CR-050 | 2026-03-14 | Durable metadata recovery runbook and operational restart order | Hardened | The durable schema inventory now explicitly required backup/restore and worker restart documentation, but the repo still had no actual runbook. That left a real commercial-readiness gap: recovery order for `performance-analytics`, `performance-compute-executor`, and `performance-lineage-worker` still depended on tribal knowledge rather than a governed operational artifact. | Added `docs/runbooks/durable-metadata-recovery.md` covering backup/restore order, worker restart order, post-restore validation, forward-only recovery rules, and evidence capture. Added `scripts/durable_recovery_runbook_check.py`, wired it into `make migration-smoke`, and added unit coverage so the runbook is enforced alongside the migration contract and durable schema inventory. Reran migration-smoke, the full suite, and the OpenAPI quality gate. | Next: the next commercial/banking-grade step is moving from runbook presence to runbook-backed validation evidence for restore drills, since the repo now defines the operational procedure but does not yet store executed drill proof. | Files: `docs/runbooks/durable-metadata-recovery.md`, `scripts/durable_recovery_runbook_check.py`, `Makefile`, `tests/unit/test_durable_recovery_runbook_check.py`; validation: `python -m pytest tests/unit/test_durable_recovery_runbook_check.py tests/unit/test_durable_schema_inventory_check.py tests/unit/test_migration_contract_check.py -q`, `make migration-smoke`, `python -m ruff check scripts/durable_recovery_runbook_check.py tests/unit/test_durable_recovery_runbook_check.py scripts/durable_schema_inventory_check.py tests/unit/test_durable_schema_inventory_check.py scripts/migration_contract_check.py tests/unit/test_migration_contract_check.py`, `python scripts/openapi_quality_gate.py`, `python -m pytest -q`. |
| LP-CR-051 | 2026-03-14 | Durable metadata bootstrap ownership and executable recovery-drill proof | Hardened | Durable metadata bootstrap was still duplicated across API startup, worker runtimes, and test/recovery helpers. That split ownership of the durable-store create/upgrade sequence and made restored-schema recovery depend on each caller remembering the same bootstrap order. The recovery runbook was now governed, but there was still no executable proof that bootstrapping a restored legacy lineage metadata store actually returned the lineage worker to a processable state. | Added a shared `bootstrap_durable_metadata_stores(...)` service covering execution, compute-job, async-result, and lineage metadata stores, then rewired app startup, compute worker startup, lineage worker startup, and test drain helpers to use that single bootstrap path. Added a recovery-drill smoke test that creates a legacy SQLite lineage schema without the newer lease columns, runs the shared bootstrap, enqueues lineage work, and proves the current lineage worker can resume processing successfully after the additive schema upgrade. Reran targeted worker/bootstrap tests, migration-smoke, the full suite, and the OpenAPI quality gate. | Next: the next commercial/banking-grade step is storing durable restore-drill evidence as a governed artifact, now that the recovery path is both documented and executable. Keep future durable-store additions on the shared bootstrap path rather than reintroducing caller-local `create_schema()` sequences. | Files: `app/services/durable_metadata_bootstrap.py`, `main.py`, `app/workers/compute_executor_worker.py`, `app/workers/lineage_worker.py`, `tests/conftest.py`, `tests/unit/services/test_durable_metadata_bootstrap.py`; validation: `python -m pytest tests/unit/services/test_durable_metadata_bootstrap.py tests/unit/services/test_lineage_worker.py tests/unit/services/test_compute_executor_worker.py -q`, `python -m ruff check app/services/durable_metadata_bootstrap.py main.py app/workers/compute_executor_worker.py app/workers/lineage_worker.py tests/conftest.py tests/unit/services/test_durable_metadata_bootstrap.py`, `python -m mypy --config-file mypy.ini app/services/durable_metadata_bootstrap.py main.py app/workers/compute_executor_worker.py app/workers/lineage_worker.py`, `make migration-smoke`, `python scripts/openapi_quality_gate.py`, `python -m pytest -q`. |
| LP-CR-052 | 2026-03-14 | Durable recovery drill evidence and lineage execution-store ownership | Hardened | Recovery governance had reached runbook presence plus a unit smoke, but it still lacked a repo-owned drill command that operators and CI could execute to emit structured restore evidence. The first implementation pass also exposed a second ownership seam: `LineageService` closed execution stages through an ambient global registry, which meant a local recovery drill could succeed while still logging execution-stage warnings against the wrong durable store. | Added `scripts/durable_recovery_drill.py` as an executable restore drill that bootstraps a legacy lineage schema, upgrades it in place, processes lineage materialization through injected worker dependencies, and emits structured JSON evidence. Wired `make migration-smoke` through a dedicated `recovery-drill-smoke` target and updated the recovery runbook/checker so the drill command and evidence artifact are part of the governed procedure. Refactored `LineageService` to accept an injected execution store, and refactored `lineage_worker.process_pending_jobs(...)` to accept explicit dependency injection for lineage store, lineage service, and execution store so recovery and worker flows no longer rely on hidden ambient state. Added unit proofs for the drill artifact, injected execution-stage completion, and updated runbook validation, then reran migration-smoke, the full suite, and the OpenAPI quality gate. | Next: the strongest remaining commercial/banking-grade gap in this lane is preserving executed drill history with operator metadata and retention policy, rather than only producing the latest structured evidence artifact. Keep future recovery controls on explicit dependency injection and governed drill outputs rather than module-global wiring. | Files: `.gitignore`, `Makefile`, `app/services/lineage_service.py`, `app/workers/lineage_worker.py`, `docs/runbooks/durable-metadata-recovery.md`, `scripts/durable_recovery_drill.py`, `scripts/durable_recovery_runbook_check.py`, `tests/unit/services/test_lineage_service.py`, `tests/unit/test_durable_recovery_drill.py`, `tests/unit/test_durable_recovery_runbook_check.py`; validation: `python -m pytest tests/unit/test_durable_recovery_drill.py tests/unit/test_durable_recovery_runbook_check.py tests/unit/services/test_lineage_service.py tests/unit/services/test_lineage_worker.py tests/unit/services/test_durable_metadata_bootstrap.py -q`, `python -m ruff check app/services/lineage_service.py app/workers/lineage_worker.py scripts/durable_recovery_drill.py scripts/durable_recovery_runbook_check.py tests/unit/test_durable_recovery_drill.py tests/unit/test_durable_recovery_runbook_check.py tests/unit/services/test_lineage_service.py tests/unit/services/test_durable_metadata_bootstrap.py`, `python -m mypy --config-file mypy.ini app/services/lineage_service.py app/workers/lineage_worker.py scripts/durable_recovery_drill.py`, `make migration-smoke`, `python scripts/openapi_quality_gate.py`, `python -m pytest -q`. |
| LP-CR-053 | 2026-03-14 | Compute worker dependency ownership and full recovery-drill queue coverage | Hardened | After LP-CR-052, recovery proof still favored lineage over compute. The compute executor continued to own its durable stores through ambient module globals, and the recovery drill only proved lineage artifact recovery, not compute-job completion and async-result durability. That asymmetry left the commercial-readiness recovery story incomplete across the two owned background queues. | Refactored the compute executor’s internal processing path to accept explicit job-store, execution-store, result-store, worker, lease, and calculator dependencies while preserving the existing public worker entrypoint. Extended the durable recovery drill to enqueue a local compute job, process it through the injected compute worker path, close a local execution stage, and emit explicit `compute_job_processed_count`, `compute_async_result_status`, and `compute_execution_status` evidence alongside the lineage proof. Updated the runbook guidance to tell operators what successful compute evidence looks like, then reran targeted compute/drill packs, migration-smoke, the OpenAPI quality gate, and the full suite. | Next: the strongest remaining recovery-governance gap is retaining timestamped drill history with operator/backup metadata rather than overwriting a single latest artifact. Keep compute and lineage recovery proof symmetrical and explicitly injected rather than sliding back to module-global worker orchestration in tests or scripts. | Files: `app/workers/compute_executor_worker.py`, `docs/runbooks/durable-metadata-recovery.md`, `scripts/durable_recovery_drill.py`, `tests/unit/test_durable_recovery_drill.py`, `tests/unit/services/test_compute_executor_worker.py`; validation: `python -m pytest tests/unit/test_durable_recovery_drill.py tests/unit/services/test_compute_executor_worker.py -q`, `python -m ruff check app/workers/compute_executor_worker.py scripts/durable_recovery_drill.py tests/unit/test_durable_recovery_drill.py tests/unit/services/test_compute_executor_worker.py`, `python -m mypy --config-file mypy.ini app/workers/compute_executor_worker.py scripts/durable_recovery_drill.py`, `make migration-smoke`, `python scripts/openapi_quality_gate.py`, `python -m pytest -q`. |
| LP-CR-054 | 2026-03-14 | Recovery drill evidence retention and audit-context capture | Hardened | After LP-CR-053, the drill proved both background queues but still wrote effectively mutable evidence. A single `latest.json` is not sufficient for auditability because it loses historical drill context, and the drill had no first-class fields for operator identity or the backup set being validated. That left the recovery story still short of banking-grade evidence retention. | Extended `scripts/durable_recovery_drill.py` to capture `operator_id`, `backup_identifier`, and a deterministic timestamped evidence file name in the evidence payload itself. The drill now persists both a timestamped historical artifact and refreshed `latest.json` under `artifacts/durable-recovery-drill/`, while `make recovery-drill-smoke` supplies explicit automation metadata. Updated the recovery runbook and runbook checker so operator/backup arguments and timestamped artifact retention are governed, and added unit coverage proving the metadata and historical artifact set are emitted correctly. Then reran migration-smoke, the OpenAPI quality gate, and the full suite. | Next: the strongest remaining gap in this recovery lane is adding a governed retention policy or index/manifest over historical drill artifacts rather than accumulating timestamped files without lifecycle rules. Keep drill evidence immutable and metadata-rich; do not regress back to overwrite-only recovery proof. | Files: `Makefile`, `docs/runbooks/durable-metadata-recovery.md`, `scripts/durable_recovery_drill.py`, `scripts/durable_recovery_runbook_check.py`, `tests/unit/test_durable_recovery_drill.py`, `tests/unit/test_durable_recovery_runbook_check.py`; validation: `python -m pytest tests/unit/test_durable_recovery_drill.py tests/unit/test_durable_recovery_runbook_check.py -q`, `python -m ruff check scripts/durable_recovery_drill.py scripts/durable_recovery_runbook_check.py tests/unit/test_durable_recovery_drill.py tests/unit/test_durable_recovery_runbook_check.py`, `python -m mypy --config-file mypy.ini scripts/durable_recovery_drill.py`, `make migration-smoke`, `python scripts/openapi_quality_gate.py`, `python -m pytest -q`. |
| LP-CR-055 | 2026-03-14 | Recovery drill manifesting and bounded history retention | Hardened | After LP-CR-054, recovery evidence became timestamped and metadata-rich, but retained history still had no machine-readable index and no bound. That leaves two operational risks: support teams cannot quickly inspect retained drill history without scanning files, and evidence accumulation can grow without lifecycle control. | Extended `scripts/durable_recovery_drill.py` to write `manifest.json` alongside retained history and `latest.json`, capturing the current latest file, retained file names, retention limit, and summary metadata for each retained drill. Added bounded retention pruning so only the configured number of timestamped drill artifacts are kept. Updated the runbook and runbook checker so manifest presence and retention-limit verification are governed, and added unit tests that prove manifest creation and historical-pruning behavior across multiple drill runs. Then reran migration-smoke, the OpenAPI quality gate, and the full suite. | Next: the remaining gap in this recovery lane is policy depth rather than mechanism: age-based expiry, environment-specific retention defaults, or an operator-facing query surface over retained drill history. Keep the manifest/index as the owned inspection surface rather than making operators infer history from the filesystem. | Files: `docs/runbooks/durable-metadata-recovery.md`, `scripts/durable_recovery_drill.py`, `scripts/durable_recovery_runbook_check.py`, `tests/unit/test_durable_recovery_drill.py`, `tests/unit/test_durable_recovery_runbook_check.py`; validation: `python -m pytest tests/unit/test_durable_recovery_drill.py tests/unit/test_durable_recovery_runbook_check.py -q`, `python -m ruff check scripts/durable_recovery_drill.py scripts/durable_recovery_runbook_check.py tests/unit/test_durable_recovery_drill.py tests/unit/test_durable_recovery_runbook_check.py`, `python -m mypy --config-file mypy.ini scripts/durable_recovery_drill.py`, `make migration-smoke`, `python scripts/openapi_quality_gate.py`, `python -m pytest -q`. |
| LP-CR-056 | 2026-03-14 | Operator-facing recovery drill history control plane | Hardened | After LP-CR-055, retained recovery evidence was bounded and indexed on disk, but it still required filesystem access to inspect. That is an operator-support gap: runtime support surfaces should expose retained recovery history through the service’s owned control plane rather than making responders shell into the host or infer state from files. | Added configurable recovery-drill artifact path and retention defaults in runtime settings, introduced a dedicated recovery-drill history service and response model over the retained manifest, and exposed `GET /integration/recovery-drills` as a read-only control-plane endpoint for retained recovery evidence. Added unit coverage for missing-manifest and manifest-backed history snapshots, added response-model proof, and added integration tests covering both unavailable and retained-history API responses. Reran migration-smoke, the OpenAPI quality gate, and the full suite. | Next: the likely next step in this lane is richer operator ergonomics such as paging/filtering or age-based retention policy, not basic accessibility. Keep retained recovery history on the service-owned control plane rather than requiring filesystem access for support workflows. | Files: `Makefile`, `app/core/config.py`, `app/services/recovery_drill_history_service.py`, `app/models/recovery_drill_history.py`, `app/api/endpoints/recovery_drill_history.py`, `main.py`, `scripts/durable_recovery_drill.py`, `tests/unit/services/test_recovery_drill_history_service.py`, `tests/unit/models/test_recovery_drill_history_models.py`, `tests/integration/test_recovery_drill_history_api.py`; validation: `python -m pytest tests/unit/services/test_recovery_drill_history_service.py tests/unit/models/test_recovery_drill_history_models.py tests/integration/test_recovery_drill_history_api.py -q`, `python -m ruff check app/core/config.py app/services/recovery_drill_history_service.py app/models/recovery_drill_history.py app/api/endpoints/recovery_drill_history.py main.py scripts/durable_recovery_drill.py tests/unit/services/test_recovery_drill_history_service.py tests/unit/models/test_recovery_drill_history_models.py tests/integration/test_recovery_drill_history_api.py`, `python -m mypy --config-file mypy.ini app/core/config.py app/services/recovery_drill_history_service.py app/models/recovery_drill_history.py app/api/endpoints/recovery_drill_history.py scripts/durable_recovery_drill.py`, `make migration-smoke`, `python scripts/openapi_quality_gate.py`, `python -m pytest -q`. |
| LP-CR-057 | 2026-03-14 | Age-based recovery-history expiry policy | Hardened | After LP-CR-056, retained recovery history was accessible and bounded by count, but not by age. Count-only retention is insufficient for banking-grade operational evidence because stale artifacts can outlive policy expectations even when the file count stays small, and the retained-history API could not tell operators what age policy was in effect. | Added `RECOVERY_DRILL_RETENTION_MAX_AGE_DAYS` to runtime settings, extended the drill script with age-based pruning before count-based retention, and included `retention_max_age_days` in the retained-history manifest and control-plane response. Updated the recovery runbook and runbook checker so operators must verify maximum-age policy alongside retention-count policy. Added unit coverage proving stale historical evidence is pruned by age and extended the history service/model/API tests to assert the surfaced age policy. Reran migration-smoke, the OpenAPI quality gate, and the full suite. | Next: the likely next step in this lane is operator query ergonomics or environment-specific policy defaults rather than more retention mechanics. Keep retention policy explicit and surfaced through the control plane rather than hidden in scripts or local conventions. | Files: `Makefile`, `app/core/config.py`, `app/models/recovery_drill_history.py`, `app/services/recovery_drill_history_service.py`, `docs/runbooks/durable-metadata-recovery.md`, `scripts/durable_recovery_drill.py`, `scripts/durable_recovery_runbook_check.py`, `tests/integration/test_recovery_drill_history_api.py`, `tests/unit/models/test_recovery_drill_history_models.py`, `tests/unit/services/test_recovery_drill_history_service.py`, `tests/unit/test_durable_recovery_drill.py`, `tests/unit/test_durable_recovery_runbook_check.py`; validation: `python -m pytest tests/unit/test_durable_recovery_drill.py tests/unit/test_durable_recovery_runbook_check.py tests/unit/services/test_recovery_drill_history_service.py tests/unit/models/test_recovery_drill_history_models.py tests/integration/test_recovery_drill_history_api.py -q`, `python -m ruff check app/core/config.py scripts/durable_recovery_drill.py scripts/durable_recovery_runbook_check.py app/services/recovery_drill_history_service.py app/models/recovery_drill_history.py tests/unit/test_durable_recovery_drill.py tests/unit/test_durable_recovery_runbook_check.py tests/unit/services/test_recovery_drill_history_service.py tests/unit/models/test_recovery_drill_history_models.py tests/integration/test_recovery_drill_history_api.py`, `python -m mypy --config-file mypy.ini app/core/config.py scripts/durable_recovery_drill.py app/services/recovery_drill_history_service.py app/models/recovery_drill_history.py`, `make migration-smoke`, `python scripts/openapi_quality_gate.py`, `python -m pytest -q`. |
| LP-CR-058 | 2026-03-14 | Recovery-history filtering and bounded query ergonomics | Hardened | After LP-CR-057, retained recovery history had a control-plane endpoint, but operators still had to consume the full retained set every time. That is an incident-ergonomics gap: responders need to bound responses and narrow the retained history by operator, backup identifier, or outcome status without reconstructing filters client-side. | Extended `GET /integration/recovery-drills` with bounded `limit` plus filters on `operator_id`, `backup_identifier`, and `status`. Updated the recovery-history service to apply those filters while still reporting `total_entries`, `returned_entries`, and a stable `applied_filters` object so callers can distinguish the retained set from the returned subset. Added unit coverage for service-side filtering and bounded return, updated response-model proof, and added integration coverage for filtered history queries. Reran migration-smoke, the OpenAPI quality gate, and the full suite. | Next: the likely next step is full pagination or richer search semantics rather than basic filter support. Keep filter application and query metadata owned by the recovery-history service instead of pushing that logic into clients. | Files: `app/api/endpoints/recovery_drill_history.py`, `app/models/recovery_drill_history.py`, `app/services/recovery_drill_history_service.py`, `tests/integration/test_recovery_drill_history_api.py`, `tests/unit/models/test_recovery_drill_history_models.py`, `tests/unit/services/test_recovery_drill_history_service.py`; validation: `python -m pytest tests/unit/services/test_recovery_drill_history_service.py tests/unit/models/test_recovery_drill_history_models.py tests/integration/test_recovery_drill_history_api.py -q`, `python -m ruff check app/services/recovery_drill_history_service.py app/models/recovery_drill_history.py app/api/endpoints/recovery_drill_history.py tests/unit/services/test_recovery_drill_history_service.py tests/unit/models/test_recovery_drill_history_models.py tests/integration/test_recovery_drill_history_api.py`, `python -m mypy --config-file mypy.ini app/services/recovery_drill_history_service.py app/models/recovery_drill_history.py app/api/endpoints/recovery_drill_history.py`, `make migration-smoke`, `python scripts/openapi_quality_gate.py`, `python -m pytest -q`. |
| LP-CR-059 | 2026-03-14 | Recovery-history paging and time-window filtering | Hardened | After LP-CR-058, retained recovery history could be filtered and bounded, but responders still lacked deterministic page traversal and time-window narrowing. That is still an incident-ergonomics gap for larger retained histories: operators need to walk retained history incrementally and constrain it by drill timestamp without reconstructing that behavior client-side. | Extended `GET /integration/recovery-drills` with `offset`, `generated_after`, and `generated_before`, and updated the recovery-history service to apply time-window filtering before paging. Refined the response contract to distinguish `total_entries` in the retained set from `matched_entries` after filtering, plus `returned_entries` and `next_offset` for deterministic page traversal. Added service-level proofs for offset and time-window filtering, updated response-model proof, and added integration coverage for offset-plus-time-window queries. Reran migration-smoke, the OpenAPI quality gate, and the full suite. | Next: the likely next step is richer search semantics or a broader review lane outside recovery history, not basic page traversal. Keep paging and time-window semantics owned by the recovery-history service rather than encoded ad hoc in clients. | Files: `app/api/endpoints/recovery_drill_history.py`, `app/models/recovery_drill_history.py`, `app/services/recovery_drill_history_service.py`, `tests/integration/test_recovery_drill_history_api.py`, `tests/unit/models/test_recovery_drill_history_models.py`, `tests/unit/services/test_recovery_drill_history_service.py`; validation: `python -m pytest tests/unit/services/test_recovery_drill_history_service.py tests/unit/models/test_recovery_drill_history_models.py tests/integration/test_recovery_drill_history_api.py -q`, `python -m ruff check app/services/recovery_drill_history_service.py app/models/recovery_drill_history.py app/api/endpoints/recovery_drill_history.py tests/unit/services/test_recovery_drill_history_service.py tests/unit/models/test_recovery_drill_history_models.py tests/integration/test_recovery_drill_history_api.py`, `python -m mypy --config-file mypy.ini app/services/recovery_drill_history_service.py app/models/recovery_drill_history.py app/api/endpoints/recovery_drill_history.py`, `make migration-smoke`, `python scripts/openapi_quality_gate.py`, `python -m pytest -q`. |
| LP-CR-060 | 2026-03-14 | Runtime-status recovery-drill freshness assurance | Hardened | Runtime-status exposed queue pressure and durable-store readiness, but it did not tell operators whether the latest retained recovery drill existed, whether it passed, or whether recovery proof had gone stale. That left a blind spot in the control plane: the service could look operationally healthy while recovery assurance was absent or outdated. | Extended the runtime-status service and response contract with a `recovery_drill` section and `recovery_drill_policy`, sourcing the latest retained recovery drill from the governed recovery-history service. Added degradation semantics for stale proof, failed latest drill, and missing history when a freshness threshold is configured, while keeping the default behavior non-degrading when no freshness policy is set. Propagated the new contract through response models and added unit, model, and integration coverage for no-history, stale-history, failed-history, and serialized API behavior. | Next: the likely next step is promoting recovery-drill freshness from a repo-level threshold to environment-specific operational policy defaults and alert-routing guidance rather than leaving it purely as a raw config surface. Keep recovery assurance visible in the main runtime control plane rather than in a separate operator-only workflow. | Files: `app/core/config.py`, `app/services/runtime_status_service.py`, `app/models/runtime_status.py`, `tests/unit/services/test_runtime_status_service.py`, `tests/unit/models/test_runtime_status_models.py`, `tests/integration/test_runtime_status_api.py`; validation: `python -m pytest tests/unit/services/test_runtime_status_service.py tests/unit/models/test_runtime_status_models.py tests/integration/test_runtime_status_api.py -q`, `python -m ruff check app/core/config.py app/services/runtime_status_service.py app/models/runtime_status.py tests/unit/services/test_runtime_status_service.py tests/unit/models/test_runtime_status_models.py tests/integration/test_runtime_status_api.py`, `python -m mypy --config-file mypy.ini app/core/config.py app/services/runtime_status_service.py app/models/runtime_status.py`, `make migration-smoke`, `python scripts/openapi_quality_gate.py`, `python -m pytest -q`. |
| LP-CR-061 | 2026-03-14 | Ambient recovery-history config capture | Hardened | The recovery-history service captured `RECOVERY_DRILL_ARTIFACT_PATH` at module import time. That creates hidden ambient state: operator or test-time config changes require module reloads to take effect, and the recovery control plane can silently read the wrong artifact directory after runtime configuration changes. | Removed the module-global settings capture and resolved the default artifact directory through `get_settings()` on each snapshot build. Added a direct unit proof that the recovery-history service honors a patched runtime artifact path without reload gymnastics. | Next: continue eliminating module-global configuration capture in other live control-plane and worker paths where runtime-owned behavior should not be frozen at import time. Keep configuration reads explicit at the ownership boundary rather than hidden in module globals. | Files: `app/services/recovery_drill_history_service.py`, `tests/unit/services/test_recovery_drill_history_service.py`; validation: `python -m pytest tests/unit/services/test_recovery_drill_history_service.py -q`, `python -m ruff check app/services/recovery_drill_history_service.py tests/unit/services/test_recovery_drill_history_service.py`, `python -m mypy --config-file mypy.ini app/services/recovery_drill_history_service.py`. |
| LP-CR-062 | 2026-03-14 | Ambient worker configuration capture | Hardened | Both long-running workers froze runtime settings at module import time. That is the wrong ownership boundary for live worker behavior: batch size, poll cadence, lease settings, retry budget, and version-dependent hashing could become implicitly tied to import order, while lower-level tests had to mutate module globals instead of injecting explicit worker settings. | Refactored the compute executor and lineage worker to resolve settings per call, with explicit `settings` injection on `process_pending_jobs(...)` and `run_forever(...)`. Updated the quiescence and retry-budget tests to pass deterministic worker settings directly instead of mutating module-level config state. This keeps worker behavior explicit, testable, and free of hidden ambient config capture. | Next: continue the same ambient-state cleanup in other live runtime paths where module-global `get_settings()` capture still hides ownership boundaries. Favor explicit settings resolution or injected settings objects over import-time config freezing. | Files: `app/workers/compute_executor_worker.py`, `app/workers/lineage_worker.py`, `tests/unit/services/test_compute_executor_worker.py`, `tests/unit/services/test_lineage_worker.py`; validation: `python -m pytest tests/unit/services/test_compute_executor_worker.py tests/unit/services/test_lineage_worker.py -q`, `python -m ruff check app/workers/compute_executor_worker.py app/workers/lineage_worker.py tests/unit/services/test_compute_executor_worker.py tests/unit/services/test_lineage_worker.py`, `python -m mypy --config-file mypy.ini app/workers/compute_executor_worker.py app/workers/lineage_worker.py`. |
| LP-CR-063 | 2026-03-14 | Request-path ambient config capture | Hardened | The live request path still had import-time `get_settings()` capture across returns-series, contribution, attribution, and performance endpoints/services. That is the wrong ownership boundary for offload policy and response metadata because executor thresholds, core-integration settings, stateful chunking settings, and `engine_version` could become implicitly tied to module import order instead of current runtime config. | Removed import-time settings capture from the returns-series, contribution, attribution, and performance request paths. Offload decisions now resolve thresholds through `get_settings()` at call time, returns-series stateful sourcing resolves core and chunking config per request, and contribution/attribution/TWR/MWR response metadata now reads `APP_VERSION` at call time instead of from a frozen module global. Added lower-level tests proving patched runtime settings drive returns-series offload, contribution/attribution offload, returns-series stateful sourcing config, and contribution/attribution `engine_version` metadata. Also updated affected integration tests to patch the real shared settings object instead of deleted module-global `settings`. | Next: continue the ambient-state cleanup in remaining live service/store singletons where import-time configuration still hides ownership boundaries, but keep the slice focused on real runtime behavior rather than broad mechanical rewrites. | Files: `app/services/returns_series_service.py`, `app/services/contribution_service.py`, `app/services/attribution_service.py`, `app/api/endpoints/returns_series.py`, `app/api/endpoints/contribution.py`, `app/api/endpoints/performance.py`, `tests/unit/app/test_returns_series_endpoint_helpers.py`, `tests/unit/app/test_request_path_runtime_settings.py`, `tests/unit/services/test_returns_series_service.py`, `tests/integration/test_recovery_drill_history_api.py`, `tests/integration/test_returns_series_api.py`; validation: `python -m pytest tests/unit/app/test_returns_series_endpoint_helpers.py tests/unit/app/test_request_path_runtime_settings.py tests/unit/services/test_returns_series_service.py -q`, `python -m pytest tests/integration/test_recovery_drill_history_api.py tests/integration/test_returns_series_api.py -q`, `python -m ruff check app/services/returns_series_service.py app/services/contribution_service.py app/services/attribution_service.py app/api/endpoints/returns_series.py app/api/endpoints/contribution.py app/api/endpoints/performance.py tests/unit/app/test_returns_series_endpoint_helpers.py tests/unit/app/test_request_path_runtime_settings.py tests/unit/services/test_returns_series_service.py tests/integration/test_recovery_drill_history_api.py tests/integration/test_returns_series_api.py`, `python -m mypy --config-file mypy.ini app/services/returns_series_service.py app/services/contribution_service.py app/services/attribution_service.py app/api/endpoints/returns_series.py app/api/endpoints/contribution.py app/api/endpoints/performance.py`, `python scripts/openapi_quality_gate.py`, `python -m pytest -q`. |
| LP-CR-064 | 2026-03-14 | Lineage storage-path ambient config capture | Hardened | The lineage singleton and lineage retrieval endpoint still froze `LINEAGE_STORAGE_PATH` at import time. That is the wrong ownership boundary for a live storage path because lineage artifact writes and reads could continue targeting a stale directory after runtime config changes, and tests had to mutate imported module globals instead of relying on current runtime settings. | Refactored `LineageService` so the singleton resolves `LINEAGE_STORAGE_PATH` at call time when no explicit storage path is injected, while still supporting explicit paths for isolated tests. Updated the lineage endpoint to read the storage path through `get_settings()` at request time instead of a frozen module-global settings object. Added a lower-level unit proof that `LineageService()` honors a patched runtime storage path and kept the lineage integration pack green against the new ownership boundary. | Next: continue the same ambient-state cleanup in remaining durable-store singletons where import-time configuration still hides ownership boundaries, but prefer slices that affect real runtime behavior rather than purely mechanical singleton rewrites. | Files: `app/services/lineage_service.py`, `app/api/endpoints/lineage.py`, `tests/unit/services/test_lineage_service.py`; validation: `python -m pytest tests/unit/services/test_lineage_service.py tests/integration/test_lineage_api.py -q`, `python -m ruff check app/services/lineage_service.py app/api/endpoints/lineage.py tests/unit/services/test_lineage_service.py tests/integration/test_lineage_api.py`, `python -m mypy --config-file mypy.ini app/services/lineage_service.py app/api/endpoints/lineage.py`. |
| LP-CR-065 | 2026-03-14 | Lineage artifact filesystem fencing | Hardened | The lineage service wrote artifact filenames directly from calculation-detail keys into the calculation directory. That is a real filesystem safety gap: a poisoned payload or upstream bug could attempt path traversal via `../` or absolute-style filenames and escape the intended lineage directory boundary. | Added explicit artifact filename validation in `LineageService` and enforced it in both `_serialize_details(...)` and `materialize_payload(...)`. Unsafe names are now rejected on enqueue and revalidated on materialization, so even poisoned durable payloads cannot escape the calculation directory. Added lower-level tests proving unsafe artifact names are rejected at both boundaries while preserving normal lineage behavior. | Next: keep reviewing filesystem and durable-worker boundaries for similar trust violations, but prefer targeted invariants like this over broad speculative rewrites. | Files: `app/services/lineage_service.py`, `tests/unit/services/test_lineage_service.py`; validation: `python -m pytest tests/unit/services/test_lineage_service.py tests/integration/test_lineage_api.py -q`, `python -m ruff check app/services/lineage_service.py tests/unit/services/test_lineage_service.py tests/integration/test_lineage_api.py`, `python -m mypy --config-file mypy.ini app/services/lineage_service.py`. |
| LP-CR-066 | 2026-03-14 | Controlled lineage artifact serving and bootstrap race safety | Hardened | The app still exposed lineage artifacts through a broad `StaticFiles` mount over the entire lineage directory. That is looser than necessary for a banking-grade artifact surface because file serving was not tied to the calculation record or artifact allow-list. While tightening this path, parallel process startup also exposed a real bootstrap race: concurrent imports could both try to create the lineage storage directory and one would fail with `FileExistsError`. | Replaced the broad static mount with an explicit lineage artifact download route bound to `calculation_id` plus artifact allow-list membership, so clients retrieve files through a controlled API path rather than a raw mounted directory. Updated lineage responses to generate those controlled artifact URLs and added integration coverage for successful artifact download plus unknown-artifact rejection. Hardened lineage storage bootstrap with `exist_ok=True` and an existing-directory short circuit so concurrent startup/import flows no longer race on directory creation. | Next: continue reviewing durable background-worker and filesystem boundaries for overly broad exposure or startup races, but keep the slices grounded in concrete runtime failure modes like this one. | Files: `app/api/endpoints/lineage.py`, `main.py`, `app/services/lineage_service.py`, `tests/integration/test_lineage_api.py`; validation: `python -m pytest tests/integration/test_lineage_api.py tests/unit/services/test_lineage_service.py -q`, `python -m ruff check app/api/endpoints/lineage.py main.py app/services/lineage_service.py tests/integration/test_lineage_api.py tests/unit/services/test_lineage_service.py`, `python -m mypy --config-file mypy.ini app/api/endpoints/lineage.py main.py app/services/lineage_service.py`. |
| LP-CR-067 | 2026-03-14 | Lineage artifact documentation drift | Hardened | After LP-CR-066, the runtime contract switched from broad `/lineage/...` static-file URLs to controlled `/performance/lineage/{calculation_id}/artifacts/{artifact_name}` downloads, but the living docs still showed the old path shape. That would leave operators and client developers reading stale guidance immediately after the artifact-surface hardening landed. | Updated the user guide, reproducibility guide, and API reference to describe the controlled artifact download route and example URLs. Extended the public-docs regression pack so the user guide now asserts the artifact download route is documented. | Next: keep the docs focused on living contracts and update RFC examples separately if those archived design documents need the same normalization pass. | Files: `docs/Portfolio Performance Analytics - A User Guide.md`, `docs/guides/reproducibility.md`, `docs/guides/api_reference.md`, `tests/unit/docs/test_public_docs_contract.py`; validation: `python -m pytest tests/unit/docs/test_public_docs_contract.py -q`, `python scripts/openapi_quality_gate.py`. |
| LP-CR-068 | 2026-03-14 | Atomic lineage artifact writes | Hardened | Even after the lineage artifact route and filename fencing were hardened, the service still wrote lineage artifacts directly to their final paths. That is a crash-consistency gap: a worker crash or replace-time failure could leave a partially written final artifact in the lineage directory. | Added an atomic text-write helper in `LineageService` that writes to a same-directory temp file, flushes and fsyncs, then uses `os.replace(...)` to publish the final artifact. Updated request, response, detail, and manifest writes to use the atomic helper. Added a lower-level failure-path test proving a replace failure leaves neither a final target file nor a stray temp file behind. | Next: continue reviewing other durable artifact or recovery evidence writes for the same crash-consistency guarantee, but keep the focus on concrete persisted outputs rather than speculative abstraction work. | Files: `app/services/lineage_service.py`, `tests/unit/services/test_lineage_service.py`; validation: `python -m pytest tests/unit/services/test_lineage_service.py -q`, `python -m ruff check app/services/lineage_service.py tests/unit/services/test_lineage_service.py`, `python -m mypy --config-file mypy.ini app/services/lineage_service.py`. |
| LP-CR-069 | 2026-03-14 | Atomic recovery-drill evidence writes | Hardened | The governed recovery drill had become part of the repo's durable recovery control plane, but its evidence files still wrote directly to final paths. That is a real crash-consistency gap for banking-grade recovery evidence: a process failure during write or replace-time error could leave partial `latest.json`, manifest, or timestamped evidence artifacts behind. | Upgraded the recovery-drill script to publish timestamped evidence, `latest.json`, and `manifest.json` through a same-directory temp-file, flush, fsync, and replace path. Added a direct failure-path unit test proving that a replace-time failure leaves neither a final target artifact nor a stray temporary file behind. | Next: continue the persisted-output review on other operator-facing or audit-facing artifacts, but keep the slices tied to concrete durability boundaries rather than generalized file-helper abstraction churn. | Files: `scripts/durable_recovery_drill.py`, `tests/unit/test_durable_recovery_drill.py`; validation: `python -m pytest tests/unit/test_durable_recovery_drill.py -q`, `python -m ruff check scripts/durable_recovery_drill.py tests/unit/test_durable_recovery_drill.py`, `python -m mypy --config-file mypy.ini scripts/durable_recovery_drill.py`. |
| LP-CR-070 | 2026-03-14 | Recovery-drill explicit output artifact atomicity | Hardened | LP-CR-069 hardened retained recovery-history artifacts, but the explicit `output_path` evidence file still wrote directly to its final path. That left the drill with asymmetric durability semantics: operators using the one-shot output artifact could still get a partial final file on write-time failure even though retained history was crash-consistent. | Routed `run_recovery_drill(..., output_path=...)` through the same atomic text-write helper used for retained history so manual evidence output and retained evidence now share the same publication semantics. Reused the existing failure-path proof on the atomic helper instead of introducing a second weaker artifact path. | Next: continue the persisted-output review in other operator or governance scripts, but only where the output is part of a real control-plane or audit workflow rather than incidental developer tooling. | Files: `scripts/durable_recovery_drill.py`, `tests/unit/test_durable_recovery_drill.py`; validation: `python -m pytest tests/unit/test_durable_recovery_drill.py -q`, `python -m ruff check scripts/durable_recovery_drill.py tests/unit/test_durable_recovery_drill.py`, `python -m mypy --config-file mypy.ini scripts/durable_recovery_drill.py`. |
| LP-CR-071 | 2026-03-14 | Queue-metrics false-green suppression on store failure | Hardened | The durable queue Prometheus collector caught compute or lineage store failures and then emitted zero backlog metrics. That is an operator-facing correctness risk: an outage in the durable metadata path could masquerade as an empty healthy queue instead of surfacing as degraded observability. | Added explicit `lotus_performance_durable_queue_store_availability{store=...}` metrics and changed the collector to suppress queue-depth and queue-age samples when the underlying store query fails instead of publishing misleading zero backlog values. Added unit and integration coverage proving healthy stores report availability `1` and outages report availability `0` without false queue samples. | Next: continue reviewing observability surfaces for similar false-green behavior, especially where control-plane or metrics code currently converts source failures into default-success data. | Files: `app/services/queue_metrics_service.py`, `tests/unit/services/test_queue_metrics_service.py`, `tests/integration/test_integration_capabilities_api.py`; validation: `python -m pytest tests/unit/services/test_queue_metrics_service.py tests/integration/test_integration_capabilities_api.py -q`, `python -m ruff check app/services/queue_metrics_service.py tests/unit/services/test_queue_metrics_service.py tests/integration/test_integration_capabilities_api.py`, `python -m mypy --config-file mypy.ini app/services/queue_metrics_service.py`. |
| LP-CR-072 | 2026-03-14 | Readiness schema-integrity truthfulness | Hardened | The durability readiness check only proved that the metadata database was reachable. That is too weak for a banking-grade ready signal because the service could still advertise ready while required durable tables for execution, compute, async results, or lineage were missing or corrupted. | Extended the durability health check to verify the owned durable schema is present by asserting all required durable tables exist after the database ping succeeds. Added a lower-level unit proof that missing required tables now produce `durable_metadata_schema_incomplete` instead of a false-ready status. | Next: continue hardening truth surfaces where the service can currently report healthy availability without proving the owned runtime contract is actually intact. | Files: `app/services/durability_health_service.py`, `tests/unit/services/test_durability_health_service.py`; validation: `python -m pytest tests/unit/services/test_durability_health_service.py -q`, `python -m ruff check app/services/durability_health_service.py tests/unit/services/test_durability_health_service.py`, `python -m mypy --config-file mypy.ini app/services/durability_health_service.py`. |
| LP-CR-073 | 2026-03-14 | Recovery-history unreadable false-green in runtime status | Hardened | The runtime-status recovery section treated an unreadable recovery-history snapshot the same as “no retained history yet” when no freshness threshold was configured. That is a false-green operator signal: the control plane could advertise recovery assurance as `available` even though the retained evidence source was actually unreadable. | Changed recovery-drill status building so an unreadable or unavailable history snapshot always surfaces as `unavailable`, regardless of freshness-threshold policy. Kept the threshold-dependent behavior only for the separate case where the history source is readable but empty. Added unit and integration coverage proving unreadable history now degrades aggregate runtime status and exposes the concrete reason. | Next: continue reviewing runtime-status and related control-plane surfaces for any remaining cases where source failures are collapsed into policy-driven “available” states. | Files: `app/services/runtime_status_service.py`, `tests/unit/services/test_runtime_status_service.py`, `tests/integration/test_runtime_status_api.py`; validation: `python -m pytest tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py -q`, `python -m ruff check app/services/runtime_status_service.py tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py`, `python -m mypy --config-file mypy.ini app/services/runtime_status_service.py`. |
| LP-CR-074 | 2026-03-14 | Recovery-history malformed manifest resilience | Hardened | The retained recovery-history snapshot path read and parsed `manifest.json` directly. If that manifest became unreadable or malformed, `/integration/recovery-drills` would fail with an internal server error and downstream control-plane consumers would lose a concrete operator reason. | Hardened the recovery-history snapshot builder to return structured `unavailable` snapshots with explicit reasons for unreadable or invalid manifests instead of raising raw file or JSON parse exceptions. Added unit and integration coverage proving malformed manifests now degrade cleanly as an operator-visible unavailable state. | Next: continue reviewing operator/control-plane artifact readers for the same pattern of raw parse failures escaping as 500s instead of structured unavailable reasons. | Files: `app/services/recovery_drill_history_service.py`, `tests/unit/services/test_recovery_drill_history_service.py`, `tests/integration/test_recovery_drill_history_api.py`; validation: `python -m pytest tests/unit/services/test_recovery_drill_history_service.py tests/integration/test_recovery_drill_history_api.py -q`, `python -m ruff check app/services/recovery_drill_history_service.py tests/unit/services/test_recovery_drill_history_service.py tests/integration/test_recovery_drill_history_api.py`, `python -m mypy --config-file mypy.ini app/services/recovery_drill_history_service.py`. |
| LP-CR-075 | 2026-03-14 | Lineage manifest malformed-reader resilience | Hardened | The lineage retrieval endpoint parsed `manifest.json` directly and collapsed malformed or unreadable manifests into a generic 500. That is weak operator behavior for a primary audit and reproducibility surface because callers could not distinguish missing lineage from corrupted lineage metadata. | Hardened the lineage endpoint to return explicit `503` responses for unreadable and invalid manifests while preserving the existing `404` for missing manifests and `500` for truly unexpected failures. Added integration coverage proving invalid manifests now surface as a concrete service-unavailable lineage condition. | Next: continue the same artifact-reader review on any remaining audit or control-plane surfaces where malformed persisted artifacts still escape as generic 500s. | Files: `app/api/endpoints/lineage.py`, `tests/integration/test_lineage_api.py`; validation: `python -m pytest tests/integration/test_lineage_api.py -q`, `python -m ruff check app/api/endpoints/lineage.py tests/integration/test_lineage_api.py`, `python -m mypy --config-file mypy.ini app/api/endpoints/lineage.py`. |
| LP-CR-076 | 2026-03-14 | Recovery-history manifest schema validation | Hardened | After LP-CR-074, the recovery-history reader handled unreadable and non-JSON manifests, but a syntactically valid manifest with missing keys, wrong types, or inconsistent top-level fields could still be treated as present and fail later with raw key or type errors. That is still below gold-standard control-plane resilience. | Added explicit manifest-shape validation for retained recovery-drill history, covering top-level fields, entry field types, and `latest_file_name` consistency against retained file names. Invalid-but-JSON manifests now degrade cleanly to `recovery_drill_manifest_invalid` instead of leaking structural errors into callers. Added unit and integration coverage proving structurally corrupted manifests are rejected as unavailable. | Next: continue the artifact-reader hardening program on other audit and control-plane surfaces where persisted metadata is still trusted without structural validation. | Files: `app/services/recovery_drill_history_service.py`, `tests/unit/services/test_recovery_drill_history_service.py`, `tests/integration/test_recovery_drill_history_api.py`; validation: `python -m pytest tests/unit/services/test_recovery_drill_history_service.py tests/integration/test_recovery_drill_history_api.py -q`, `python -m ruff check app/services/recovery_drill_history_service.py tests/unit/services/test_recovery_drill_history_service.py tests/integration/test_recovery_drill_history_api.py`, `python -m mypy --config-file mypy.ini app/services/recovery_drill_history_service.py`. |
| LP-CR-077 | 2026-03-14 | Engine performance characterization contract fidelity | Hardened | The repo's benchmark lane did not provide trustworthy capacity evidence. The existing benchmark claimed a 500k-row workload, but it reused only three dates, so the engine dataframe collapsed to 3 effective rows. A true 500k unique-daily-row dataset is also not representable in this engine path because timestamp-backed daily dates hit pandas/numpy bounds first. | Rebuilt the engine characterization workload around a truthful `75,000` unique-daily-row dataset, added an enforceable median runtime contract (`<= 0.50s` after warm-up across 5 measured runs), and documented the governed workload and its representability constraint. Added a dedicated `make performance-characterization` target so the characterization lane is explicit and repeatable instead of buried in an ad hoc benchmark file. | Next: extend the same characterization discipline to stateful retrieval and durable queue hot paths so capacity evidence covers orchestration and storage, not only engine compute. | Files: `tests/benchmarks/test_engine_performance.py`, `docs/technical/performance_characterization.md`, `Makefile`; validation: `python -m pytest tests/benchmarks/test_engine_performance.py -q`, `python -m ruff check tests/benchmarks/test_engine_performance.py`, `python -m mypy --config-file mypy.ini tests/benchmarks/test_engine_performance.py`. |
| LP-CR-078 | 2026-03-14 | Durable queue-stat characterization contracts | Hardened | After LP-CR-077, the engine hot path had a governed runtime budget, but the control-plane hot path behind `/integration/runtime-status` and `/metrics` still had no explicit capacity contract. That left durable queue aggregation exposed to silent performance regressions even though it is part of the service's operational control surface. | Added explicit characterization contracts for compute and lineage queue-stat aggregation, with bounded workloads (`5,000` compute jobs and `1,000` lineage payloads), measured median-runtime budgets, and repeatable benchmark tests. Expanded the performance characterization doc and made `make performance-characterization` run the full benchmark suite rather than only the engine benchmark. | Next: move outward to stateful retrieval and execution-polling characterization so orchestration and control-plane read paths are both governed under realistic load. | Files: `tests/benchmarks/test_runtime_store_performance.py`, `docs/technical/performance_characterization.md`, `Makefile`; validation: `python -m pytest tests/benchmarks/test_runtime_store_performance.py -q`, `python -m ruff check tests/benchmarks/test_runtime_store_performance.py`, `python -m mypy --config-file mypy.ini tests/benchmarks/test_runtime_store_performance.py`. |
| LP-CR-079 | 2026-03-14 | Execution-polling characterization contract | Hardened | After LP-CR-078, compute and queue-stat hot paths were governed, but the public async execution-polling read path still had no explicit capacity contract. That left a user-visible control-plane route uncharacterized under retained stage and upstream-snapshot load. | Added an execution-polling characterization test covering one async execution with five lifecycle stages, one hundred upstream snapshots, durable compute-job state, and durable async-result state. Recorded an explicit median-runtime budget and expanded the performance characterization doc so async polling is now part of the repo-owned capacity story. | Next: move outward to stateful retrieval and normalization characterization so upstream orchestration load is governed alongside compute and control-plane reads. | Files: `tests/benchmarks/test_execution_polling_performance.py`, `docs/technical/performance_characterization.md`; validation: `python -m pytest tests/benchmarks/test_execution_polling_performance.py -q`, `python -m ruff check tests/benchmarks/test_execution_polling_performance.py`, `python -m mypy --config-file mypy.ini tests/benchmarks/test_execution_polling_performance.py`. |
| LP-CR-080 | 2026-03-14 | Stateful portfolio retrieval characterization contract | Hardened | After LP-CR-079, compute, queue stats, and execution polling were governed, but the stateful portfolio-retrieval orchestration path still had no explicit capacity contract. That left the main long-window stateful input service uncharacterized even though it performs chunk planning, paginated upstream fetch, deduped merge, and durable upstream snapshot recording. | Added a stateful input characterization test for a ten-year portfolio window using 90-day chunks, paginated responses, canonical merge, and durable snapshot recording. Recorded an explicit median-runtime budget and expanded the performance characterization doc so orchestration capacity is now part of the repo-owned performance story. | Next: extend the same discipline to benchmark/risk-free stateful retrieval and possibly an end-to-end returns-series stateful path if we want a fuller orchestration budget beyond the portfolio source contract. | Files: `tests/benchmarks/test_stateful_input_performance.py`, `docs/technical/performance_characterization.md`; validation: `python -m pytest tests/benchmarks/test_stateful_input_performance.py -q`, `python -m ruff check tests/benchmarks/test_stateful_input_performance.py`, `python -m mypy --config-file mypy.ini tests/benchmarks/test_stateful_input_performance.py`. |
| LP-CR-081 | 2026-03-14 | Stateful snapshot write-amplification reduction | Hardened | The first stateful portfolio characterization exposed a real bottleneck: long-window retrieval spent disproportionate time rewriting upstream snapshot rows that had already been recorded for the same calculation and request fingerprints. That made repeated retrieval and replay-heavy paths significantly slower than necessary on file-backed durable stores. | Added batched upstream snapshot recording and taught the execution registry to skip already-persisted snapshot IDs during batch writes instead of re-merging them. This keeps snapshot durability intact while materially reducing redundant write overhead on repeated stateful retrieval of the same calculation. The stateful portfolio characterization contract now passes with the optimized write path. | Next: extend the same performance review to benchmark/risk-free retrieval and, if needed, consider deeper SQL-shape optimization for snapshot persistence on PostgreSQL specifically. | Files: `app/services/stateful_input_service.py`, `app/services/execution_registry.py`, `tests/benchmarks/test_stateful_input_performance.py`; validation: `python -m pytest tests/unit/services/test_stateful_input_service.py tests/benchmarks/test_stateful_input_performance.py -q`, `python -m ruff check app/services/stateful_input_service.py app/services/execution_registry.py tests/benchmarks/test_stateful_input_performance.py`, `python -m mypy --config-file mypy.ini app/services/stateful_input_service.py app/services/execution_registry.py tests/benchmarks/test_stateful_input_performance.py`. |
| LP-CR-082 | 2026-03-14 | Stateful reference retrieval characterization contracts | Hardened | After LP-CR-081, the portfolio side of the stateful input boundary was governed, but benchmark and risk-free reference retrieval still had no explicit capacity contract even though they use chunk planning, merge logic, and durable snapshot recording on the same orchestration boundary. | Added governed characterization tests for long-window benchmark return-series and risk-free retrieval, each covering ten-year windows, reference chunking, canonical merge, and snapshot recording. Expanded the performance characterization document so the full stateful input boundary now has explicit repo-owned budgets instead of only the portfolio leg being measured. | Next: the next larger performance step is a fuller end-to-end stateful returns-series orchestration contract if we want one public-route budget that composes retrieval, normalization, and response shaping together. | Files: `tests/benchmarks/test_stateful_input_performance.py`, `docs/technical/performance_characterization.md`; validation: `python -m pytest tests/benchmarks/test_stateful_input_performance.py -q`, `python -m ruff check tests/benchmarks/test_stateful_input_performance.py`, `python -m mypy --config-file mypy.ini tests/benchmarks/test_stateful_input_performance.py`. |
| LP-CR-083 | 2026-03-14 | PostgreSQL hot-path query-plan verification | Hardened | After LP-CR-082, the repo had runtime budgets for engine, queue stats, execution polling, and stateful retrieval, but it still lacked real PostgreSQL planner proof on the durable hot-path reads. That left the performance story incomplete: we knew the paths were fast locally, but not whether PostgreSQL used sane access paths under realistic backlog and snapshot-retention workloads. | Extracted first-class statement builders for compute queue stats, lineage queue stats, execution lookup, and ordered upstream snapshot reads so the repo can run `EXPLAIN (FORMAT JSON)` against the exact owned queries. Added a composite `analytics_upstream_snapshot(calculation_id, created_at_utc)` index with runtime creation safety, added unit proof that the index is declared, and added a PostgreSQL-backed benchmark pack that seeds realistic workloads, runs `ANALYZE`, and verifies plan shape. The governed contract is now explicit: queue-stat reads remain SQL-side aggregate plans without sort regressions, and ordered snapshot polling participates in the composite index without falling back to sequential scan. | Next: if we want to push the performance lane further, the next meaningful step is a full end-to-end stateful returns-series orchestration contract or higher-cardinality PostgreSQL characterization under concurrent worker load. The missing planner-proof gap itself is now closed. | Files: `app/services/compute_job_store.py`, `app/services/lineage_metadata_store.py`, `app/services/execution_registry.py`, `tests/unit/services/test_execution_registry.py`, `tests/benchmarks/test_postgres_query_plans.py`, `docs/technical/performance_characterization.md`, `Makefile`; validation: `docker compose up -d performance-lineage-db`, `python -m pytest tests/unit/services/test_execution_registry.py tests/benchmarks/test_postgres_query_plans.py -q`, `python -m pytest tests/benchmarks -q`, `python -m ruff check app/services/compute_job_store.py app/services/lineage_metadata_store.py app/services/execution_registry.py tests/unit/services/test_execution_registry.py tests/benchmarks/test_postgres_query_plans.py`, `python -m mypy --config-file mypy.ini app/services/compute_job_store.py app/services/lineage_metadata_store.py app/services/execution_registry.py tests/benchmarks/test_postgres_query_plans.py`. |
| LP-CR-084 | 2026-03-14 | PostgreSQL multi-worker lease-fencing proof | Hardened | After LP-CR-083, the repo had live PostgreSQL plan verification for the durable hot-path reads, but it still lacked live multi-worker proof that compute-job and lineage payload claims remain disjoint under concurrent workers. The store code already used `FOR UPDATE SKIP LOCKED`, but that behavior had only been proven by SQL compilation and single-process unit tests, not by real PostgreSQL concurrency. | Added a live PostgreSQL concurrency-proof pack that seeds pending compute jobs and lineage payloads, races two workers against the same queue using `ThreadPoolExecutor`, and asserts the claimed calculation sets are disjoint and exhaust the available work without duplicate pickup. Extended the PostgreSQL characterization target and doc so the repo now owns both planner proof and worker-fencing proof against the production dialect rather than only SQLite or compiled-SQL evidence. | Next: the strongest remaining runtime-hardening step in this lane is higher-cardinality or longer-running concurrent characterization, not basic duplicate-claim proof. The missing PostgreSQL worker-fencing evidence gap itself is now closed. | Files: `tests/benchmarks/postgres_runtime_helpers.py`, `tests/benchmarks/test_postgres_query_plans.py`, `tests/benchmarks/test_postgres_concurrency_contracts.py`, `docs/technical/performance_characterization.md`, `Makefile`; validation: `make performance-characterization-postgres`, `python -m pytest tests/benchmarks/test_postgres_query_plans.py tests/benchmarks/test_postgres_concurrency_contracts.py -q`, `python -m ruff check tests/benchmarks/postgres_runtime_helpers.py tests/benchmarks/test_postgres_query_plans.py tests/benchmarks/test_postgres_concurrency_contracts.py`, `python -m mypy --config-file mypy.ini tests/benchmarks/postgres_runtime_helpers.py tests/benchmarks/test_postgres_query_plans.py tests/benchmarks/test_postgres_concurrency_contracts.py`. |
| LP-CR-085 | 2026-03-14 | End-to-end stateful returns-series orchestration budget | Hardened | After LP-CR-084, the repo had component-level budgets for stateful input, execution polling, and engine compute, but it still lacked one composed budget over the actual stateful returns-series business path. That meant we could reason about the parts without owning a contract for retrieval, normalization, engine execution, and response shaping together. | Added a stateful returns-series orchestration characterization contract covering a ten-year daily request with portfolio, benchmark, and risk-free series enabled. The benchmark exercises the service-level orchestration path end-to-end against the existing stateful core-service stub and a durable execution registry, then records a median-runtime budget for the composed path. Expanded the characterization doc so the performance story now includes both component budgets and the composed returns-series contract. | Next: if we keep pushing on performance from here, the next stronger slice would be an async/offloaded long-window returns-series executor contract or higher-cardinality stateful orchestration under concurrent request load. The missing composed-budget gap itself is now closed. | Files: `tests/benchmarks/test_returns_series_orchestration_performance.py`, `docs/technical/performance_characterization.md`; validation: `python -m pytest tests/benchmarks/test_returns_series_orchestration_performance.py -q`, `python -m pytest tests/benchmarks -q`, `python -m ruff check tests/benchmarks/test_returns_series_orchestration_performance.py`, `python -m mypy --config-file mypy.ini tests/benchmarks/test_returns_series_orchestration_performance.py`. |
| LP-CR-086 | 2026-03-14 | Runtime-status work-item inspection anchors | Hardened | `/integration/runtime-status` exposed counts, ages, and degradation reasons, but operators still had to inspect the database manually to answer the most practical support question: which exact calculation or lineage item is oldest, stuck, or most recently failed right now. That made the control-plane snapshot informative but not directly actionable. | Added durable compute and lineage queue inspection-anchor lookups and surfaced them through runtime-status as best-effort operator handles. Compute queue snapshots now expose the oldest pending, leased, and running calculation IDs plus the latest terminal failure; lineage snapshots expose the oldest pending and leased calculation IDs plus the latest terminal failure. Kept the new lookups additive so anchor-query failure does not collapse the wider runtime-status snapshot. Added unit, model, integration, OpenAPI, and docs regression coverage proving the new operator drill-down contract is stable. | Next: the next stronger operator slice is a dedicated inspection surface for filtered stuck/failing work-item listings, not just single-anchor handles. The control-plane gap between backlog visibility and calculation-level drill-down is now materially closed. | Files: `app/services/compute_job_store.py`, `app/services/lineage_metadata_store.py`, `app/services/runtime_status_service.py`, `app/models/runtime_status.py`, `tests/unit/services/test_compute_job_store.py`, `tests/unit/services/test_lineage_metadata_store.py`, `tests/unit/models/test_runtime_status_models.py`, `tests/unit/services/test_runtime_status_service.py`, `tests/integration/test_runtime_status_api.py`, `docs/guides/api_reference.md`; validation: `python -m pytest tests/unit/services/test_compute_job_store.py tests/unit/services/test_lineage_metadata_store.py tests/unit/models/test_runtime_status_models.py tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py -q`, `python -m ruff check app/services/compute_job_store.py app/services/lineage_metadata_store.py app/services/runtime_status_service.py app/models/runtime_status.py tests/unit/services/test_compute_job_store.py tests/unit/services/test_lineage_metadata_store.py tests/unit/models/test_runtime_status_models.py tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py`, `python -m mypy --config-file mypy.ini app/services/compute_job_store.py app/services/lineage_metadata_store.py app/services/runtime_status_service.py app/models/runtime_status.py`, `python scripts/openapi_quality_gate.py`, `python -m pytest tests/unit/docs/test_public_docs_contract.py -q`. |
| LP-CR-087 | 2026-03-14 | Runtime work-item inspection surface | Hardened | After LP-CR-086, operators could jump to one anchor calculation quickly, but they still lacked a service-owned listing surface for the actual stuck or failed work behind queue pressure. That left triage stuck between a single anchor and manual database inspection. | Added `GET /integration/runtime-work-items`, backed by durable store inspection methods for compute and lineage work items. The new surface returns filtered `active`, `failed`, or `all` items per queue with exact calculation handles, lifecycle status, active-since timestamp, age, attempts, and durable failure context. Added lower-level store proofs plus integration coverage, and documented the route in the public API reference. | Next: the next stronger operator slice would be paging and richer filtering over this inspection surface, or linking these items directly to retry/reconcile actions if the platform wants an active control plane rather than read-only diagnostics. The gap between queue health and actionable item-level triage is now materially closed. | Files: `app/services/compute_job_store.py`, `app/services/lineage_metadata_store.py`, `app/services/runtime_work_item_service.py`, `app/models/runtime_work_items.py`, `app/api/endpoints/runtime_work_items.py`, `main.py`, `tests/unit/services/test_compute_job_store.py`, `tests/unit/services/test_lineage_metadata_store.py`, `tests/integration/test_runtime_work_items_api.py`, `docs/guides/api_reference.md`; validation: `python -m pytest tests/unit/services/test_compute_job_store.py tests/unit/services/test_lineage_metadata_store.py tests/integration/test_runtime_work_items_api.py -q`, `python -m ruff check app/services/compute_job_store.py app/services/lineage_metadata_store.py app/services/runtime_work_item_service.py app/models/runtime_work_items.py app/api/endpoints/runtime_work_items.py tests/unit/services/test_compute_job_store.py tests/unit/services/test_lineage_metadata_store.py tests/integration/test_runtime_work_items_api.py main.py`, `python -m mypy --config-file mypy.ini app/services/compute_job_store.py app/services/lineage_metadata_store.py app/services/runtime_work_item_service.py app/models/runtime_work_items.py app/api/endpoints/runtime_work_items.py main.py`. |
| LP-CR-088 | 2026-03-14 | Runtime work-item partial-failure truthfulness | Hardened | The initial runtime work-item surface added exact compute and lineage listings, but it still failed too harshly under partial reader failure: if one queue inspection path raised, the whole endpoint could collapse even when the other queue remained readable. That is below gold-standard control-plane behavior because it hides healthy evidence behind one bad reader. | Hardened runtime work-item snapshot building to degrade per queue instead of per endpoint. The response now carries queue-specific availability and reason fields, and the service falls back to empty items only for the queue that failed while preserving the other queue's data. Added unit and integration coverage proving compute-queue inspection failure no longer hides healthy lineage inspection or the durable-store readiness signal. | Next: the stronger operator step from here would be paging and richer filtering on the inspection surface, now that the base partial-failure behavior is truthful and stable. The gap between item-level inspection and partial-outage resilience is now closed. | Files: `app/services/runtime_work_item_service.py`, `app/models/runtime_work_items.py`, `tests/unit/services/test_runtime_work_item_service.py`, `tests/integration/test_runtime_work_items_api.py`, `docs/guides/api_reference.md`; validation: `python -m pytest tests/unit/services/test_runtime_work_item_service.py tests/integration/test_runtime_work_items_api.py -q`, `python -m ruff check app/services/runtime_work_item_service.py app/models/runtime_work_items.py tests/unit/services/test_runtime_work_item_service.py tests/integration/test_runtime_work_items_api.py`, `python -m mypy --config-file mypy.ini app/services/runtime_work_item_service.py app/models/runtime_work_items.py`, `python scripts/openapi_quality_gate.py`. |
| LP-CR-089 | 2026-03-14 | Stateful replay snapshot-build amplification | Hardened | The stateful returns-series orchestration benchmark regressed above its governed budget even though the core stateful retrieval contracts were still green. Investigation showed replay runs with the same `calculation_id` were rebuilding and hashing upstream snapshot payloads even when those snapshots already existed durably, wasting work on the hottest replay path in the composed benchmark. | Added durable snapshot-id introspection to the execution registry and taught the stateful input service to pre-check existing snapshot identities before rebuilding full snapshot payload fingerprints. Existing portfolio, benchmark, and risk-free snapshot records are now skipped before expensive response hashing and JSON building, while new snapshot records still follow the existing durable write path. Added a lower-level test proving duplicate stateful portfolio retrieval no longer rebuilds snapshots once the calculation already owns them. | Next: if stateful replay becomes an even bigger production path, the next stronger step would be caching or memoizing normalized upstream payloads across the full returns-series service path rather than only skipping duplicate snapshot-materialization work. The benchmark regression caused by duplicate snapshot build overhead is now closed. | Files: `app/services/execution_registry.py`, `app/services/stateful_input_service.py`, `tests/unit/services/test_stateful_input_service.py`, `tests/benchmarks/test_returns_series_orchestration_performance.py`; validation: `python -m pytest tests/unit/services/test_stateful_input_service.py tests/benchmarks/test_returns_series_orchestration_performance.py -q`, `python -m ruff check app/services/execution_registry.py app/services/stateful_input_service.py tests/unit/services/test_stateful_input_service.py tests/benchmarks/test_returns_series_orchestration_performance.py`, `python -m mypy --config-file mypy.ini app/services/execution_registry.py app/services/stateful_input_service.py`. |
| LP-CR-090 | 2026-03-14 | PostgreSQL benchmark availability fast-fail harness | Hardened | The PostgreSQL plan and concurrency proof tests were meant to skip when the proof database was unavailable, but the helper used the driver default connection timeout. In practice that made an unavailable PostgreSQL instance look like a hung benchmark lane instead of a fast, explicit skip. | Added an explicit short PostgreSQL connect timeout to the benchmark helper and a unit proof that the helper creates its SQLAlchemy engine with the fast-fail timeout. The PostgreSQL benchmark files now skip in seconds when the proof database is absent instead of consuming the benchmark timeout budget and confusing the signal. | Next: if the platform wants stronger benchmark determinism, the next step is wiring the PostgreSQL proof database lifecycle into automation so these tests run as `pass` more often and `skip` only when the environment truly lacks the proof dependency. The benchmark-harness hang ambiguity is now closed. | Files: `tests/benchmarks/postgres_runtime_helpers.py`, `tests/unit/benchmarks/test_postgres_runtime_helpers.py`, `tests/benchmarks/test_postgres_query_plans.py`, `tests/benchmarks/test_postgres_concurrency_contracts.py`; validation: `python -m pytest tests/unit/benchmarks/test_postgres_runtime_helpers.py tests/benchmarks/test_postgres_query_plans.py tests/benchmarks/test_postgres_concurrency_contracts.py -q`, `python -m ruff check tests/benchmarks/postgres_runtime_helpers.py tests/unit/benchmarks/test_postgres_runtime_helpers.py`. |
| LP-CR-091 | 2026-03-14 | Durable-store runtime ownership and readiness introspection boundary | Hardened | The owned durable stores still froze `LINEAGE_METADATA_DATABASE_URL` at import time via module-global singletons. That weakens runtime ownership, makes live config changes and isolated test harnesses harder to reason about, and left readiness checking coupled to `execution_registry._engine`, a private SQLAlchemy detail rather than a store-owned interface. | Added a shared runtime store resolver/proxy boundary and converted compute-job, lineage-metadata, execution-registry, and async-result stores to resolve against the active runtime database URL instead of a one-time import capture. Added a public `list_table_names()` method on `ExecutionRegistry` and moved durability readiness to that API so health checks no longer reach into private engine internals. Added lower-level tests proving the durable store globals follow runtime DB URL changes and that the readiness contract still reports reachable, unreachable, and schema-incomplete states correctly. | Next: continue the same ownership cleanup pattern on any remaining service modules that still capture durable runtime collaborators too early, but only where it changes real runtime/test isolation semantics rather than producing churn. The durable-store ownership gap itself is now materially closed. | Branch `review-ledger-durable-store-ownership`; files: `app/services/durable_store_runtime.py`, `app/services/compute_job_store.py`, `app/services/lineage_metadata_store.py`, `app/services/execution_registry.py`, `app/services/async_result_store.py`, `app/services/durability_health_service.py`, `tests/unit/services/test_durable_store_runtime.py`, `tests/unit/services/test_durability_health_service.py`; validation: `python -m pytest tests/unit/services/test_durable_store_runtime.py tests/unit/services/test_durability_health_service.py tests/unit/services/test_runtime_status_service.py tests/unit/services/test_runtime_work_item_service.py -q`, `python -m ruff check app/services/durable_store_runtime.py app/services/compute_job_store.py app/services/lineage_metadata_store.py app/services/execution_registry.py app/services/async_result_store.py app/services/durability_health_service.py tests/unit/services/test_durable_store_runtime.py tests/unit/services/test_durability_health_service.py tests/unit/services/test_runtime_status_service.py tests/unit/services/test_runtime_work_item_service.py`, `python -m mypy --config-file mypy.ini app/services/durable_store_runtime.py app/services/compute_job_store.py app/services/lineage_metadata_store.py app/services/execution_registry.py app/services/async_result_store.py app/services/durability_health_service.py tests/unit/services/test_durable_store_runtime.py`. |
| LP-CR-092 | 2026-03-14 | Runtime work-item triage ergonomics: queue scoping, paging, and stale-item filtering | Hardened | The new runtime work-item surface closed the gap between aggregate queue health and exact work-item drill-down, but it was still too blunt for real operations: no queue scoping, no paging, and no way to focus on truly stale work. Under larger backlogs, operators would still need repeated full-page scans or database queries to isolate the right items. | Extended compute and lineage inspection queries to return paged result sets with total counts, then upgraded `GET /integration/runtime-work-items` with `queue`, `offset`, and `min_age_seconds` filters plus per-queue `total_count` and `returned_count`. Kept partial-failure behavior intact so one queue can still degrade independently. Added lower-level store tests proving count-vs-returned semantics and stale-item filtering, service tests for excluded queues, integration coverage for queue-scoped stale-item triage, and updated the human API reference. | Next: the next stronger operator slice would be richer filtering by analytics type or calculation handle, or cursor-based paging if backlog sizes grow enough that offset scans become meaningfully expensive. The main ergonomics gap between item-level inspection and practical stuck-work triage is now materially closed. | Branch `review-ledger-durable-store-ownership`; files: `app/services/compute_job_store.py`, `app/services/lineage_metadata_store.py`, `app/services/runtime_work_item_service.py`, `app/models/runtime_work_items.py`, `app/api/endpoints/runtime_work_items.py`, `tests/unit/services/test_compute_job_store.py`, `tests/unit/services/test_lineage_metadata_store.py`, `tests/unit/services/test_runtime_work_item_service.py`, `tests/integration/test_runtime_work_items_api.py`, `docs/guides/api_reference.md`; validation: `python -m pytest tests/unit/services/test_compute_job_store.py tests/unit/services/test_lineage_metadata_store.py tests/unit/services/test_runtime_work_item_service.py tests/integration/test_runtime_work_items_api.py -q`, `python -m ruff check app/services/compute_job_store.py app/services/lineage_metadata_store.py app/services/runtime_work_item_service.py app/models/runtime_work_items.py app/api/endpoints/runtime_work_items.py tests/unit/services/test_compute_job_store.py tests/unit/services/test_lineage_metadata_store.py tests/unit/services/test_runtime_work_item_service.py tests/integration/test_runtime_work_items_api.py`, `python -m mypy --config-file mypy.ini app/services/compute_job_store.py app/services/lineage_metadata_store.py app/services/runtime_work_item_service.py app/models/runtime_work_items.py app/api/endpoints/runtime_work_items.py`, `python scripts/openapi_quality_gate.py`. |
| LP-CR-093 | 2026-03-14 | Targeted runtime work-item filtering by analytics family and calculation handle | Hardened | After LP-CR-092, operators could page and stale-filter runtime work items, but they still could not directly answer questions like "show failed Attribution jobs" or "show items matching this calculation handle fragment" without client-side scanning. That left the triage surface meaningfully better, but still not sharp enough for focused incident response. | Extended compute and lineage inspection queries with durable filtering for `analytics_type` / `calculation_type` and `calculation_id_contains`, then surfaced those filters through `GET /integration/runtime-work-items`. Added the echoed filter state to the response contract so snapshots are self-describing in logs and runbooks. Added lower-level store tests for both filter types, service tests to prove filter propagation, integration coverage for targeted filtered queries, and updated the API reference. | Next: if operator demand keeps growing, the next stronger step would be cursor paging or explicit sort controls, but the core targeted triage gap is now materially closed. | Branch `review-ledger-durable-store-ownership`; files: `app/services/compute_job_store.py`, `app/services/lineage_metadata_store.py`, `app/services/runtime_work_item_service.py`, `app/models/runtime_work_items.py`, `app/api/endpoints/runtime_work_items.py`, `tests/unit/services/test_compute_job_store.py`, `tests/unit/services/test_lineage_metadata_store.py`, `tests/unit/services/test_runtime_work_item_service.py`, `tests/integration/test_runtime_work_items_api.py`, `docs/guides/api_reference.md`; validation: `python -m pytest tests/unit/services/test_compute_job_store.py tests/unit/services/test_lineage_metadata_store.py tests/unit/services/test_runtime_work_item_service.py tests/integration/test_runtime_work_items_api.py -q`, `python -m ruff check app/services/compute_job_store.py app/services/lineage_metadata_store.py app/services/runtime_work_item_service.py app/models/runtime_work_items.py app/api/endpoints/runtime_work_items.py tests/unit/services/test_compute_job_store.py tests/unit/services/test_lineage_metadata_store.py tests/unit/services/test_runtime_work_item_service.py tests/integration/test_runtime_work_items_api.py`, `python -m mypy --config-file mypy.ini app/services/compute_job_store.py app/services/lineage_metadata_store.py app/services/runtime_work_item_service.py app/models/runtime_work_items.py app/api/endpoints/runtime_work_items.py`, `python scripts/openapi_quality_gate.py`. |
| LP-CR-094 | 2026-03-14 | Reclaimable runtime work-item triage for expired worker leases | Hardened | Even after paging and targeted filters, operators still could not directly ask for the work items that were immediately eligible for recovery because their worker lease had already expired. That left one of the most operationally important backlog slices buried inside age inference and store internals instead of making it a first-class control-plane concept. | Added a `reclaimable` status filter to the durable compute and lineage inspection queries and surfaced it through `GET /integration/runtime-work-items`. Compute reclaimable items now include expired `leased` and `running` jobs awaiting reconciliation or re-lease; lineage reclaimable items include expired leased payloads still marked pending. Added store, service, and integration coverage proving the new filter returns only expired-lease items and updated the API reference so the recovery semantics are explicit for operators. | Next: the stronger follow-on from here is explicit recovery-outcome visibility such as latest reconciled or re-leased calculations, but the gap around directly triaging reclaimable work is now materially closed. | Branch `review-ledger-durable-store-ownership`; files: `app/services/compute_job_store.py`, `app/services/lineage_metadata_store.py`, `app/api/endpoints/runtime_work_items.py`, `tests/unit/services/test_compute_job_store.py`, `tests/unit/services/test_lineage_metadata_store.py`, `tests/unit/services/test_runtime_work_item_service.py`, `tests/integration/test_runtime_work_items_api.py`, `docs/guides/api_reference.md`; validation: `python -m pytest tests/unit/services/test_compute_job_store.py tests/unit/services/test_lineage_metadata_store.py tests/unit/services/test_runtime_work_item_service.py tests/integration/test_runtime_work_items_api.py -q`, `python -m ruff check app/services/compute_job_store.py app/services/lineage_metadata_store.py app/api/endpoints/runtime_work_items.py tests/unit/services/test_compute_job_store.py tests/unit/services/test_lineage_metadata_store.py tests/unit/services/test_runtime_work_item_service.py tests/integration/test_runtime_work_items_api.py`, `python -m mypy --config-file mypy.ini app/services/compute_job_store.py app/services/lineage_metadata_store.py app/api/endpoints/runtime_work_items.py`, `python scripts/openapi_quality_gate.py`. |
| LP-CR-095 | 2026-03-14 | Reclaimable backlog counts in runtime status and metrics | Hardened | After LP-CR-094, operators could query reclaimable expired-lease work directly, but the primary control-plane surfaces still did not quantify how much reclaimable backlog existed. That forced a second endpoint call to answer whether recovery pressure was present at all, which is avoidable friction during incident triage. | Extended durable compute and lineage queue stats with reclaimable counts, then surfaced those counts through `/integration/runtime-status` and the Prometheus queue failure-pressure metrics. Added lower-level store proofs for reclaimable counts, model and integration coverage for the runtime-status contract, and refreshed the API reference so the control-plane description matches the shipped behavior. | Next: the stronger follow-on from here is explicit recent recovery-outcome visibility, such as the latest reconciled or re-leased calculations, not just the size of the reclaimable backlog. The gap around quantifying reclaimable recovery pressure is now closed. | Branch `review-ledger-durable-store-ownership`; files: `app/services/compute_job_store.py`, `app/services/lineage_metadata_store.py`, `app/models/runtime_status.py`, `app/services/queue_metrics_service.py`, `tests/unit/services/test_compute_job_store.py`, `tests/unit/services/test_lineage_metadata_store.py`, `tests/unit/services/test_queue_metrics_service.py`, `tests/unit/models/test_runtime_status_models.py`, `tests/integration/test_runtime_status_api.py`, `docs/guides/api_reference.md`; validation: `python -m pytest tests/unit/services/test_compute_job_store.py tests/unit/services/test_lineage_metadata_store.py tests/unit/services/test_queue_metrics_service.py tests/unit/models/test_runtime_status_models.py tests/integration/test_runtime_status_api.py -q`, `python -m ruff check app/services/compute_job_store.py app/services/lineage_metadata_store.py app/models/runtime_status.py app/services/queue_metrics_service.py tests/unit/services/test_compute_job_store.py tests/unit/services/test_lineage_metadata_store.py tests/unit/services/test_queue_metrics_service.py tests/unit/models/test_runtime_status_models.py tests/integration/test_runtime_status_api.py`, `python -m mypy --config-file mypy.ini app/services/compute_job_store.py app/services/lineage_metadata_store.py app/models/runtime_status.py app/services/queue_metrics_service.py`, `python scripts/openapi_quality_gate.py`. |
| LP-CR-096 | 2026-03-14 | Recent recovery-outcome anchors in runtime status | Hardened | Even after reclaimable counts and reclaimable work-item filtering, operators still could not see whether recovery was actually happening recently. The control plane described recovery pressure, but not the latest concrete item successfully returned to pending after retry or stale-lease recovery. | Extended compute and lineage inspection anchors with `latest_recovered_calculation_id` and tightened lineage record timestamp semantics so status transitions update the durable record timestamp instead of leaving it at creation time. Compute now derives the latest recovered anchor from pending items with consumed attempts ordered by last durable error time; lineage now derives the latest recovered anchor from retried pending items ordered by the updated record transition timestamp. Added store, model, service, and integration coverage plus API-reference updates so recent recovery outcomes are visible on `/integration/runtime-status`. | Next: the next stronger operator slice is a read-only recent recovery event history rather than just the latest anchor, but the gap around concrete recent recovery outcomes is now materially closed. | Branch `review-ledger-durable-store-ownership`; files: `app/services/compute_job_store.py`, `app/services/lineage_metadata_store.py`, `app/models/runtime_status.py`, `tests/unit/services/test_compute_job_store.py`, `tests/unit/services/test_lineage_metadata_store.py`, `tests/unit/models/test_runtime_status_models.py`, `tests/unit/services/test_runtime_status_service.py`, `tests/integration/test_runtime_status_api.py`, `docs/guides/api_reference.md`; validation: `python -m pytest tests/unit/services/test_compute_job_store.py tests/unit/services/test_lineage_metadata_store.py tests/unit/models/test_runtime_status_models.py tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py -q`, `python -m ruff check app/services/compute_job_store.py app/services/lineage_metadata_store.py app/models/runtime_status.py tests/unit/services/test_compute_job_store.py tests/unit/services/test_lineage_metadata_store.py tests/unit/models/test_runtime_status_models.py tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py`, `python -m mypy --config-file mypy.ini app/services/compute_job_store.py app/services/lineage_metadata_store.py app/models/runtime_status.py`, `python scripts/openapi_quality_gate.py`. |
| LP-CR-097 | 2026-03-14 | Recent recovery event history in runtime status | Hardened | Even with latest recovery anchors, operators still only had a single recovered calculation handle. That left a gap between “recovery is happening” and “what actually recovered in the last few events,” especially when multiple jobs were being retried or requeued around the same time. | Added a bounded recent recovery history to `/integration/runtime-status` for both compute and lineage queues, governed by `RUNTIME_STATUS_RECENT_RECOVERY_LIMIT`. Compute recovery events now expose calculation handle, analytics type, recovery kind, timestamp, attempt count, and last error type; lineage recovery events expose the analogous pending-materialization recovery trail. Added durable store readers, runtime-status service plumbing, response-model serialization, and updated API-reference guidance so operators can see the latest few recovery events without a second endpoint call. | Next: the next stronger step would be a dedicated recent-recovery history endpoint with filtering or paging if operators need longer retained recovery timelines, but the gap around bounded recent recovery visibility on the primary control plane is now closed. | Branch `review-ledger-durable-store-ownership`; files: `app/core/config.py`, `app/services/compute_job_store.py`, `app/services/lineage_metadata_store.py`, `app/services/runtime_status_service.py`, `app/models/runtime_status.py`, `tests/unit/services/test_compute_job_store.py`, `tests/unit/services/test_lineage_metadata_store.py`, `tests/unit/services/test_runtime_status_service.py`, `tests/unit/models/test_runtime_status_models.py`, `tests/integration/test_runtime_status_api.py`, `docs/guides/api_reference.md`; validation: `python -m pytest tests/unit/services/test_compute_job_store.py tests/unit/services/test_lineage_metadata_store.py tests/unit/services/test_runtime_status_service.py tests/unit/models/test_runtime_status_models.py tests/integration/test_runtime_status_api.py -q`, `python -m ruff check app/services/compute_job_store.py app/services/lineage_metadata_store.py app/services/runtime_status_service.py app/models/runtime_status.py app/core/config.py tests/unit/services/test_compute_job_store.py tests/unit/services/test_lineage_metadata_store.py tests/unit/services/test_runtime_status_service.py tests/unit/models/test_runtime_status_models.py tests/integration/test_runtime_status_api.py`, `python -m mypy --config-file mypy.ini app/services/compute_job_store.py app/services/lineage_metadata_store.py app/services/runtime_status_service.py app/models/runtime_status.py app/core/config.py`, `python scripts/openapi_quality_gate.py`. |
| LP-CR-098 | 2026-03-14 | Dedicated runtime recovery inspection surface | Hardened | After LP-CR-097, operators could see only a bounded recent recovery sample inside `/integration/runtime-status`. That was good enough for quick signal, but still too constrained for incident drill-down because there was no queue-scoped, paged, filterable recovery history surface owned by the service. Longer recovery timelines still required database access or repeated ad hoc status polling. | Added `GET /integration/runtime-recoveries`, backed by durable compute and lineage recovery queries with queue scoping, offset paging, and targeted filters for analytics family, calculation family, and calculation-handle fragments. Extended the durable stores to return paged recovery result objects with exact `total_count` and `returned_count`, kept partial-failure semantics so one queue can degrade independently, and updated the runtime-status service to consume the new paged recovery contract safely. Added new unit, model, and integration coverage plus API-reference updates so recent recovery drill-down is now a first-class operator surface. | Next: the next stronger operator step would be cursor paging or time-range filters if retained recovery history grows large enough that offset-based inspection becomes expensive, but the main gap between bounded status snapshots and actionable recovery drill-down is now closed. | Branch `review-ledger-durable-store-ownership`; files: `app/services/compute_job_store.py`, `app/services/lineage_metadata_store.py`, `app/services/runtime_recovery_service.py`, `app/services/runtime_status_service.py`, `app/models/runtime_recoveries.py`, `app/api/endpoints/runtime_recoveries.py`, `main.py`, `tests/unit/services/test_compute_job_store.py`, `tests/unit/services/test_lineage_metadata_store.py`, `tests/unit/services/test_runtime_recovery_service.py`, `tests/unit/models/test_runtime_recoveries_models.py`, `tests/integration/test_runtime_recoveries_api.py`, `tests/unit/services/test_runtime_status_service.py`, `docs/guides/api_reference.md`; validation: `python -m pytest tests/unit/services/test_compute_job_store.py tests/unit/services/test_lineage_metadata_store.py tests/unit/services/test_runtime_recovery_service.py tests/unit/models/test_runtime_recoveries_models.py tests/integration/test_runtime_recoveries_api.py tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py -q`, `python -m ruff check app/services/compute_job_store.py app/services/lineage_metadata_store.py app/services/runtime_recovery_service.py app/services/runtime_status_service.py app/models/runtime_recoveries.py app/api/endpoints/runtime_recoveries.py main.py tests/unit/services/test_compute_job_store.py tests/unit/services/test_lineage_metadata_store.py tests/unit/services/test_runtime_recovery_service.py tests/unit/models/test_runtime_recoveries_models.py tests/integration/test_runtime_recoveries_api.py tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py`, `python -m mypy --config-file mypy.ini app/services/compute_job_store.py app/services/lineage_metadata_store.py app/services/runtime_recovery_service.py app/services/runtime_status_service.py app/models/runtime_recoveries.py app/api/endpoints/runtime_recoveries.py main.py`, `python scripts/openapi_quality_gate.py`. |
| LP-CR-099 | 2026-03-14 | Direct operator navigation links on runtime inspection surfaces | Hardened | The runtime work-item and runtime-recovery surfaces had become rich enough to show the right rows, but operators still had to manually construct the next-hop URLs to poll execution state or inspect lineage. That is avoidable friction during incident response and makes the control plane less actionable than it should be. | Added a shared operator-navigation helper and extended both runtime inspection contracts to include direct `execution_path` and `lineage_path` fields per compute item, lineage item, and recovery event. Added model-level serialization proofs plus integration assertions that the drill-down links are emitted on both `/integration/runtime-work-items` and `/integration/runtime-recoveries`, and updated the API reference to describe the surfaces as directly navigable, not just descriptive. | Next: if we want the operator plane to become even more task-oriented, the next step would be explicit result-path links for async compute jobs where a stable result route exists, but the core drill-down navigation gap is now closed. | Branch `review-ledger-durable-store-ownership`; files: `app/services/operator_navigation_service.py`, `app/models/runtime_work_items.py`, `app/models/runtime_recoveries.py`, `tests/unit/models/test_runtime_work_items_models.py`, `tests/unit/models/test_runtime_recoveries_models.py`, `tests/integration/test_runtime_work_items_api.py`, `tests/integration/test_runtime_recoveries_api.py`, `docs/guides/api_reference.md`; validation: `python -m pytest tests/unit/models/test_runtime_work_items_models.py tests/unit/models/test_runtime_recoveries_models.py tests/integration/test_runtime_work_items_api.py tests/integration/test_runtime_recoveries_api.py -q`, `python -m ruff check app/models/runtime_work_items.py app/models/runtime_recoveries.py app/services/operator_navigation_service.py tests/unit/models/test_runtime_work_items_models.py tests/unit/models/test_runtime_recoveries_models.py tests/integration/test_runtime_work_items_api.py tests/integration/test_runtime_recoveries_api.py`, `python -m mypy --config-file mypy.ini app/models/runtime_work_items.py app/models/runtime_recoveries.py app/services/operator_navigation_service.py`. |
| LP-CR-100 | 2026-03-14 | Direct async result navigation on runtime inspection surfaces | Hardened | Even after LP-CR-099, the operator plane still stopped one click short for async-capable workflows. Support could jump to execution state and lineage, but still had to know the endpoint-specific result route by memory for returns-series, contribution, and attribution. That left avoidable friction in the highest-value drill-down path: going from incident triage to the durable result payload. | Extended the shared operator-navigation helper to derive stable `result_path` links for async-capable analytics families and surfaced those optional links on runtime work-item and runtime-recovery responses. Returns-series, contribution, and attribution items now carry a direct result route; workflows without a stable async result route omit the field from the JSON contract. Added model-level and integration coverage proving both the positive and omitted-field cases, and updated the API reference so operators know the control plane can now link straight to final async results when that route exists. | Next: if we keep deepening the operator plane, the next worthwhile step would be time-range or cursor-driven history on the recovery side rather than more path enrichment. The main gap between runtime inspection and direct async result retrieval is now closed. | Branch `review-ledger-durable-store-ownership`; files: `app/services/operator_navigation_service.py`, `app/models/runtime_work_items.py`, `app/models/runtime_recoveries.py`, `tests/unit/models/test_runtime_work_items_models.py`, `tests/unit/models/test_runtime_recoveries_models.py`, `tests/integration/test_runtime_work_items_api.py`, `tests/integration/test_runtime_recoveries_api.py`, `docs/guides/api_reference.md`; validation: `python -m pytest tests/unit/models/test_runtime_work_items_models.py tests/unit/models/test_runtime_recoveries_models.py tests/integration/test_runtime_work_items_api.py tests/integration/test_runtime_recoveries_api.py -q`, `python -m ruff check app/models/runtime_work_items.py app/models/runtime_recoveries.py app/services/operator_navigation_service.py tests/unit/models/test_runtime_work_items_models.py tests/unit/models/test_runtime_recoveries_models.py tests/integration/test_runtime_work_items_api.py tests/integration/test_runtime_recoveries_api.py`, `python -m mypy --config-file mypy.ini app/models/runtime_work_items.py app/models/runtime_recoveries.py app/services/operator_navigation_service.py`, `python scripts/openapi_quality_gate.py`. |
| LP-CR-101 | 2026-03-14 | Pre-merge benchmark and proxy-type contract stabilization | Hardened | During the pre-merge full CI gate, three correctness/governance gaps showed up that the targeted slice packs had not exercised together: the durable-store proxy signatures were not fully reflected in bootstrap/worker typing, the PostgreSQL queue-plan benchmark still called a refactored statement builder with the old signature, and the composed stateful returns-series characterization budget was tighter than the current coverage-backed merge gate on this environment. That combination would have produced avoidable CI churn even though the underlying runtime behavior was correct. | Broadened bootstrap and worker type signatures so runtime store proxies are first-class in mypy, fixed the PostgreSQL compute-queue plan benchmark to pass the explicit `now` argument required by the current statement builder, and added snapshot-id caching inside `StatefulInputService` so stateful replay does not refetch the same durable snapshot-id set multiple times within one orchestration call. After measuring the optimized path under the actual full coverage-backed test gate, recalibrated the composed returns-series characterization budget from `1000ms` to `1200ms` and updated the performance characterization doc to keep the enforced contract truthful. | Next: if we want to tighten the composed returns-series budget again, the next meaningful step is deeper service-path optimization or a separate no-coverage benchmark lane, not an unrealistically strict threshold in the merge gate. The pre-merge instability gap is now closed. | Branch `review-ledger-durable-store-ownership`; files: `app/services/durable_metadata_bootstrap.py`, `app/services/stateful_input_service.py`, `app/workers/compute_executor_worker.py`, `app/workers/lineage_worker.py`, `tests/benchmarks/test_postgres_query_plans.py`, `tests/benchmarks/test_returns_series_orchestration_performance.py`, `docs/technical/performance_characterization.md`; validation: `python -m mypy --config-file mypy.ini app/services/durable_metadata_bootstrap.py app/workers/compute_executor_worker.py app/workers/lineage_worker.py`, `python -m pytest tests/benchmarks/test_postgres_query_plans.py::test_postgres_compute_queue_stats_plan_contract tests/benchmarks/test_returns_series_orchestration_performance.py -q`, `make ci`. |
| LP-CR-102 | 2026-03-14 | Runtime recovery history paging and time-window determinism | Hardened | `GET /integration/runtime-recoveries` had become a useful operator drill-down surface, but it still lacked two practical controls: deterministic page traversal metadata and recovery-time window narrowing. That forced support to guess whether more matching events remained and to overfetch wide recovery streams when isolating a specific incident window. | Extended compute and lineage recovery pages with queue-local `next_offset`, added inclusive `recovered_after` / `recovered_before` filters to the durable query and count paths, and threaded the new contract through the runtime recovery service, response models, endpoint, docs, and tests. The filtering and page math now live in the durable stores rather than in the API layer, so `total_count`, `returned_count`, and `next_offset` stay aligned with the real filtered result set. | Next: if retained recovery history grows beyond offset-friendly sizes, the next step is cursor-based traversal anchored on recovery timestamp. The current gap around deterministic paging and incident-window filtering is closed. | Branch `review-ledger-runtime-recovery-history`; files: `app/services/compute_job_store.py`, `app/services/lineage_metadata_store.py`, `app/services/runtime_recovery_service.py`, `app/models/runtime_recoveries.py`, `app/api/endpoints/runtime_recoveries.py`, `tests/unit/services/test_compute_job_store.py`, `tests/unit/services/test_lineage_metadata_store.py`, `tests/unit/services/test_runtime_recovery_service.py`, `tests/unit/models/test_runtime_recoveries_models.py`, `tests/integration/test_runtime_recoveries_api.py`, `docs/guides/api_reference.md`; validation: `python -m pytest tests/unit/services/test_compute_job_store.py tests/unit/services/test_lineage_metadata_store.py tests/unit/services/test_runtime_recovery_service.py tests/unit/models/test_runtime_recoveries_models.py tests/integration/test_runtime_recoveries_api.py -q`, `python -m ruff check app/services/compute_job_store.py app/services/lineage_metadata_store.py app/services/runtime_recovery_service.py app/models/runtime_recoveries.py app/api/endpoints/runtime_recoveries.py tests/unit/services/test_compute_job_store.py tests/unit/services/test_lineage_metadata_store.py tests/unit/services/test_runtime_recovery_service.py tests/unit/models/test_runtime_recoveries_models.py tests/integration/test_runtime_recoveries_api.py`, `python -m mypy --config-file mypy.ini app/services/compute_job_store.py app/services/lineage_metadata_store.py app/services/runtime_recovery_service.py app/models/runtime_recoveries.py app/api/endpoints/runtime_recoveries.py`, `python scripts/openapi_quality_gate.py`. |
| LP-CR-103 | 2026-03-14 | Runtime work-item paging determinism | Hardened | `GET /integration/runtime-work-items` exposed exact filtered compute and lineage items, but unlike the recovery endpoint it still gave operators no direct signal for whether another filtered page existed. Support had `total_count` and `returned_count`, but still had to derive traversal state themselves while the underlying queue could be moving. | Extended compute and lineage inspection pages with queue-local `next_offset`, pushed that page math into the durable store layer, and threaded the additive contract through the runtime work-item service, response models, docs, and tests. This keeps both operator drill-down endpoints aligned and makes page traversal deterministic without teaching clients queue-specific arithmetic. | Next: if these inspection surfaces need to scale to larger or more volatile backlogs, the follow-on is cursor-based traversal rather than offset-only paging. The current gap around next-page determinism on runtime work-item inspection is closed. | Branch `review-ledger-runtime-recovery-history`; files: `app/services/compute_job_store.py`, `app/services/lineage_metadata_store.py`, `app/services/runtime_work_item_service.py`, `app/models/runtime_work_items.py`, `tests/unit/services/test_runtime_work_item_service.py`, `tests/unit/models/test_runtime_work_items_models.py`, `tests/integration/test_runtime_work_items_api.py`, `docs/guides/api_reference.md`; validation: pending targeted pytest/ruff/mypy/openapi gate for this slice. |
| LP-CR-104 | 2026-03-14 | Deterministic seek pagination for runtime recovery history | Hardened | Offset paging plus `next_offset` made recovery-history traversal better, but it still drifted under live churn because newly recovered items could shift later pages between calls. That left the hottest operator lane, recent recovery triage during incidents, exposed to a moving-target pagination model. | Added seek-style recovery cursors keyed by recovery timestamp plus calculation handle across the durable compute and lineage recovery queries, then surfaced those cursors through the runtime recovery service, response contract, endpoint, docs, and tests. The endpoint now keeps its existing offset model for compatibility while also exposing `cursor_recovered_before` / `cursor_calculation_id_before` and queue-local next-cursor fields for deterministic traversal of older matching events. | Next: if we want to finish this lane completely, the follow-on is de-emphasizing offset-based traversal in docs and clients in favor of cursor-first usage for volatile backlogs. The core gold-standard gap around deterministic hot-stream recovery pagination is now closed. | Branch `review-ledger-runtime-recovery-history`; files: `app/services/compute_job_store.py`, `app/services/lineage_metadata_store.py`, `app/services/runtime_recovery_service.py`, `app/models/runtime_recoveries.py`, `app/api/endpoints/runtime_recoveries.py`, `tests/unit/services/test_compute_job_store.py`, `tests/unit/services/test_lineage_metadata_store.py`, `tests/unit/services/test_runtime_recovery_service.py`, `tests/unit/models/test_runtime_recoveries_models.py`, `tests/integration/test_runtime_recoveries_api.py`, `docs/guides/api_reference.md`; validation: pending targeted pytest/ruff/mypy/openapi gate for this slice. |
| LP-CR-105 | 2026-03-14 | Compute recovery timestamp UTC normalization | Hardened | While proving cursor-based recovery pagination end to end, the compute runtime surface exposed a deeper correctness bug: SQLite-backed recovery timestamps were being formatted as if naive datetimes were local time before conversion to UTC. On non-UTC machines, that could shift emitted `recovered_at_utc` values and any cursor or operator workflow built on them by the local offset. | Normalized compute timestamp formatting through the existing UTC coercion helper instead of direct `astimezone(...)` on possibly naive values, and added a lower-level store test that proves a SQLite-backed recovery event preserves the expected UTC timestamp string. This keeps runtime recovery inspection truthful across local environments instead of only under fully timezone-aware dialect behavior. | Next: the broader follow-on is reviewing other durable timestamp serialization paths for the same naive-local-time hazard, but the compute recovery path uncovered by this slice is now closed. | Branch `review-ledger-runtime-recovery-history`; files: `app/services/compute_job_store.py`, `tests/unit/services/test_compute_job_store.py`; validation: `python -m pytest tests/unit/services/test_compute_job_store.py tests/integration/test_runtime_recoveries_api.py -q`, `python -m ruff check app/services/compute_job_store.py tests/unit/services/test_compute_job_store.py tests/integration/test_runtime_recoveries_api.py`, `python -m mypy --config-file mypy.ini app/services/compute_job_store.py`. |
| LP-CR-106 | 2026-03-14 | Durable-store UTC serialization consistency | Hardened | After LP-CR-105, a follow-up audit found the same naive-local-time serialization hazard still present in other durable stores. `async_result_store` and `execution_registry` formatted timestamps through direct `astimezone(...)`, and `lineage_metadata_store.get_record(...)` had its own inline UTC conversion path. That meant local SQLite-backed environments could emit shifted UTC strings for async results, execution records, or lineage records even though the underlying data was intended to be UTC. | Standardized those paths around UTC coercion before formatting and added direct SQLite-backed unit proofs for async-result timestamps, execution-record timestamps, and lineage-record timestamps. This closes the broader durable-state contract gap instead of leaving UTC correctness dependent on which store or endpoint happens to be reading the record. | Next: if we want to finish the timestamp-hardening lane completely, the remaining work is a final sweep of non-store manifest or artifact metadata serialization paths for the same naive timestamp assumption. The durable database-backed surfaces are now aligned. | Branch `review-ledger-runtime-recovery-history`; files: `app/services/async_result_store.py`, `app/services/execution_registry.py`, `app/services/lineage_metadata_store.py`, `tests/unit/services/test_async_result_store.py`, `tests/unit/services/test_execution_registry.py`, `tests/unit/services/test_lineage_metadata_store.py`; validation: pending targeted pytest/ruff/mypy for this slice. |
| LP-CR-107 | 2026-03-14 | Lineage manifest and durable-record completion timestamp consistency | Hardened | After the durable-store UTC audit, the remaining timestamp-hardening gap was filesystem-backed rather than database-backed. `LineageService.materialize_payload(...)` wrote `manifest.json` from the pre-completion durable record and only then advanced the durable lineage record timestamp via `mark_complete(...)`. That could leave the persisted manifest and the DB-backed lineage API disagreeing about when materialization completed, undermining audit-trail trust. | Changed `LineageMetadataStore.mark_complete(...)` to accept an explicit completion timestamp and made `LineageService.materialize_payload(...)` generate a single completion timestamp that is used for both the durable record and the persisted `manifest.json`. Added regression coverage proving the manifest timestamp matches the metadata record timestamp after materialization and that explicit completion timestamps are preserved exactly at the store boundary. | Next: if we keep hardening artifact truthfulness, the next worthwhile review is whether any other persisted lineage metadata is still duplicated across filesystem and durable-store surfaces instead of being written from one canonical source. The completion-timestamp drift between manifest and record is now closed. | Branch `review-ledger-runtime-recovery-history`; files: `app/services/lineage_service.py`, `app/services/lineage_metadata_store.py`, `tests/unit/services/test_lineage_service.py`, `tests/unit/services/test_lineage_metadata_store.py`; validation: pending targeted pytest/ruff/mypy for this slice. |
| LP-CR-108 | 2026-03-14 | Lineage manifest structural and consistency validation | Hardened | Even after LP-CR-107, the lineage read path still treated `manifest.json` mostly as an existence check. The endpoint parsed the file, but did not require it to agree with the durable lineage record, so partial corruption or stale filesystem metadata could still masquerade as valid lineage while the API quietly trusted the DB-backed values instead. | Expanded `manifest.json` to include the artifact inventory, introduced explicit manifest validation on the lineage read path, and now require the manifest calculation type, completion timestamp, status, and artifact list to match the durable record exactly. Invalid manifests continue to return `503`, and now structurally valid but inconsistent manifests also degrade cleanly with a specific `503` instead of being silently ignored. Added service and integration coverage plus public-doc updates to describe the stronger audit contract. | Next: if we keep deepening lineage truthfulness, the next worthwhile pass is reviewing whether any artifact-download or manifest-reader paths still flatten distinct corruption modes into generic availability errors. The main read-side manifest drift gap is now closed. | Branch `review-ledger-runtime-recovery-history`; files: `app/services/lineage_service.py`, `app/api/endpoints/lineage.py`, `tests/unit/services/test_lineage_service.py`, `tests/integration/test_lineage_api.py`, `docs/guides/reproducibility.md`, `docs/guides/api_reference.md`; validation: pending targeted pytest/ruff/mypy/openapi gate for this slice. |
| LP-CR-109 | 2026-03-14 | Lineage artifact-download corruption semantics | Hardened | After LP-CR-108, the lineage status endpoint enforced manifest integrity, but the direct artifact-download route still bypassed that validation and flattened storage corruption into the same `404` used for unknown artifact names. That left a real operator gap: callers could hit the artifact route directly and get a misleading “not found” instead of a truthful degraded-state signal when lineage storage had drifted or partially disappeared. | Reused the manifest integrity validator on the artifact-download route, require the manifest to be present and consistent before serving any artifact, and now distinguish “unknown artifact name” (`404`) from “declared artifact missing from storage” (`503`). Added integration coverage for missing manifest, inconsistent manifest, and missing-on-disk artifact cases, and updated the public reproducibility/API docs so the artifact route’s corruption semantics match the stronger lineage-audit contract. | Next: if we keep hardening this lane, the remaining worthwhile pass is distinguishing unreadable-on-disk artifact content from missing artifact files where the framework allows that to be surfaced cleanly before streaming. The main artifact-route truthfulness gap is now closed. | Branch `review-ledger-runtime-recovery-history`; files: `app/api/endpoints/lineage.py`, `tests/integration/test_lineage_api.py`, `docs/guides/reproducibility.md`, `docs/guides/api_reference.md`; validation: pending targeted pytest/ruff/mypy/openapi gate for this slice. |
| LP-CR-110 | 2026-03-14 | Lineage status completeness vs on-disk artifact presence | Hardened | Even after LP-CR-109, the lineage status endpoint could still advertise a calculation as complete and return artifact URLs when the manifest and durable record agreed but one or more declared artifact files had already disappeared from disk. That left the control plane overstating lineage completeness and pushed the corruption signal later to the artifact-download step. | Added an explicit declared-artifact existence check to `GET /performance/lineage/{calculation_id}` so complete lineage now requires both manifest integrity and full on-disk artifact presence before URLs are returned. The endpoint now degrades with a specific `503` when the persisted artifact set is incomplete, and the public reproducibility/API docs were updated to document that stronger completeness contract. | Next: if we continue this lane, the next worthwhile step is a structured recovery or remediation hint for incomplete lineage storage rather than more generic degraded-state text. The main status-vs-storage truthfulness gap is now closed. | Branch `review-ledger-runtime-recovery-history`; files: `app/api/endpoints/lineage.py`, `tests/integration/test_lineage_api.py`, `docs/guides/reproducibility.md`, `docs/guides/api_reference.md`; validation: pending targeted pytest/ruff/mypy/openapi gate for this slice. |
| LP-CR-111 | 2026-03-14 | Runtime-status visibility for lineage storage outages | Hardened | The lineage API surfaces had become truthful about missing or corrupted artifacts, but `/integration/runtime-status` still only reflected durable DB health plus queue stats. That meant operators could see a healthy durable store and healthy queue counts while the lineage filesystem itself was missing or unreadable, discovering the problem only after calling the lineage endpoints directly. | Added a lineage-storage readiness check into the runtime-status lineage queue path and now report lineage storage outages as `lineage_queue.status="unavailable"` with concrete reasons like `lineage_storage_path_missing`, even when the durable metadata database is healthy. Added unit and integration coverage so the control plane now degrades explicitly when lineage storage is unavailable instead of hiding the issue behind later endpoint failures. | Next: if we keep deepening operator visibility, the next worthwhile step is attaching more specific remediation hints or storage-capacity signals rather than just availability state. The main control-plane blind spot around lineage storage outages is now closed. | Branch `review-ledger-runtime-recovery-history`; files: `app/services/runtime_status_service.py`, `tests/unit/services/test_runtime_status_service.py`, `tests/integration/test_runtime_status_api.py`, `docs/guides/api_reference.md`; validation: pending targeted pytest/ruff/mypy/openapi gate for this slice. |
| LP-CR-112 | 2026-03-14 | Shared durability readiness for lineage storage and DB-backed state | Hardened | After LP-CR-111, runtime status knew about lineage storage outages, but readiness still only checked durable database health. That left a split-brain operator contract: `/integration/runtime-status` could say lineage storage was broken while `/health/ready` still returned `200 ready`, which is not a safe commercial-grade readiness boundary. | Moved lineage storage validation into the shared durability-health service and made durable readiness require both metadata-store health and usable lineage storage. `runtime_status_service` now reuses that shared check instead of carrying its own filesystem probe, and `/health/ready` fails closed on lineage storage outages with the same concrete reason surfaced by runtime status. Added unit and integration coverage across durability health, readiness, and runtime status so the service now has one owned definition of durable readiness. | Next: if we continue this lane, the next worthwhile step is surfacing remediation guidance or capacity pressure for lineage storage rather than only availability state. The split between readiness and runtime-status durability truth is now closed. | Branch `review-ledger-runtime-recovery-history`; files: `app/services/durability_health_service.py`, `app/services/runtime_status_service.py`, `tests/unit/services/test_durability_health_service.py`, `tests/unit/services/test_runtime_status_service.py`, `tests/integration/test_integration_capabilities_api.py`, `tests/integration/test_runtime_status_api.py`, `docs/guides/api_reference.md`; validation: `python -m pytest tests/unit/services/test_durability_health_service.py tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py tests/integration/test_integration_capabilities_api.py -q`, `python -m ruff check app/services/durability_health_service.py app/services/runtime_status_service.py app/api/endpoints/health.py tests/unit/services/test_durability_health_service.py tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py tests/integration/test_integration_capabilities_api.py`, `python -m mypy --config-file mypy.ini app/services/durability_health_service.py app/services/runtime_status_service.py app/api/endpoints/health.py`, `python scripts/openapi_quality_gate.py`. |
| LP-CR-113 | 2026-03-14 | Lineage storage write-probe readiness | Hardened | The shared durability check for lineage storage had improved the contract boundary, but it still only validated directory existence, type, and access bits. That is not strong enough for a service whose runtime success depends on actually persisting lineage artifacts; path metadata can look healthy while writes still fail due to mount issues, filesystem errors, or stale permission state. | Strengthened `check_lineage_storage_ready()` to perform a real write/delete probe in the lineage storage directory by default, added configuration to disable the probe when explicitly needed, and added lower-level tests proving probe failure, disabled-probe behavior, and temp-file cleanup. Updated the public readiness contract docs so `usable lineage storage` now explicitly means write-probe verified storage, not just a present directory. | Next: if we keep deepening this lane, the next worthwhile step is surfacing richer storage remediation or capacity signals rather than only availability outcomes. The main gap between path-level checks and actual lineage-write readiness is now closed. | Branch `review-ledger-runtime-recovery-history`; files: `app/core/config.py`, `app/services/durability_health_service.py`, `tests/unit/services/test_durability_health_service.py`, `docs/guides/api_reference.md`; validation: pending targeted pytest/ruff/mypy/openapi gate for this slice. |
| LP-CR-114 | 2026-03-14 | Operator remediation hints for durability failures | Hardened | After LP-CR-113, the service had become much more accurate about readiness and lineage storage failures, but it still mostly gave operators reason codes without telling them the likely recovery action. That is workable for engineers who know the system well, but it is unnecessary friction for support and operations during incident triage. | Added a service-owned remediation-hint mapping for the highest-value durability failure reasons and surfaced those hints through `/health/ready` and `/integration/runtime-status`. Readiness failures now include `remediation_hint` when the service knows the next recovery step, and runtime status includes the same guidance on durable metadata store and lineage queue unavailability reasons. Added integration coverage for lineage storage path failures and write-probe failures plus documentation updates so the operator contract is now both truthful and actionable. | Next: if we keep deepening this operator lane, the next worthwhile step is structured remediation metadata or docs links rather than only a short hint string. The main gap between failure reporting and immediate operator guidance is now closed. | Branch `review-ledger-runtime-recovery-history`; files: `app/services/remediation_hint_service.py`, `app/api/endpoints/health.py`, `app/models/runtime_status.py`, `tests/integration/test_integration_capabilities_api.py`, `tests/integration/test_runtime_status_api.py`, `docs/guides/api_reference.md`; validation: pending targeted pytest/ruff/mypy/openapi gate for this slice. |
| LP-CR-115 | 2026-03-14 | Proactive lineage storage saturation visibility | Hardened | After LP-CR-114, the service had become truthful and actionable about lineage storage outages, but it still only reacted once storage was already unavailable or unwritable. That left a real operational blind spot: support could see a healthy storage path while free space was collapsing toward zero, with no first-class control-plane signal until the first artifact write failed. | Added a shared lineage-storage capacity snapshot in the durability-health layer, surfaced storage totals/free space/free ratio through `/integration/runtime-status`, and introduced configurable lineage-storage degradation thresholds for minimum free bytes and free-capacity ratio. Runtime status now degrades proactively with `lineage_storage_free_bytes_below_threshold` or `lineage_storage_free_ratio_below_threshold`, includes remediation hints for those reasons, and exposes the active storage-capacity policy alongside the observed capacity numbers. Added lower-level capacity math coverage, runtime-status service proofs, response-model serialization coverage, API integration assertions, and operator-doc updates so storage saturation risk is visible before it turns into a write outage. | Next: if we keep deepening this lane, the next worthwhile step is pushing these storage-pressure signals into Prometheus or other long-horizon observability surfaces, not just the point-in-time runtime-status control plane. The main gap around pre-failure lineage storage saturation visibility is now closed. | Branch `review-ledger-runtime-recovery-history`; files: `app/core/config.py`, `app/services/durability_health_service.py`, `app/services/runtime_status_service.py`, `app/models/runtime_status.py`, `app/services/remediation_hint_service.py`, `tests/unit/services/test_durability_health_service.py`, `tests/unit/services/test_runtime_status_service.py`, `tests/unit/models/test_runtime_status_models.py`, `tests/integration/test_runtime_status_api.py`, `docs/guides/api_reference.md`; validation: `python -m pytest tests/unit/services/test_durability_health_service.py tests/unit/services/test_runtime_status_service.py tests/unit/models/test_runtime_status_models.py tests/integration/test_runtime_status_api.py -q`, `python -m ruff check app/core/config.py app/services/durability_health_service.py app/services/runtime_status_service.py app/models/runtime_status.py app/services/remediation_hint_service.py tests/unit/services/test_durability_health_service.py tests/unit/services/test_runtime_status_service.py tests/unit/models/test_runtime_status_models.py tests/integration/test_runtime_status_api.py`, `python -m mypy --config-file mypy.ini app/core/config.py app/services/durability_health_service.py app/services/runtime_status_service.py app/models/runtime_status.py app/services/remediation_hint_service.py`, `python scripts/openapi_quality_gate.py`. |
| LP-CR-116 | 2026-03-14 | Prometheus visibility for lineage storage saturation | Hardened | LP-CR-115 made lineage storage pressure visible in the point-in-time runtime-status surface, but the signal still vanished from long-horizon observability. Operators could inspect the current free-space risk manually, yet Prometheus had no first-class storage-capacity series to alert on or trend over time, which left the storage saturation lane weaker than the existing queue-pressure metrics. | Extended the durable queue collector to export lineage storage capacity availability, total/used/free bytes, free-capacity ratio, and the configured proactive pressure thresholds alongside the existing queue metrics. Kept the same truthfulness rule used elsewhere in the review program: when capacity reads fail, the collector emits explicit availability `0` and threshold gauges, but does not emit false zero byte or ratio samples. Added collector-level proofs, `/metrics` integration coverage, and runtime-topology/API-reference updates so long-horizon observability now matches the runtime-status storage-pressure contract. | Next: if we keep deepening this lane, the next worthwhile step is adding explicit alert/runbook guidance for these new storage-capacity series rather than only surfacing the raw metrics. The main gap between point-in-time storage saturation visibility and time-series observability is now closed. | Branch `review-ledger-runtime-recovery-history`; files: `app/services/queue_metrics_service.py`, `tests/unit/services/test_queue_metrics_service.py`, `tests/integration/test_integration_capabilities_api.py`, `docs/guides/api_reference.md`, `docs/technical/runtime_topology.md`; validation: `python -m pytest tests/unit/services/test_queue_metrics_service.py tests/integration/test_integration_capabilities_api.py -q`, `python -m ruff check app/services/queue_metrics_service.py tests/unit/services/test_queue_metrics_service.py tests/integration/test_integration_capabilities_api.py`, `python -m mypy --config-file mypy.ini app/services/queue_metrics_service.py`. |
| LP-CR-117 | 2026-03-14 | Alert-ready lineage storage pressure breach metrics | Hardened | LP-CR-116 exported the raw lineage storage capacity values and configured thresholds, but alerting still had to reimplement the threshold comparison externally. That is workable, but weaker than the rest of the runtime contract because the service already owns the storage-pressure policy and should expose the breach state directly rather than requiring every downstream alert or dashboard to restate the logic. | Extended the Prometheus collector with `lotus_performance_lineage_storage_pressure_breach{reason=...}` so the time-series surface now exports the same threshold-breach semantics used by runtime status. The collector only emits breach gauges when capacity inspection succeeds, preventing a false `0` from being interpreted as “healthy” when the storage-capacity source is unavailable. Added collector and `/metrics` integration proofs for both non-breach and breach cases, and updated operator docs so these new gauges are treated as the intended alert-ready storage-saturation signals. | Next: if we continue this lane, the strongest follow-on is explicit runbook and alert-threshold guidance tied to these breach gauges rather than more raw metric expansion. The gap between raw storage telemetry and policy-aligned alertability is now materially closed. | Branch `review-ledger-runtime-recovery-history`; files: `app/services/queue_metrics_service.py`, `tests/unit/services/test_queue_metrics_service.py`, `tests/integration/test_integration_capabilities_api.py`, `docs/guides/api_reference.md`, `docs/technical/runtime_topology.md`; validation: `python -m pytest tests/unit/services/test_queue_metrics_service.py tests/integration/test_integration_capabilities_api.py -q`, `python -m ruff check app/services/queue_metrics_service.py tests/unit/services/test_queue_metrics_service.py tests/integration/test_integration_capabilities_api.py`, `python -m mypy --config-file mypy.ini app/services/queue_metrics_service.py`. |
| LP-CR-118 | 2026-03-14 | Alert-ready queue policy breach metrics | Hardened | Even after LP-CR-117, Prometheus still exposed most queue degradation policy only as raw counters and ages. That meant alerting for compute and lineage backlog pressure still had to restate the same threshold logic that `runtime_status_service` already owns, which is weaker than exporting the breach state directly from the service. | Extended the Prometheus collector with `lotus_performance_compute_queue_degradation_breach{reason=...}` and `lotus_performance_lineage_queue_degradation_breach{reason=...}` so the time-series surface now exports active queue-threshold breaches for compute retry backlog, lease-expiry pressure, terminal failures, pending/leased/running age, and lineage retry/terminal/pending/leased pressure. Kept the same truthfulness rule as the rest of the collector: breach gauges are emitted only when the underlying durable queue stats are available, avoiding false `0` samples when the source is unavailable. Added collector-level proofs, a durable-state-backed `/metrics` integration proof, and operator-doc updates so these gauges become the first-class alert surface for queue degradation policy instead of a downstream reimplementation. | Next: if we keep deepening this lane, the strongest follow-on is explicit runbook/alert guidance tied to these breach gauges or the analogous recovery-drill breach surface, not more raw metric expansion. The gap between queue policy in runtime-status and queue policy in Prometheus is now materially closed. | Branch `review-ledger-runtime-recovery-history`; files: `app/services/queue_metrics_service.py`, `tests/unit/services/test_queue_metrics_service.py`, `tests/integration/test_integration_capabilities_api.py`, `docs/guides/api_reference.md`, `docs/technical/runtime_topology.md`; validation: `python -m pytest tests/unit/services/test_queue_metrics_service.py tests/integration/test_integration_capabilities_api.py -q`, `python -m ruff check app/services/queue_metrics_service.py tests/unit/services/test_queue_metrics_service.py tests/integration/test_integration_capabilities_api.py`, `python -m mypy --config-file mypy.ini app/services/queue_metrics_service.py`. |
| LP-CR-119 | 2026-03-14 | Alert-ready recovery-drill policy metrics | Hardened | Even after LP-CR-118, recovery assurance was still the last major runtime-policy area that remained mostly trapped in `/integration/runtime-status`. Operators could inspect recovery-drill age and failure state via JSON, but Prometheus had no first-class surface for alerting on stale or failed retained recovery drills, which left the runtime governance story asymmetric. | Extended the Prometheus collector with recovery-drill availability, latest-age, policy-threshold, and degradation-breach gauges derived from the retained recovery-drill history plus the configured max-age policy. The collector now exports `lotus_performance_recovery_drill_degradation_breach{reason=...}` for both `recovery_drill_latest_not_passed` and `recovery_drill_age_exceeded`, while keeping the same truthfulness rule used elsewhere: if retained drill history is unavailable, the collector emits availability `0` but does not fake a healthy breach state. Added collector-level proofs, a `/metrics` integration proof for stale/failed drill history, and operator-doc updates so recovery assurance is now alertable alongside queue and storage policy. | Next: if we keep pushing this lane, the strongest follow-on is explicit alert/runbook guidance tied to these now-complete breach gauges rather than more observability surface expansion. The main gap between recovery-drill policy in runtime-status and Prometheus is now closed. | Branch `review-ledger-runtime-recovery-history`; files: `app/services/queue_metrics_service.py`, `tests/unit/services/test_queue_metrics_service.py`, `tests/integration/test_integration_capabilities_api.py`, `docs/guides/api_reference.md`, `docs/technical/runtime_topology.md`; validation: `python -m pytest tests/unit/services/test_queue_metrics_service.py tests/integration/test_integration_capabilities_api.py -q`, `python -m ruff check app/services/queue_metrics_service.py tests/unit/services/test_queue_metrics_service.py tests/integration/test_integration_capabilities_api.py`, `python -m mypy --config-file mypy.ini app/services/queue_metrics_service.py`. |
| LP-CR-120 | 2026-03-14 | Governed operator runbook for breach gauges | Hardened | After LP-CR-119, the service exported alert-ready queue, storage, and recovery-drill breach gauges, but the repo still had no governed operator contract telling support which metrics are first-class, what they mean, and what the first response sequence should be. That left the observability layer technically strong but operationally under-specified. | Added `docs/runbooks/runtime-alerts.md` as the first-response runbook for queue degradation, lineage storage pressure, and recovery-drill breach gauges. The runbook defines the authoritative alert surfaces, the required availability-gauge checks, the first-response inspection sequence across runtime-status, runtime-work-items, runtime-recoveries, and recovery-drill history, and the escalation conditions for persistence or combined breaches. Updated the API reference and runtime topology docs to point operators at the runbook and added a docs regression test so the breach gauges and linked operator surfaces cannot drift out of the governed guidance. | Next: if we keep deepening this lane, the next strongest step is turning these documented alert semantics into environment-level alert rule templates or platform-wide SLO defaults. The repo-level gap between alert-ready metrics and operator guidance is now closed. | Branch `review-ledger-runtime-recovery-history`; files: `docs/runbooks/runtime-alerts.md`, `docs/guides/api_reference.md`, `docs/technical/runtime_topology.md`, `tests/unit/docs/test_public_docs_contract.py`; validation: `python -m pytest tests/unit/docs/test_public_docs_contract.py -q`, `python -m ruff check tests/unit/docs/test_public_docs_contract.py`. |
| LP-CR-121 | 2026-03-14 | Governed alert-rule templates for runtime breach gauges | Hardened | After LP-CR-120, the repo had a solid first-response runbook, but operators still had to translate that narrative guidance into Prometheus expressions by hand. That left one more drift point between service-owned breach semantics and environment-level alert definitions. | Added `docs/operations/runtime-alert-rule-templates.md` as the governed Prometheus-style rule-template artifact for queue degradation, lineage storage pressure, recovery-drill policy, and their matching availability gauges. The templates intentionally point at the service-owned breach metrics instead of re-encoding threshold logic outside the service, and they link back to the runbook for incident handling. Updated the API reference, runtime topology, and runtime alert runbook to point at the templates, and extended the docs regression pack so the template file must keep covering the exported breach and availability gauges. | Next: if we keep deepening this lane, the next strongest step is platform-level default severity/SLO policy rather than more repo-local observability guidance. The repo-level gap between alert-ready metrics and concrete alert-rule templates is now closed. | Branch `review-ledger-runtime-recovery-history`; files: `docs/operations/runtime-alert-rule-templates.md`, `docs/runbooks/runtime-alerts.md`, `docs/guides/api_reference.md`, `docs/technical/runtime_topology.md`, `tests/unit/docs/test_public_docs_contract.py`; validation: `python -m pytest tests/unit/docs/test_public_docs_contract.py -q`, `python -m ruff check tests/unit/docs/test_public_docs_contract.py`. |
| LP-CR-122 | 2026-03-14 | Governed severity defaults for runtime alert policy | Hardened | After LP-CR-121, the repo had concrete rule templates, but severity and response expectations still lived only as inline labels inside one template artifact. That left a policy gap: environments could still reinterpret the same queue, storage, and recovery gauges differently because there was no single governed standard saying which conditions are page-worthy and which are governance-ticket class. | Added `docs/standards/runtime-alert-policy.md` as the authoritative severity and response policy for runtime breach and availability gauges. The policy defines `page` versus `ticket`, maps every exported breach/availability metric family to a default severity, sets response targets, and defines when recovery-drill alerts must be promoted from `ticket` to `page`. Updated the alert templates, runtime runbook, API reference, runtime topology, and standards docs to point at the new policy, and extended the docs regression pack so the policy coverage and links stay aligned. | Next: if we keep pushing this lane, the next strongest step is exporting this policy into platform-shared alert packages or default environment overlays rather than keeping it repo-local. The repo-level gap between alert templates and governed severity defaults is now closed. | Branch `review-ledger-runtime-recovery-history`; files: `docs/standards/runtime-alert-policy.md`, `docs/operations/runtime-alert-rule-templates.md`, `docs/runbooks/runtime-alerts.md`, `docs/guides/api_reference.md`, `docs/technical/runtime_topology.md`, `docs/standards/scalability-availability.md`, `docs/standards/enterprise-readiness.md`, `tests/unit/docs/test_public_docs_contract.py`; validation: pending targeted docs regression and lint for this slice. |
| LP-CR-123 | 2026-03-14 | Governed runtime threshold profiles by environment | Hardened | After LP-CR-122, alert severity and response policy were governed, but the underlying runtime thresholds still lacked a repo-owned environment profile. That left a real commercial-grade gap: the same alert policy could sit on top of arbitrary queue-age, failure-pressure, storage-capacity, or recovery-drill thresholds because there was no canonical dev/staging/production baseline for the settings in `app/core/config.py`. | Added `docs/standards/runtime-threshold-profiles.md` as the authoritative profile standard for runtime degradation settings. The new standard defines the controlled settings surface, provides recommended dev, staging, and production values for compute, lineage, storage, and recovery-drill thresholds, and states adoption rules so production does not silently run with disabled thresholds. Updated the alert policy, alert templates, API reference, runtime topology, and enterprise/scalability standards to point at the profile doc, and extended the docs regression pack so the controlled settings and cross-links stay governed. | Next: if we keep pushing this lane, the next strongest step is converting these repo-owned profiles into concrete deployment overlay examples or platform-shared configuration bundles. The repo-level gap between alert policy and governed threshold baselines is now closed. | Branch `review-ledger-runtime-recovery-history`; files: `docs/standards/runtime-threshold-profiles.md`, `docs/standards/runtime-alert-policy.md`, `docs/operations/runtime-alert-rule-templates.md`, `docs/guides/api_reference.md`, `docs/technical/runtime_topology.md`, `docs/standards/scalability-availability.md`, `docs/standards/enterprise-readiness.md`, `tests/unit/docs/test_public_docs_contract.py`; validation: pending targeted docs regression and lint for this slice. |
| LP-CR-124 | 2026-03-14 | Concrete environment overlays for runtime threshold profiles | Hardened | After LP-CR-123, the repo had a governed threshold standard, but operators still had to transcribe the values into deployment config by hand. That left a practical delivery gap: even with the right policy, profile adoption remained error-prone because there were no concrete env artifacts to copy into deployment overlays. | Added repo-owned env overlay examples for development, staging, and production under `docs/examples/`, covering the full runtime threshold surface for compute, lineage, storage, and recovery-drill settings. Updated the threshold-profile standard, API reference, and runtime topology docs to point at those files, and extended the docs regression pack so the example overlays stay aligned to the documented profile values. | Next: if we keep pushing this lane, the next strongest step is exporting these overlays into platform-shared config bundles or compose/env examples that can be consumed without manual copy/paste. The repo-level gap between threshold standards and concrete deployment artifacts is now closed. | Branch `review-ledger-runtime-recovery-history`; files: `docs/examples/runtime-thresholds.development.env`, `docs/examples/runtime-thresholds.staging.env`, `docs/examples/runtime-thresholds.production.env`, `docs/standards/runtime-threshold-profiles.md`, `docs/guides/api_reference.md`, `docs/technical/runtime_topology.md`, `tests/unit/docs/test_public_docs_contract.py`; validation: pending targeted docs regression and lint for this slice. |
| LP-CR-125 | 2026-03-14 | Compose-ready threshold profile overlays | Hardened | After LP-CR-124, the repo had concrete env examples, but operators still had to translate those values into actual compose service definitions by hand. That left one more deployment gap for the default local/runtime topology because the thresholds were documented and exampled, yet not directly consumable as a compose override. | Added repo-owned compose override examples for development, staging, and production under `docs/examples/`, covering `performance-analytics`, `performance-lineage-worker`, and `performance-compute-executor` with the governed threshold settings. Updated the threshold-profile standard, API reference, runtime topology, and README to show how to layer the override files over `docker-compose.yml`, and extended the docs regression pack so the compose overlays stay aligned to the governed profile values. | Next: if we keep pushing this lane, the next strongest step is a platform-shared config bundle or targeted refresh workflow that consumes these overlays automatically rather than only documenting the compose command. The repo-level gap between threshold standards and compose-ready deployment artifacts is now closed. | Branch `review-ledger-runtime-recovery-history`; files: `docs/examples/docker-compose.runtime-thresholds.development.yml`, `docs/examples/docker-compose.runtime-thresholds.staging.yml`, `docs/examples/docker-compose.runtime-thresholds.production.yml`, `README.md`, `docs/standards/runtime-threshold-profiles.md`, `docs/guides/api_reference.md`, `docs/technical/runtime_topology.md`, `tests/unit/docs/test_public_docs_contract.py`; validation: pending targeted docs regression and lint for this slice. |
| LP-CR-126 | 2026-03-14 | Durable runtime retention cleanup lifecycle | Hardened | The repo had strong recovery and observability coverage for durable runtime state, but no governed cleanup lifecycle for aged terminal records and lineage artifacts. That left a real production gap: retained control-plane state would grow without an owned pruning contract, and operators had no first-class cleanup runbook or script-backed evidence path. | Added runtime-retention lifecycle support across the owned durable stores plus a shared retention service and CLI. Terminal executions, terminal compute jobs, async results, terminal lineage metadata, and matching lineage artifact directories can now be counted or pruned against a governed retention cutoff via `python scripts/runtime_retention_cleanup.py`, with dry-run-first behavior and JSON summaries. Added lower-level pruning tests for each durable store, an end-to-end retention service test covering both dry-run and apply behavior, a runbook for operator execution, and an API-reference entry so the lifecycle control is discoverable. | Next: if we keep pushing this lane, the strongest follow-on is wiring the cleanup into governed automation or scheduled operations with retained operator evidence, not expanding the deletion surface beyond terminal state. The repo-level gap around durable runtime retention lifecycle automation is now materially closed. | Branch `review-ledger-retention-lifecycle`; files: `app/core/config.py`, `app/services/async_result_store.py`, `app/services/compute_job_store.py`, `app/services/execution_registry.py`, `app/services/lineage_metadata_store.py`, `app/services/runtime_retention_service.py`, `scripts/runtime_retention_cleanup.py`, `docs/runbooks/runtime-retention-cleanup.md`, `docs/guides/api_reference.md`, `tests/unit/services/test_async_result_store.py`, `tests/unit/services/test_compute_job_store.py`, `tests/unit/services/test_execution_registry.py`, `tests/unit/services/test_lineage_metadata_store.py`, `tests/unit/services/test_runtime_retention_service.py`, `tests/unit/docs/test_public_docs_contract.py`; validation: `python -m pytest tests/unit/services/test_async_result_store.py tests/unit/services/test_execution_registry.py tests/unit/services/test_compute_job_store.py tests/unit/services/test_lineage_metadata_store.py tests/unit/services/test_runtime_retention_service.py tests/unit/docs/test_public_docs_contract.py -q`, `python -m ruff check app/core/config.py app/services/async_result_store.py app/services/compute_job_store.py app/services/execution_registry.py app/services/lineage_metadata_store.py app/services/runtime_retention_service.py scripts/runtime_retention_cleanup.py tests/unit/services/test_async_result_store.py tests/unit/services/test_compute_job_store.py tests/unit/services/test_execution_registry.py tests/unit/services/test_lineage_metadata_store.py tests/unit/services/test_runtime_retention_service.py tests/unit/docs/test_public_docs_contract.py`, `python -m mypy --config-file mypy.ini app/services/runtime_retention_service.py scripts/runtime_retention_cleanup.py`. |
| LP-CR-127 | 2026-03-14 | Retained runtime-retention cleanup evidence and inspection surface | Hardened | LP-CR-126 added the cleanup mechanism, but auditability was still weak: unless an operator saved stdout manually, a runtime-retention execution left no durable evidence trail, no bounded history, and no service-owned inspection surface. That is below the standard already established for recovery drills and makes cleanup governance hard to prove later. | Extended `scripts/runtime_retention_cleanup.py` to persist timestamped evidence, refreshed `latest.json`, and `manifest.json` under a governed artifact path with count and age retention policy. Added runtime settings for the artifact directory and history retention. Added a read-side inspection surface at `GET /integration/runtime-retention-cleanups`, backed by a retention-history service and response models, so operators can query retained cleanup executions by operator, mode, status, and time window without touching the filesystem directly. Added unit coverage for evidence persistence and manifest pruning, service/model/integration coverage for the history endpoint, and updated the runbook plus API reference so the lifecycle is operationally discoverable. | Next: if we keep pushing this lane, the strongest follow-on is scheduled cleanup automation with retained operator or job identity, not widening the cleanup semantics beyond terminal state. The gap between runtime-retention execution and durable audit evidence is now materially closed. | Branch `review-ledger-retention-lifecycle`; files: `app/core/config.py`, `app/api/endpoints/runtime_retention_history.py`, `app/models/runtime_retention_history.py`, `app/services/runtime_retention_history_service.py`, `main.py`, `scripts/runtime_retention_cleanup.py`, `docs/runbooks/runtime-retention-cleanup.md`, `docs/guides/api_reference.md`, `tests/unit/test_runtime_retention_cleanup.py`, `tests/unit/services/test_runtime_retention_history_service.py`, `tests/unit/models/test_runtime_retention_history_models.py`, `tests/integration/test_runtime_retention_history_api.py`, `tests/unit/docs/test_public_docs_contract.py`; validation: pending targeted pytest/ruff/mypy/openapi gate for this slice. |
| LP-CR-128 | 2026-03-14 | Runtime-retention freshness and alertability | Hardened | LP-CR-127 made runtime-retention cleanup auditable, but operators still had no first-class runtime signal telling them whether cleanup governance was current. Without that, cleanup freshness remained discoverable only by manually querying the retained history surface, and Prometheus had no alert-ready lifecycle signal comparable to recovery-drill freshness. | Extended runtime status with a `runtime_retention` section and `runtime_retention_policy`, backed by the retained runtime-retention history. Runtime now degrades when the latest retained cleanup is stale or only a dry run. Mirrored that lifecycle signal into Prometheus via availability, latest-age, policy-threshold, and degradation-breach gauges. Propagated the new threshold into the governed threshold profiles, env overlays, compose overlays, alert runbook, alert templates, alert policy, and public API reference. Added unit, model, integration, metrics, and docs regression coverage so the runtime-retention lifecycle signal is test-backed and operator-visible across both JSON and time-series surfaces. | Next: the strongest follow-on is scheduled cleanup automation with service-owned operator/job identity so the new freshness policy has a standard execution path, not more retention-signal expansion. The gap between retained cleanup evidence and runtime/alert-level governance is now materially closed. | Branch `review-ledger-retention-lifecycle`; files: `app/core/config.py`, `app/models/runtime_status.py`, `app/services/runtime_status_service.py`, `app/services/queue_metrics_service.py`, `docs/guides/api_reference.md`, `docs/runbooks/runtime-alerts.md`, `docs/operations/runtime-alert-rule-templates.md`, `docs/standards/runtime-alert-policy.md`, `docs/standards/runtime-threshold-profiles.md`, `docs/examples/runtime-thresholds.development.env`, `docs/examples/runtime-thresholds.staging.env`, `docs/examples/runtime-thresholds.production.env`, `docs/examples/docker-compose.runtime-thresholds.development.yml`, `docs/examples/docker-compose.runtime-thresholds.staging.yml`, `docs/examples/docker-compose.runtime-thresholds.production.yml`, `tests/unit/models/test_runtime_status_models.py`, `tests/unit/services/test_runtime_status_service.py`, `tests/integration/test_runtime_status_api.py`, `tests/unit/services/test_queue_metrics_service.py`, `tests/integration/test_integration_capabilities_api.py`, `tests/unit/docs/test_public_docs_contract.py`; validation: `python -m pytest tests/unit/models/test_runtime_status_models.py tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py tests/unit/services/test_queue_metrics_service.py tests/integration/test_integration_capabilities_api.py tests/unit/docs/test_public_docs_contract.py -q`, `python -m ruff check app/core/config.py app/models/runtime_status.py app/services/runtime_status_service.py app/services/queue_metrics_service.py tests/unit/models/test_runtime_status_models.py tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py tests/unit/services/test_queue_metrics_service.py tests/integration/test_integration_capabilities_api.py tests/unit/docs/test_public_docs_contract.py`, `python -m mypy --config-file mypy.ini app/models/runtime_status.py app/services/runtime_status_service.py app/services/queue_metrics_service.py`, `python scripts/openapi_quality_gate.py`. |
| LP-CR-129 | 2026-03-14 | Live runtime-retention preview visibility | Hardened | LP-CR-128 made cleanup freshness visible, but operators still could not see the current retention debt without manually running the cleanup script. That left one operational blind spot: runtime could tell you cleanup was stale, but not how much terminal execution, async-result, or lineage debt would actually be removed right now. | Extended the runtime-retention status surface with a live dry-run preview under the active policy, including the current cutoff, retention window, and prunable execution, compute-job, async-result, lineage-record, and lineage-artifact counts. Mirrored that preview into Prometheus via `lotus_performance_runtime_retention_preview_availability` and `lotus_performance_runtime_retention_prunable_items{category=...}`. Updated the retention runbook and API reference so operators know to inspect the live preview before or after cleanup, and added targeted unit, integration, and docs regression coverage. | Next: the strongest follow-on is scheduled cleanup automation that records a service-owned operator or job identity and uses these preview counts as the pre-apply decision surface. The gap between retention freshness and retention debt visibility is now materially closed. | Branch `review-ledger-retention-lifecycle`; files: `app/models/runtime_status.py`, `app/services/runtime_status_service.py`, `app/services/queue_metrics_service.py`, `docs/guides/api_reference.md`, `docs/runbooks/runtime-retention-cleanup.md`, `tests/unit/models/test_runtime_status_models.py`, `tests/unit/services/test_runtime_status_service.py`, `tests/integration/test_runtime_status_api.py`, `tests/unit/services/test_queue_metrics_service.py`, `tests/integration/test_integration_capabilities_api.py`, `tests/unit/docs/test_public_docs_contract.py`; validation: `python -m pytest tests/unit/models/test_runtime_status_models.py tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py tests/unit/services/test_queue_metrics_service.py tests/integration/test_integration_capabilities_api.py tests/unit/docs/test_public_docs_contract.py -q`, `python -m ruff check app/models/runtime_status.py app/services/runtime_status_service.py app/services/queue_metrics_service.py tests/unit/models/test_runtime_status_models.py tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_status_api.py tests/unit/services/test_queue_metrics_service.py tests/integration/test_integration_capabilities_api.py tests/unit/docs/test_public_docs_contract.py`, `python -m mypy --config-file mypy.ini app/models/runtime_status.py app/services/runtime_status_service.py app/services/queue_metrics_service.py`. |
| LP-CR-130 | 2026-03-14 | Runtime-retention automation provenance and job identity | Hardened | LP-CR-129 exposed retention debt, but the retained cleanup evidence still could not prove whether the latest execution was a manual operator action or a scheduled automation run. That left an audit gap: history and runtime status carried operator identity and cleanup mode, but not the trigger path or automation job identity needed for commercial-grade operational traceability. | Extended the retained runtime-retention cleanup evidence schema with `trigger_mode` and optional `job_id`, added scheduled automation defaults to the runtime config, and taught `python scripts/runtime_retention_cleanup.py` to run in a governed scheduled mode. Propagated the new provenance fields through the manifest reader, history API, and runtime-status latest-cleanup view so operators can filter by scheduled/manual trigger and inspect automation job identity directly from control-plane surfaces. Updated the retention runbook and API reference to document scheduled execution, and added unit, model, integration, and docs regression coverage to keep the audit boundary test-backed. | Next: the strongest follow-on is platform-owned scheduling or service-triggered execution that uses these governed identities by default, not more manual cleanup variants. The repo-level gap between retention automation and auditable cleanup provenance is now materially closed. | Branch `review-ledger-retention-lifecycle`; files: `app/core/config.py`, `app/api/endpoints/runtime_retention_history.py`, `app/models/runtime_retention_history.py`, `app/models/runtime_status.py`, `app/services/runtime_retention_history_service.py`, `app/services/runtime_status_service.py`, `scripts/runtime_retention_cleanup.py`, `docs/guides/api_reference.md`, `docs/runbooks/runtime-retention-cleanup.md`, `tests/unit/test_runtime_retention_cleanup.py`, `tests/unit/services/test_runtime_retention_history_service.py`, `tests/unit/models/test_runtime_retention_history_models.py`, `tests/unit/models/test_runtime_status_models.py`, `tests/unit/services/test_runtime_status_service.py`, `tests/integration/test_runtime_retention_history_api.py`, `tests/integration/test_runtime_status_api.py`, `tests/integration/test_integration_capabilities_api.py`, `tests/unit/docs/test_public_docs_contract.py`; validation: pending targeted pytest/ruff/mypy/openapi gate for this slice. |
| LP-CR-131 | 2026-03-14 | Service-owned scheduled runtime-retention execution path | Hardened | LP-CR-130 established governed scheduled identity, but scheduled runtime-retention execution was still documentation-level only: the repo had a CLI and retained evidence semantics, but no service-owned worker or compose/runtime path to execute the cleanup on a schedule. That left a real production gap between governed policy and deployable automation. | Extracted runtime-retention execution and evidence persistence into a shared execution service, rewired the CLI onto that owned boundary, and added an optional `performance-runtime-retention-worker` with safe default dry-run behavior plus governed scheduled identity and job id. Added `make runtime-retention-smoke`, compose ops-profile wiring, and runtime/docs updates so scheduled retention execution is deployable and test-backed rather than just documented. Added dedicated unit coverage for the shared execution service, the worker loop, and the scheduled CLI path. | Next: the strongest follow-on is operator-surface security and audit hardening for these privileged runtime inspection routes now that retention automation is a real runtime component. The repo-level gap between retention governance and deployable scheduled execution is now materially closed. | Branch `review-ledger-retention-lifecycle`; files: `app/core/config.py`, `app/services/runtime_retention_execution_service.py`, `app/workers/runtime_retention_worker.py`, `scripts/runtime_retention_cleanup.py`, `docker-compose.yml`, `Makefile`, `README.md`, `docs/guides/api_reference.md`, `docs/runbooks/runtime-retention-cleanup.md`, `docs/technical/runtime_topology.md`, `tests/unit/services/test_runtime_retention_execution_service.py`, `tests/unit/services/test_runtime_retention_worker.py`, `tests/unit/test_runtime_retention_cleanup.py`, `tests/unit/docs/test_public_docs_contract.py`; validation: `python -m pytest tests/unit/services/test_runtime_retention_execution_service.py tests/unit/services/test_runtime_retention_worker.py tests/unit/test_runtime_retention_cleanup.py tests/unit/services/test_runtime_retention_history_service.py tests/unit/models/test_runtime_retention_history_models.py tests/unit/models/test_runtime_status_models.py tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_retention_history_api.py tests/integration/test_runtime_status_api.py tests/integration/test_integration_capabilities_api.py tests/unit/docs/test_public_docs_contract.py -q`, `python -m ruff check app/core/config.py app/services/runtime_retention_execution_service.py app/workers/runtime_retention_worker.py scripts/runtime_retention_cleanup.py tests/unit/services/test_runtime_retention_execution_service.py tests/unit/services/test_runtime_retention_worker.py tests/unit/test_runtime_retention_cleanup.py tests/unit/services/test_runtime_retention_history_service.py tests/unit/models/test_runtime_retention_history_models.py tests/unit/models/test_runtime_status_models.py tests/unit/services/test_runtime_status_service.py tests/integration/test_runtime_retention_history_api.py tests/integration/test_runtime_status_api.py tests/integration/test_integration_capabilities_api.py tests/unit/docs/test_public_docs_contract.py`, `python -m mypy --config-file mypy.ini app/core/config.py app/services/runtime_retention_execution_service.py app/workers/runtime_retention_worker.py scripts/runtime_retention_cleanup.py app/api/endpoints/runtime_retention_history.py app/models/runtime_retention_history.py app/models/runtime_status.py app/services/runtime_retention_history_service.py app/services/runtime_status_service.py`, `python scripts/openapi_quality_gate.py`. |
| LP-CR-132 | 2026-03-14 | Privileged-read authz for operator runtime surfaces | Hardened | Enterprise authz in the repo only governed write methods, while the highest-value operator GET endpoints remained effectively open even when enterprise authz was enabled. That left a material security gap: runtime status, work-item inspection, recovery inspection, and retention/recovery history exposed sensitive operational state without the same actor/tenant/capability enforcement already applied to privileged writes. | Extended the enterprise readiness middleware with a configurable privileged-read policy for selected `GET /integration/...` routes, with default governed coverage for runtime status, runtime work items, runtime recoveries, recovery-drill history, and runtime-retention history. Added `authorize_privileged_read_request(...)`, default capability mapping to `operations.runtime.read`, middleware enforcement and audit logging, plus unit and E2E proof that privileged operator reads are denied without enterprise identity/capability context when enforcement is enabled. Updated enterprise-readiness and API-reference docs so the security contract is explicit and test-backed. | Next: the strongest follow-on is extending auditability from allow/deny semantics into richer operator action evidence on the same surfaces, not widening the protected set blindly. The repo-level gap between write authz and operator read authz is now materially closed. | Branch `review-ledger-retention-lifecycle`; files: `app/enterprise_readiness.py`, `docs/standards/enterprise-readiness.md`, `docs/guides/api_reference.md`, `tests/unit/app/test_enterprise_readiness.py`, `tests/unit/app/test_enterprise_readiness_additional.py`, `tests/e2e/test_workflow_journeys.py`, `tests/unit/docs/test_public_docs_contract.py`; validation: pending targeted pytest/ruff/openapi gate for this slice. |
| LP-CR-133 | 2026-03-14 | Audit metadata for allowed privileged runtime reads | Hardened | LP-CR-132 blocked unauthorized operator reads, but allowed privileged reads still emitted the same generic audit shape as ordinary writes. That meant investigations could prove that a runtime route was accessed, but not which governed surface was inspected or which capability justified the access. For sensitive operator endpoints, that is weaker than the repo’s target audit standard. | Extended the enterprise audit middleware so allowed privileged reads now emit structured audit metadata including `access_mode="privileged_read"`, the governed surface path, and the required capability. Added unit proof that an allowed runtime-status read records those fields, and updated enterprise-readiness plus API-reference docs so the stronger audit contract is explicit. This keeps the new read authz lane from stopping at allow/deny semantics and makes sensitive runtime inspection materially more traceable. | Next: the strongest follow-on is applying the same audit-evidence depth to privileged operator actions that trigger state changes or remediation workflows, not widening the read-protected set further. The gap between privileged read authz and privileged read auditability is now materially closed. | Branch `review-ledger-retention-lifecycle`; files: `app/enterprise_readiness.py`, `docs/standards/enterprise-readiness.md`, `docs/guides/api_reference.md`, `tests/unit/app/test_enterprise_readiness.py`, `tests/unit/docs/test_public_docs_contract.py`; validation: `python -m pytest tests/unit/app/test_enterprise_readiness.py tests/unit/app/test_enterprise_readiness_additional.py tests/e2e/test_workflow_journeys.py tests/unit/docs/test_public_docs_contract.py -q`, `python -m ruff check app/enterprise_readiness.py tests/unit/app/test_enterprise_readiness.py tests/unit/app/test_enterprise_readiness_additional.py tests/e2e/test_workflow_journeys.py tests/unit/docs/test_public_docs_contract.py`, `python -m mypy --config-file mypy.ini app/enterprise_readiness.py`. |
| LP-CR-134 | 2026-03-14 | Service-owned privileged runtime-retention action path | Hardened | Runtime-retention cleanup had a CLI and a scheduled worker, but no first-class service-owned action path for operators. That left a gap between governed retention policy and operator remediation: applied cleanup still depended on shell access, and the strongest enterprise actor identity available at request time was not being carried directly into retained cleanup evidence. | Added `POST /integration/runtime-retention-cleanups/run` as a governed control-plane action for retained dry-run and apply execution. The route resolves enterprise actor or service identity from request headers, persists that identity into the retained cleanup evidence, and returns the evidence summary directly to the caller. Tightened enterprise write governance by adding a default capability rule requiring `operations.runtime.manage` for this endpoint when enterprise authz is enabled. Added targeted unit and integration proofs for missing-identity rejection, retained actor/job propagation, and capability-gated denial/allow behavior, and updated the API reference, retention runbook, and enterprise-readiness standard so the new operator action path is explicit and governed. | Next: the strongest follow-on is extending the same privileged-action pattern to other high-value remediation workflows or enriching retained cleanup evidence with full enterprise tenant/correlation context if investigations require it. The gap between runtime-retention governance and service-owned operator execution is now materially closed. | Branch `review-ledger-retention-lifecycle`; files: `app/enterprise_readiness.py`, `app/models/runtime_retention_history.py`, `app/api/endpoints/runtime_retention_history.py`, `docs/guides/api_reference.md`, `docs/runbooks/runtime-retention-cleanup.md`, `docs/standards/enterprise-readiness.md`, `tests/unit/app/test_enterprise_readiness.py`, `tests/integration/test_runtime_retention_run_api.py`, `tests/unit/docs/test_public_docs_contract.py`; validation: pending targeted pytest/ruff/mypy/openapi gate for this slice. |
| LP-CR-135 | 2026-03-14 | Durable enterprise request context for privileged retention actions | Hardened | LP-CR-134 added a governed service-owned retention action path, but the retained cleanup evidence still dropped enterprise tenant and correlation context after auth succeeded. That left post-incident traceability weaker than it should be: investigators could see who ran cleanup, but not the exact enterprise request scope that triggered it. | Extended runtime-retention cleanup evidence, manifest entries, history responses, and immediate run responses with optional `tenant_id` and `correlation_id`. The service-owned retention action now captures `X-Tenant-Id` and `X-Correlation-Id` and persists them into durable evidence, so cleanup history remains tied to the original enterprise request context. Added unit, model, and integration proofs for the new fields and updated the enterprise-readiness standard, runbook, API reference, and docs regression pack so the stronger audit boundary is explicit and durable. | Next: the strongest follow-on is carrying the same durable request-context standard into other privileged remediation workflows, not widening retention scope further. The gap between enterprise auth context and retained cleanup evidence is now materially closed. | Branch `review-ledger-retention-lifecycle`; files: `app/services/runtime_retention_execution_service.py`, `app/services/runtime_retention_history_service.py`, `app/models/runtime_retention_history.py`, `app/api/endpoints/runtime_retention_history.py`, `docs/guides/api_reference.md`, `docs/runbooks/runtime-retention-cleanup.md`, `docs/standards/enterprise-readiness.md`, `tests/unit/test_runtime_retention_cleanup.py`, `tests/unit/services/test_runtime_retention_history_service.py`, `tests/unit/models/test_runtime_retention_history_models.py`, `tests/integration/test_runtime_retention_run_api.py`, `tests/unit/docs/test_public_docs_contract.py`; validation: pending targeted pytest/ruff/mypy/openapi gate for this slice. |
| LP-CR-136 | 2026-03-14 | Service-owned governed recovery-drill action path | Hardened | Durable recovery drills were governed and retained, but still shell-owned. That left a real operational gap versus runtime-retention cleanup: operators could inspect retained drill evidence through the API, but could not run a governed drill through the service-owned control plane, and the retained recovery evidence still dropped enterprise tenant and correlation context. | Added `POST /integration/recovery-drills/run` as a service-owned recovery-drill action path. The endpoint resolves enterprise actor or service identity, optional tenant and correlation context, and persists that context into retained recovery-drill evidence and history. Tightened enterprise write governance by adding a default capability rule requiring `operations.runtime.manage` for this endpoint when enterprise authz is enabled. Extended recovery-drill history and response models to surface the new durable request context, added unit, model, and integration proofs for the action path and capability gating, and updated the API reference plus enterprise-readiness standard so the new remediation surface is explicit and governed. | Next: the strongest follow-on is applying the same service-owned remediation pattern to any remaining operational scripts that still mutate or validate durable state outside a governed API boundary. The gap between recovery-drill governance and service-owned operator execution is now materially closed. | Branch `review-ledger-retention-lifecycle`; files: `app/enterprise_readiness.py`, `scripts/durable_recovery_drill.py`, `app/services/recovery_drill_history_service.py`, `app/models/recovery_drill_history.py`, `app/api/endpoints/recovery_drill_history.py`, `docs/guides/api_reference.md`, `docs/standards/enterprise-readiness.md`, `tests/unit/app/test_enterprise_readiness.py`, `tests/unit/test_durable_recovery_drill.py`, `tests/unit/services/test_recovery_drill_history_service.py`, `tests/unit/models/test_recovery_drill_history_models.py`, `tests/integration/test_recovery_drill_run_api.py`; validation: pending targeted pytest/ruff/mypy/openapi gate for this slice. |
| LP-CR-137 | 2026-03-15 | Audit metadata depth for allowed governed write actions | Hardened | LP-CR-133 closed the auditability gap for privileged reads, but successful governed write actions still emitted generic write audit metadata without the governed surface or required capability. That meant investigations could prove that a privileged mutation happened, but not which governed write surface was used or which capability justified it, weakening incident reconstruction for remediation workflows. | Extended the enterprise audit middleware so allowed governed write actions now emit the same `required_capability` and `governed_surface` metadata model already used for privileged reads whenever a governed write rule applies. Added unit proof for `POST /integration/runtime-retention-cleanups/run`, updated enterprise-readiness and API-reference docs so the stronger write-side audit contract is explicit, and added docs regression coverage to keep the governance language from drifting. | Next: the strongest follow-on is applying the same audit-evidence standard to any remaining governed mutation surfaces added in future slices, not broadening the capability map without a concrete risk unit. The gap between privileged write authz and privileged write auditability is now materially closed. | Branch `main`; files: `app/enterprise_readiness.py`, `tests/unit/app/test_enterprise_readiness.py`, `docs/standards/enterprise-readiness.md`, `docs/guides/api_reference.md`, `tests/unit/docs/test_public_docs_contract.py`; validation: pending targeted pytest/ruff/mypy/openapi gate for this slice. |
| LP-CR-138 | 2026-03-15 | Replay fencing for governed remediation actions | Hardened | `POST /integration/runtime-retention-cleanups/run` and `POST /integration/recovery-drills/run` were capability-gated and enterprise-audited, but they still executed on every repeat call. That left a real operator-safety gap: accidental double-submit or a retry-happy client could immediately rerun expensive governed remediation actions without any service-owned cooldown fence. | Added a shared operator action guard service and config-backed manual cooldowns for runtime-retention cleanup and recovery-drill actions. Both service-owned remediation endpoints now inspect the latest retained evidence before running, reject immediate manual replays with `409` plus `Retry-After`, and return structured cooldown details naming the last retained evidence artifact. Added focused unit proof for cooldown math plus integration proof for both operator action routes, and updated the enterprise-readiness standard and API reference so the replay-fence contract is explicit and governed. | Next: the strongest follow-on is rate-shaping or deeper idempotency semantics for any future governed remediation actions that outgrow a bounded cooldown fence, not relaxing the current action guard without evidence. The gap between privileged-action authz and accidental replay protection is now materially closed. | Branch `main`; files: `app/core/config.py`, `app/services/operator_action_guard_service.py`, `app/api/endpoints/runtime_retention_history.py`, `app/api/endpoints/recovery_drill_history.py`, `tests/unit/services/test_operator_action_guard_service.py`, `tests/integration/test_runtime_retention_run_api.py`, `tests/integration/test_recovery_drill_run_api.py`, `docs/guides/api_reference.md`, `docs/standards/enterprise-readiness.md`; validation: pending targeted pytest/ruff/mypy/openapi gate for this slice. |
| LP-CR-139 | 2026-03-15 | Idempotent replay for governed remediation retries | Hardened | LP-CR-138 fenced immediate duplicate submissions, but it still forced same-request retries into either a rerun or a cooldown conflict. That left an operator ergonomics and correctness gap: a caller retrying the same governed remediation request with the same enterprise correlation context should get the original durable evidence back, not create a second mutation or receive a blind conflict that still requires manual evidence lookup. | Added a shared operator action replay service that resolves same-correlation manual retries against retained durable evidence for runtime-retention cleanup and recovery-drill actions. The service-owned endpoints now return the original retained response body with `X-Idempotent-Replay: true` when the incoming request matches the original governed action shape, and only fall back to cooldown conflicts for non-idempotent repeats. Added lower-level replay resolution proof plus integration coverage for same-correlation replay on both operator action routes, and updated the enterprise-readiness standard and API reference so the replay contract is explicit. | Next: the strongest follow-on is extending the same replay model to any future governed remediation actions that retain enterprise correlation context, not broadening mutation surfaces without the same idempotent boundary. The gap between replay fencing and true operator-safe retries is now materially closed. | Branch `main`; files: `app/services/operator_action_replay_service.py`, `app/api/endpoints/runtime_retention_history.py`, `app/api/endpoints/recovery_drill_history.py`, `tests/unit/services/test_operator_action_replay_service.py`, `tests/integration/test_runtime_retention_run_api.py`, `tests/integration/test_recovery_drill_run_api.py`, `docs/guides/api_reference.md`, `docs/standards/enterprise-readiness.md`; validation: pending targeted pytest/ruff/mypy/openapi gate for this slice. |
| LP-CR-140 | 2026-03-15 | Preview-before-apply fence for retention cleanup | Hardened | After LP-CR-138 and LP-CR-139, `POST /integration/runtime-retention-cleanups/run` still allowed `apply=true` without proving an operator had recently reviewed a matching dry-run, and the broad manual cooldown could even block the safe `dry_run` then immediate `apply` path. That left a real mutation-safety gap in the most destructive governed remediation workflow. | Scoped runtime-retention manual cooldown to the requested action shape instead of all manual cleanup history, so an immediate `dry_run` then matching `apply` is allowed while repeated `apply` or repeated `dry_run` requests are still fenced. Added a governed preview-before-apply rule requiring a recent matching manual dry-run before `apply=true` can execute, with explicit `409` semantics when no suitable preview exists. Added focused unit proof for the new preview guard plus integration proof that `apply` is rejected without preview and allowed immediately after a matching preview, and updated the enterprise-readiness standard and API reference so the new destructive-action contract is explicit. | Next: the strongest follow-on is extending preview-before-apply semantics or equivalent approval fencing to any future governed destructive action paths, not adding more mutation surfaces without the same review boundary. The gap between operator-safe replay and operator-safe destructive execution is now materially closed. | Branch `main`; files: `app/core/config.py`, `app/services/operator_action_guard_service.py`, `app/api/endpoints/runtime_retention_history.py`, `tests/unit/services/test_operator_action_guard_service.py`, `tests/integration/test_runtime_retention_run_api.py`, `docs/guides/api_reference.md`, `docs/standards/enterprise-readiness.md`; validation: pending targeted pytest/ruff/mypy/openapi gate for this slice. |
| LP-CR-141 | 2026-03-15 | Recovery-drill cooldown scoped to governed drill shape | Hardened | LP-CR-138 added a cooldown fence to recovery-drill actions, but it still used the latest manual drill globally. That created a real operator-control bug: validating one backup could incorrectly block an immediate validation of a different backup, even though the governed action shape and risk unit were different. | Scoped the recovery-drill cooldown fence to the governed drill shape by matching operator identity, tenant, and backup identifier before applying `409`/`Retry-After` cooldown semantics. Same-backup repeats remain fenced, but distinct backup validations no longer get falsely serialized behind the last drill. Added unit proof that different backup identifiers bypass the cooldown safely plus integration proof that two immediate drills for different backups both succeed, and updated the enterprise-readiness standard and API reference so the scoped-cooldown contract is explicit. | Next: the strongest follow-on is applying the same action-shape scoping rule to any future governed mutation surface that adds cooldown semantics, not restoring broad global throttles that hide the real risk unit. The gap between basic recovery-drill replay fencing and correct drill-shape execution policy is now materially closed. | Branch `main`; files: `app/services/operator_action_guard_service.py`, `app/api/endpoints/recovery_drill_history.py`, `tests/unit/services/test_operator_action_guard_service.py`, `tests/integration/test_recovery_drill_run_api.py`, `docs/guides/api_reference.md`, `docs/standards/enterprise-readiness.md`; validation: pending targeted pytest/ruff/mypy/openapi gate for this slice. |
| LP-CR-142 | 2026-03-15 | Replay ownership fencing for governed remediation actions | Hardened | LP-CR-139 introduced idempotent replay for same-correlation retries, but the replay matcher still ignored operator and tenant ownership. That meant a reused correlation identifier from a different actor or tenant could incorrectly replay someone else’s retained remediation evidence, which is a real security and correctness gap on privileged mutation surfaces. | Tightened the replay matcher for both runtime-retention cleanup and recovery-drill actions so replay only succeeds when correlation id, governed request shape, operator identity, and tenant context all match the original retained evidence. Added lower-level proof that cross-operator and cross-tenant correlation reuse does not replay retained evidence, plus integration proof that a different operator reusing the same correlation id gets a fresh execution instead of `X-Idempotent-Replay`. Updated the enterprise-readiness standard and API reference so replay ownership is explicit rather than implied. | Next: the strongest follow-on is applying the same ownership fence to any future governed replay path the moment it is added, not trusting correlation identifiers alone as an authorization boundary. The gap between replay ergonomics and replay ownership safety is now materially closed. | Branch `main`; files: `app/services/operator_action_replay_service.py`, `app/api/endpoints/runtime_retention_history.py`, `app/api/endpoints/recovery_drill_history.py`, `tests/unit/services/test_operator_action_replay_service.py`, `tests/integration/test_runtime_retention_run_api.py`, `tests/integration/test_recovery_drill_run_api.py`, `docs/guides/api_reference.md`, `docs/standards/enterprise-readiness.md`; validation: pending targeted pytest/ruff/mypy/openapi gate for this slice. |
| LP-CR-143 | 2026-03-15 | Explicit non-replay semantics for cross-owner correlation reuse | Hardened | LP-CR-142 fenced replay ownership, but the public contract still left one subtle ambiguity: if another operator or tenant reused the same correlation id, the request should not fail as a replay conflict and should not silently inherit the original actor’s evidence. It should proceed as a fresh governed action. That distinction matters for incident reconstruction and for operators interpreting whether a `200` came from replay or fresh execution. | Kept the replay matcher ownership-scoped and clarified the public contract so same-correlation requests from a different operator or tenant are treated as fresh governed actions rather than as replay. Added integration proof for both runtime-retention cleanup and recovery-drill endpoints that cross-owner correlation reuse yields a fresh execution with no `X-Idempotent-Replay` header, and updated the API reference plus review ledger to make the non-replay behavior explicit rather than implicit. | Next: the strongest follow-on is extending the same explicit non-replay semantics to any future governed action that adopts replay, not adding ownership-aware replay without documenting the fresh-execution fallback. The gap between replay ownership fencing and explicit operator-facing replay semantics is now materially closed. | Branch `main`; files: `app/api/endpoints/runtime_retention_history.py`, `app/api/endpoints/recovery_drill_history.py`, `tests/integration/test_runtime_retention_run_api.py`, `tests/integration/test_recovery_drill_run_api.py`, `docs/guides/api_reference.md`; validation: pending targeted pytest/ruff/mypy/openapi gate for this slice. |
| LP-CR-144 | 2026-03-15 | In-flight lease fencing for governed remediation actions | Hardened | LP-CR-138 through LP-CR-143 fenced duplicate operator actions only after durable evidence existed. That still left a real race window: two same-shape privileged actions arriving at the same time could both begin execution before either one wrote retained history, defeating cooldown and replay semantics. | Added a shared operator action lease service backed by atomic lock-file creation under the governed artifact directories. Runtime-retention cleanup and recovery-drill action endpoints now acquire an in-flight same-shape lease before execution and return `409` when the same governed action is already running for the same risk unit. Added unit proof for lease conflict and cleanup behavior plus integration proof that pre-existing in-flight leases block both operator action routes. Updated the enterprise-readiness standard and API reference so the in-flight concurrency fence is explicit rather than implicit. | Next: the strongest follow-on is extending the same in-flight lease model to any future governed mutation surface as soon as it is introduced, not relying on post-completion replay/cooldown semantics alone. The gap between post-hoc duplicate fencing and true in-flight execution control is now materially closed. | Branch `main`; files: `app/services/operator_action_lease_service.py`, `app/api/endpoints/runtime_retention_history.py`, `app/api/endpoints/recovery_drill_history.py`, `tests/unit/services/test_operator_action_lease_service.py`, `tests/integration/test_runtime_retention_run_api.py`, `tests/integration/test_recovery_drill_run_api.py`, `docs/guides/api_reference.md`, `docs/standards/enterprise-readiness.md`; validation: pending targeted pytest/ruff/mypy/openapi gate for this slice. |
| LP-CR-145 | 2026-03-15 | Stale in-flight lease reclaim for governed remediation actions | Hardened | LP-CR-144 closed the in-flight race, but the initial lock-file lease had no timestamp or reclaim rule. That left a real recovery gap: a crashed process or interrupted run could strand a same-shape governed action behind a stale lock forever, turning a safety fence into an operator dead-end. | Extended operator action lease metadata with `acquired_at_utc` and added bounded stale-lock reclaim based on new runtime-retention and recovery-drill stale-lease thresholds. Fresh leases still block concurrent same-shape execution, but locks older than the configured threshold are reclaimed automatically so operators can recover from interrupted runs without manual filesystem cleanup. Added unit proof for stale-lock reclaim plus integration proof that stale retention and recovery-drill leases are reclaimed and allow a fresh execution, and updated the enterprise-readiness standard and API reference so the reclaim behavior is explicit. | Next: the strongest follow-on is surfacing stale-lease reclaim events in the operator control plane or metrics so recovery from interrupted governed actions is observable, not just implicit. The gap between in-flight execution control and crash-safe lease recovery is now materially closed. | Branch `main`; files: `app/core/config.py`, `app/services/operator_action_lease_service.py`, `app/api/endpoints/runtime_retention_history.py`, `app/api/endpoints/recovery_drill_history.py`, `tests/unit/services/test_operator_action_lease_service.py`, `tests/integration/test_runtime_retention_run_api.py`, `tests/integration/test_recovery_drill_run_api.py`, `docs/guides/api_reference.md`, `docs/standards/enterprise-readiness.md`; validation: pending targeted pytest/ruff/mypy/openapi gate for this slice. |
