# Issue Fix Closure Matrix

This matrix records the current local closure posture for the GitHub issue batch on branch
`feat/performance-architecture-boundary-refactor`.

It is intentionally stricter than "code changed": an issue is only safe to close after the fix is
merged to `main`, required checks are green, and any changed repo-authored wiki source is published.
Until then, the local status can be `Fixed locally` while the GitHub issue remains open.

## Current Batch Summary

| Category | Count | Notes |
| --- | ---: | --- |
| Open GitHub issues reviewed | 41 | Includes the tracking ledger issue `#380`. |
| Actionable issues fixed locally | 33 | Current open GitHub issues with local fixes on this branch: `#415`, `#416`, `#417`, `#418`, `#419`, `#420`, `#421`, `#422`, `#423`, `#424`, `#425`, `#433`, `#434`, `#435`, `#436`, `#437`, `#438`, `#439`, `#440`, `#441`, `#442`, `#443`, `#444`, `#445`, `#446`, `#447`, `#448`, `#449`, `#450`, `#451`, `#452`, `#453`, and `#454`. |
| Actionable issues remaining | 7 | Current open issues still needing implementation evidence: `#426`, `#427`, `#428`, `#429`, `#430`, `#431`, and `#432`. |
| Tracking ledger issues | 1 | Issue `#380` remains open as the discovery ledger, not as a fix ticket. |
| Issues safe to close now | 0 | Closure waits for PR merge, CI evidence, and wiki publication where applicable. |

## Verification Baseline

Latest local pre-PR verification for this batch:

- Local `make test-unit`: passed with `3314 passed`.
- `make quality-test-taxonomy-gate`: passed with 301 modules, 3,493 source test functions, 684
  API/runtime test functions, 140 contract/governance test functions, and 950 uncategorized test
  functions under the unchanged 969 ceiling.
- `make openapi-gate` and `python scripts/api_vocabulary_inventory.py --validate-only`: passed.
- `python -m pytest --collect-only -q`: collected `3,732` tests.
- Wiki check-only: expected unpublished branch drift where repo-authored wiki source changed; publish
  with the governed wiki automation after merge to `main`.

## Issue Matrix

| Issue | Local status | Ledger evidence | Same-pattern and regression posture | Close condition |
| --- | --- | --- | --- | --- |
| `#415` Certify mandate health in integration capabilities publication | Fixed locally | `LP-CR-1654` | The certification page now lists `mandate_performance_health_context` as a certified analytics surface with its stateless DPM supportability boundary. Docs regression now compares the certified surface list with both the canonical example payload and the generated runtime report, preventing future capability-publication drift. | Close after merge and green PR evidence. |
| `#417` Stop presenting static trust telemetry fixtures as runtime proof | Fixed locally | `LP-CR-1655` | Repo-owned `contracts/trust-telemetry/` files are now explicitly classified as static fallback fixtures, not runtime proof. Tests verify the README wording and fixture `source_artifact_uri` classification so contract fixtures cannot be mislabeled as runtime telemetry without separate runtime output evidence. | Close after merge and green PR evidence. |
| `#424` Make migration-apply exercise durable schema upgrades, not only contract prose | Fixed locally | `LP-CR-1656` | `make migration-apply` now runs executable durable schema bootstrap apply/verify through `scripts/durable_schema_apply.py`, emits structured operator evidence under `artifacts/durable-schema-apply/`, verifies owned execution, compute, async-result, lineage, and composite metadata tables, and checks additive lineage/composite schema columns. Same-pattern scan kept prose-only validation in clearly named contract/inventory/runbook targets while blocking `migration-apply` from reverting to the prose checker. | Close after merge, green PR evidence, and wiki publication. |
| `#425` Clarify and enforce returns-series strict-intersection fill semantics | Fixed locally | `LP-CR-1657` | Returns-series normalization now applies selected benchmark/risk-free fill before strict intersection, while strict no-fill behavior remains unchanged. Diagnostics expose `fill_evidence` so filled side-series dates are distinguishable from observed source dates, and freshness continues to use source frames. Same-pattern coverage includes benchmark and risk-free side series, forward-fill and zero-fill, API behavior, active/cumulative active outputs, docs certification, method docs, runbook, and wiki source. | Close after merge, green PR evidence, and wiki publication. |
| `#433` Make stateful async promotion atomic on duplicate calculation conflicts | Fixed locally | `LP-CR-1675` | Stateful async finalization now delegates offloaded resolved requests to the shared submission-promotion helper before mutating execution contract or identity. Duplicate `calculation_id` conflicts with a different resolved payload therefore return the existing governed conflict behavior without rewriting the durable execution contract, input fingerprint, calculation hash, stage evidence, or polling identity. Same-pattern coverage spans the shared finalization path used by TWR, contribution, attribution, benchmark, and returns-series stateful promotion paths, plus submission fencing tests for replay/conflict preservation and taxonomy classification of runtime/idempotency evidence. | Close after merge and green PR evidence; no repo-local wiki source change was needed because this is an internal idempotency invariant with unchanged operator workflow. |
| `#434` Anti-corrupt malformed upstream retrieval metadata before stateful analytics use | Fixed locally | `LP-CR-1674` | Shared upstream retrieval metadata parsing no longer lets malformed optional telemetry raise uncategorized `ValueError` in stateful analytics flows. Positive integer and positive integral legacy numeric-string/float counts are preserved; booleans, non-numeric strings, non-integral floats, zero, and negative values default to configured safe counters and record source-safe invalid field names. Benchmark exposure context now exposes `metadata.retrieval_metadata_quality` with bounded `MALFORMED_UPSTREAM_RETRIEVAL_METADATA_COUNT` reason codes so operators can distinguish malformed telemetry from missing exposure rows. Same-pattern coverage spans shared parser modes, benchmark exposure service/API, OpenAPI, API vocabulary, docs, repo context, wiki source, and quality evidence. | Close after merge, green PR evidence, and wiki publication. |
| `#435` Govern the MARKET calendar holiday source beyond the hard-coded 2025-2027 horizon | Fixed locally | `LP-CR-1673` | Returns-series MARKET calendar handling now uses `app.services.lotus_reference_market_calendar` instead of a finite in-service holiday set. The provider generates Good Friday, New Year, Christmas, and observed weekday holidays over the certified 1970-01-01 through 2099-12-31 horizon, fails closed outside that horizon, and exposes `diagnostics.calendar_source` so downstream consumers can distinguish governed market-calendar evidence from weekday-only behavior. Same-pattern coverage spans daily filtering, required-date counts, gap/freshness diagnostics, API responses, docs, endpoint certification, repo context, wiki source, and quality evidence. | Close after merge, green PR evidence, and wiki publication. |
| `#436` Add legal-hold exclusions to runtime retention cleanup | Fixed locally | `LP-CR-1672` | Runtime-retention cleanup now loads a durable legal-hold source from `RUNTIME_RETENTION_LEGAL_HOLD_PATH` before manifest generation and destructive phases. Protected execution, compute-job, async-result, lineage-record, and lineage-artifact records are excluded from dry-run and apply deletion; cleanup evidence exposes protected counts and bounded `protected_reason_counts`, and target manifests retain eligible and protected identifiers separately. Same-pattern coverage spans runtime-retention service planning/deletion, retained-history projection, manual run evidence, compute/async store pruning, operator docs, API references, repo context, wiki source, quality taxonomy, and docs guards. | Close after merge, green PR evidence, and wiki publication. |
| `#437` Add request DTO mappers before analytics use-case workflows | Fixed locally | `LP-CR-1644` | TWR, workspace-summary, contribution, benchmark, and returns-series routes now map request DTOs through `app.api.mappers.analytics_workflow_requests` into typed workflow command carriers before invoking application workflows. The architecture inventory enforces the route-to-workflow mapper boundary with `ROUTE_WORKFLOW_DTO_DIRECT_CALL`, while lower-level compatibility preserves existing idempotency, requested-window, async payload, lineage, and source-evidence behavior. | Close after merge and green PR evidence; no repo-local wiki source change was needed for this internal architecture boundary. |
| `#438` Centralize durable database engine policy for Postgres runtime operations | Fixed locally | `LP-CR-1671` | Execution registry, lineage metadata, compute-job, async-result, composite metadata, and restore-validation drill paths now share `app.services.durable_database_engine`. PostgreSQL engine construction applies explicit connect timeout, pool pre-ping, pool size/overflow, pool recycle, statement timeout, and lock timeout; SQLite local mode preserves `check_same_thread=false` and adds a bounded busy timeout. Tests prove SQLite/PostgreSQL options plus execution-registry commit and rollback behavior. Service reference, API reference, runtime topology, alert runbook, wiki operations source, repo context, quality evidence, and docs guards now expose and protect the durable DB policy. | Close after merge, green PR evidence, and wiki publication. |
| `#439` Add real backup restore validation to the durable recovery drill | Fixed locally | `LP-CR-1670` | Added read-only restore-validation mode to `scripts/durable_recovery_drill.py` for restored durable metadata database targets. Evidence now distinguishes `synthetic_smoke` from `restore_validation`, records backup source/id, restore timestamp, backup age/RPO, restore duration/RTO, restored owned-table row counts, schema/readiness status, and representative execution/lineage retrieval checks. Runbook, endpoint certification, API references, wiki source, runbook guard, docs guard, quality evidence, and restore-validation tests now prevent synthetic smoke evidence from being presented as real backup restore proof. | Close after merge, green PR evidence, and wiki publication. |
| `#440` Add first-party and third-party license compliance evidence | Fixed locally | `LP-CR-1669` | Added the repo MIT `LICENSE`, machine-readable license compliance policy, generated runtime/development dependency license inventory, and blocking `make license-compliance-gate`. The gate detects dependency-license drift, blocked license families, and review-required licenses without owner-bound/time-bound exceptions. README, CI workflow docs, contracts index, quality scorecards, wiki source, and CI wiring tests now document regeneration and release-review expectations. | Close after merge, green PR evidence, and wiki publication. |
| `#441` Onboard remaining upstream dependency watchlist into governed contract declarations | Fixed locally | `LP-CR-1668` | Added a route-level upstream dependency inventory for every active `CoreIntegrationService.get_*` dependency. Declared dependencies map to repo-native consumer products; benchmark definition, benchmark vendor return-series, and FX operational-read routes carry time-bound exception records with owner, expiry, route/contract posture, trust metadata, failure posture, validation lane, allowed interpretation, promotion condition, and evidence tests. `make domain-product-validate` now fails when an active upstream method lacks declared or exception coverage. RFC-0082/RFC-0086 docs, contracts README, repo context, wiki source, and contract tests now point to the inventory. | Close after merge, green PR evidence, and wiki publication. |
| `#443` Define async completion SLOs and capacity budgets for executor-backed analytics | Fixed locally | `LP-CR-1667` | Added the repo-owned async submission-to-terminal SLO and capacity contract covering returns-series, TWR, contribution, attribution, workspace-summary, and benchmark async surfaces. The contract defines production p95 objectives, concurrency and worker assumptions, a worker-sizing formula, threshold-to-SLO-burn mapping, scale triggers, cost guardrails, and evidence commands tied to existing performance characterization tests. Scalability, threshold-profile, characterization, alert runbook, repo context, docs index, wiki source, and docs-contract guards now link the contract. | Close after merge, green PR evidence, and wiki publication. |
| `#444` Make runtime retention cleanup restart-safe across stores and artifacts | Fixed locally | `LP-CR-1666` | Runtime-retention apply mode now persists an `in_progress` evidence file before destructive phases start, including the selected execution ids, lineage ids, lineage artifact paths, cutoff, retention policy, operator identity, and cleanup run id. Apply cleanup records per-phase target/deleted/skipped/failed counts for compute jobs, async results, lineage artifacts, lineage records, and executions; failed phases rewrite the same evidence as `failed` and are not replayed as successful idempotent requests. Reruns reconcile already-deleted aggregate rows and missing artifact directories through idempotent phase results. | Close after merge, green PR evidence, and wiki publication. |
| `#445` Remove raw exception text from lineage and workflow failure surfaces | Fixed locally | `LP-CR-1665` | Unexpected lineage inventory failures now log the raw exception server-side while returning the governed internal-error envelope without injected exception text. Unexpected workflow and externally readable execution failure state now use operation-specific support-safe messages for lineage enqueue, Benchmark, Workspace Summary, Returns Series, Attribution, Contribution, TWR, MWR, Benchmark Exposure Context, and compute worker terminal/retryable failure paths. Mapped application errors and intentional public 4xx validation details remain unchanged. Same-pattern coverage includes API examples, durable execution failure records, async result/job errors, and regression tests that assert injected raw text is absent. | Close after merge and green PR evidence; no repo-local wiki source change was needed because this is an error-safety invariant rather than new operator workflow truth. |
| `#446` Classify and minimize downloadable lineage artifacts | Fixed locally | `LP-CR-1664` | Lineage manifests now include per-artifact classification, intended audience, sensitivity, minimization posture, retention category, and redaction-required metadata. The lineage response exposes that metadata beside controlled artifact URLs, and manifest validation rejects missing or unknown artifact classifications with degraded `503` behavior instead of advertising incomplete inventory. Raw `request.json` and `response.json` are conservatively `operator_only`; derived detail files are minimized operator evidence; customer-consumable support packs must be explicitly produced. Same-pattern coverage includes manifest writing, response projection, artifact download eligibility, API docs, reproducibility docs, and public docs guards. | Close after merge and green PR evidence; no repo-local wiki source change was needed because the existing wiki does not publish lineage artifact sharing rules. |
| `#447` Split the monolithic stateful source retrieval service into governed ports | Fixed locally | `LP-CR-1663` | Portfolio reference and portfolio timeseries retrieval now flow through the named `StatefulPortfolioSourcePort` and default `CoreStatefulPortfolioSourceAdapter`, removing direct Core call ownership for the highest-risk portfolio source family while keeping `StatefulInputService` as the chunking, pagination, dedupe, request-fingerprint, and upstream-snapshot orchestration layer. The design note records the target source-family port split and the no-runtime-split decision. Focused tests prove adapter request forwarding, injected fake-port orchestration, pagination sequence, deduped payload assembly, snapshot paging metadata, and request fingerprints after extraction. | Close after merge and green PR evidence; no repo-local wiki source change was needed for this internal architecture boundary. |
| `#448` Align execution completion with lineage materialization terminal state | Fixed locally | `LP-CR-1662` | Execution polling no longer reports a plain top-level `complete` while mandatory lineage or inspection artifact materialization is still `in_progress`. Shared lifecycle helpers now leave executions `running` after calculation output until the lineage worker atomically completes the materialization stage and top-level execution; terminal lineage/artifact retry exhaustion atomically marks the stage and execution failed. Public docs and OpenAPI schema explain that `complete` includes mandatory evidence materialization success where that stage exists. | Close after merge and green PR evidence; no repo-local wiki source change was needed for this issue. |
| `#449` Govern calculation-id filters on runtime operator list APIs | Fixed locally | `LP-CR-1661` | Runtime work-item and recovery APIs now reject short or non-UUID-shaped `calculation_id_contains` values at the API boundary and document the parameter as a governed calculation-id prefix/full UUID lookup with minimum length 8. Compute and lineage durable-store adapters use shared prefix filtering instead of arbitrary SQL substring matching, and static guard coverage blocks `.calculation_id.contains(...)` from returning on those hot operator paths. Integration, OpenAPI, docs, API vocabulary, and taxonomy evidence were updated for both operator surfaces. | Close after merge and green PR evidence; no repo-local wiki source change was needed for this issue. |
| `#452` Publish an endpoint-by-endpoint RFC-020 multi-currency support matrix | Fixed locally | `LP-CR-1658` | Added the canonical RFC-020 multi-currency support matrix covering TWR, MWR, Contribution, Attribution, Benchmark, Returns Series, and Workspace Summary. RFC-020/RFC index, API reference, endpoint guides, methodology docs, certification pages, and wiki source now point to endpoint-specific support truth. Docs-contract coverage parses the matrix and guards MWR `SINGLE_REPORTING_CURRENCY` / `SOURCE_PRECONVERTED_WITH_FX_EVIDENCE`, Attribution `currency_mode=\"BOTH\"`, Contribution local/FX reconciliation, and returns-series non-decomposition semantics. | Close after merge, green PR evidence, and wiki publication. |
| `#450` Resolve RFC-023 benchmark_spec drift against the core-sourced benchmark architecture | Fixed locally | `LP-CR-1660` | Added the RFC-023 benchmark contract baseline and reclassified RFC-023's free-form `benchmark_spec`, helper resolve endpoint, and dynamic rebalancing design as superseded by RFC-042 for the current public API contract. RFC-023, RFC-042, RFC index, implementation status, delta backlog, benchmark guide, API reference, and wiki source now agree that supported benchmark requests use `benchmark_id`, `input_mode`, `return_source`, and the current stateless/stateful fields rather than a public `benchmark_spec` block. Docs-contract coverage parses the baseline and blocks stale current-doc promotion of the superseded request shape. | Close after merge, green PR evidence, and wiki publication. |
| `#451` Rebaseline RFC-021 gross-to-net bridge truth against current NET/GROSS fee support | Fixed locally | `LP-CR-1659` | Added the RFC-021 gross/net support baseline and reclassified RFC-021 from final bridge truth to rebaseline-required fee-basis support truth. RFC-021, RFC index, TWR guide, API reference, TWR methodology, TWR/workspace endpoint certification, and wiki source now agree that current support is `metric_basis=NET` / `metric_basis=GROSS`, while shared `costs` DTOs, `gross_net` bridge responses, `engine/costs.py`, performance-fee HWM/hurdle logic, and cost allocation remain unsupported backlog. Docs-contract coverage blocks accidental re-advertising of unsupported bridge support. | Close after merge, green PR evidence, and wiki publication. |
| `#397` Protect TWR inspection artifact downloads with privileged-read authorization | Fixed locally | `LP-CR-1625` | Central privileged-read rule family now covers `/performance/inspections`; same-class file-backed evidence route scan found lineage already governed and inspections aligned; unit and integration authz tests cover parent and child artifact paths. | Close after merge and green PR evidence. |
| `#396` Normalize stateful source currency codes before mixed-currency FX gating | Fixed locally | `LP-CR-1626` | Shared `normalized_currency_code(...)` helper replaces raw string comparisons across contribution, attribution, and cash-flow currency checks; blank, whitespace, case-only, and true mixed-currency regressions are covered. | Close after merge and green PR evidence. |
| `#388` Normalize legacy ITD period usage to canonical SI across performance APIs | Fixed locally | `LP-CR-1627` | Since-inception aliases normalize to canonical `SI` before calculations, metadata, lineage windows, and response keys; current authored examples reject `"period": "ITD"` unless documenting alias compatibility. | Close after merge and green PR evidence. |
| `#401` Add jitter and retry-budget controls to upstream HTTP retries | Fixed locally | `LP-CR-1628` | Shared outbound HTTP retry helper now uses bounded fallback jitter, preserves safe `Retry-After`, records retry-budget diagnostics, and has entropy/bounds tests to avoid concurrent retry waves. | Close after merge and green PR evidence. |
| `#400` Declare active benchmark and index upstream products in the data-mesh consumer contract | Fixed locally | `LP-CR-1629` | Active benchmark/index dependencies are machine-readable consumer declarations; watchlist-only dependencies remain explicitly documented until upstream producer declarations exist; domain-product validation covers the contract. | Close after merge, green PR evidence, and wiki publication. |
| `#399` Promote runtime alert templates into deployable validated monitoring artifacts | Fixed locally | `LP-CR-1630` | Deployable PrometheusRule and Grafana artifacts are repo-owned; observability readiness gate validates alert metrics, labels, links, dashboard panels, and sensitive-label policy. | Close after merge, green PR evidence, and wiki publication. |
| `#398` Add local README indexes for major repository packs | Fixed locally | `LP-CR-1631` | Major source, docs, contracts, quality, scripts, tests, monitoring, and wiki packs now have local README indexes; documentation inventory enforces 12/12 coverage. | Close after merge, green PR evidence, and wiki publication. |
| `#393` Align repo context with full performance trust telemetry coverage | Fixed locally | `LP-CR-1632` | Repository context now follows the all-active-product trust telemetry rule derived from domain-product declarations and trust telemetry snapshots; docs tests block the stale single-product claim. | Close after merge and green PR evidence. |
| `#392` Make governed agent reading order discoverable from repo context surfaces | Fixed locally | `LP-CR-1633` | README, repo context, docs pack index, and wiki Home expose `AGENTS.md`, procedural memory, skill routing, and wiki publication navigation; docs tests protect the reading-order surface. | Close after merge, green PR evidence, and wiki publication. |
| `#391` Align cleanup scope with generated runtime artifact policy | Fixed locally | `LP-CR-1634` | `make clean` now plans runtime/evidence roots, local coverage variants, SQLite sidecars, and logs while preserving durable source truth; cleanup and hygiene tests cover the policy. | Close after merge, green PR evidence, and wiki publication. |
| `#389` Align integration-capabilities examples with runtime capability truth | Fixed locally | `LP-CR-1635` | OpenAPI and JSON examples now match runtime capability output for TWR inspection notes, sync path-template nulls, and mandate health wording; model and docs regressions compare runtime truth to published examples. | Close after merge and green PR evidence. |
| `#390` Refresh integration-capabilities certification after Gateway query-param fix | Fixed locally | `LP-CR-1636` | Endpoint certification now reflects current Gateway canonical `consumer_system` and `tenant_id` behavior; docs regression blocks stale camelCase/current-issue wording. | Close after merge and green PR evidence. |
| `#387` Refresh stale quality evidence after post-fix suite and architecture drift | Fixed locally | `LP-CR-1637` | Quality taxonomy, baseline, scorecard, and CI gate evidence are refreshed from repo-native scanners; docs guard derives current taxonomy counts to prevent future stale evidence. | Close after merge and green PR evidence. |
| `#442` Add polling cadence guidance to async 202 responses | Fixed locally | `LP-CR-1651` | Shared async accepted response projection now emits `recommended_poll_after_seconds` and matching `Retry-After` headers for initial submissions and pending-result responses across TWR, benchmark, contribution, attribution, returns-series, workspace-summary, and TWR inspection; OpenAPI, docs, and representative integration tests distinguish analytics polling cadence from manual operator-action cooldowns. | Close after merge, green PR evidence, and wiki publication. |
| `#454` Complete attribution diagnostics and audit footer parity | Fixed locally | `LP-CR-1652` | Completed attribution responses now require non-null shared `diagnostics` and `audit` footer blocks, with bounded period-status, residual-materiality, supportability-evidence, input, level, group, reason, supportability, residual, and benchmark-context counts. Same-pattern coverage adds a cross-endpoint footer parity contract for TWR, MWR, Contribution, and Attribution, and RFC-014-D02 is closed in the delta backlog. | Close after merge, green PR evidence, and wiki publication. |
| `#453` Enforce or retire flags.fail_fast consistently across core analytics endpoints | Fixed locally | `LP-CR-1653` | Shared fail-fast policy now covers completed TWR, MWR, Contribution, and Attribution responses. Same degraded requests return `200` when `flags.fail_fast=false` and `422` with `FAIL_FAST_SOFT_WARNING` when true. Same-pattern coverage verifies TWR daily evidence warnings, MWR fallback warnings, Contribution diagnostic notes, and Attribution supportability reasons; RFC-014-D01 is closed in the delta backlog. | Close after merge, green PR evidence, and wiki publication. |
| `#380` Lotus Performance Issue Discovery Ledger | Tracking ledger | Review ledger plus this matrix | This remains the parent discovery ledger for the campaign. Do not close it as part of one batch unless the broader campaign owner explicitly signs off. | Keep open. |

## No-PR-Yet Decision

No PR should be raised from this branch until the issue matrix remains complete, the branch is pushed,
and current local or remote validation evidence is available. This file does not close the issues by
itself; it gives the PR author and reviewers a single place to confirm that the local fixes, similar
pattern scans, docs/context/wiki decisions, and closure gates line up.
