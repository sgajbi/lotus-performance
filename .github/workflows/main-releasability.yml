name: Main Releasability Gate

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  actions: read
  contents: read

env:
  PIP_DISABLE_PIP_VERSION_CHECK: "1"
  PYTHONUNBUFFERED: "1"
  PYTHON_VERSION: "3.11"
  COVERAGE_FAIL_UNDER: "99"

jobs:
  workflow-lint:
    name: Main Releasability / Workflow Lint
    runs-on: ubuntu-latest
    steps:
      - name: Configure Git Default Branch
        run: git config --global init.defaultBranch main
      - uses: actions/checkout@v6
      - uses: reviewdog/action-actionlint@v1

  static-quality-gates:
    name: Main Releasability / Static Quality Gates
    runs-on: ubuntu-latest
    steps:
      - name: Configure Git Default Branch
        run: git config --global init.defaultBranch main
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: "pip"
      - name: Install
        run: make install-ci
      - name: Verify Installed Dependencies
        run: python -m pip check
      - name: Ruff Lint And Format
        run: make lint
      - name: Quality Complexity Gate
        run: make quality-complexity-gate
      - name: Quality Architecture Gate
        run: make quality-architecture-gate
      - name: Quality Router Thinness Gate
        run: make quality-router-thinness-gate
      - name: Quality Duplicate Code Gate
        run: make quality-duplicate-code-gate
      - name: Quality Observability Readiness Gate
        run: make quality-observability-readiness-gate
      - name: No-Alias Contract Guard
        run: make no-alias-gate
      - name: Mypy Typecheck
        run: make typecheck

  contract-security-gates:
    name: Main Releasability / Contract Security Gates
    runs-on: ubuntu-latest
    steps:
      - name: Configure Git Default Branch
        run: git config --global init.defaultBranch main
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: "pip"
      - name: Install
        run: make install-ci
      - name: OpenAPI Gate
        run: make openapi-gate
      - name: API Vocabulary Gate
        run: make api-vocabulary-gate
      - name: Domain Product Validation
        run: make domain-product-validate
      - name: Quality Evaluation Gate
        run: make quality-evaluation-gate
      - name: Migration Contract Smoke
        run: make migration-smoke
      - name: Security Audit
        run: make security-audit
      - name: Python Security Gate
        run: make python-security-gate

  test-suites:
    name: Main Releasability / Tests (${{ matrix.suite }})
    needs: [static-quality-gates, contract-security-gates]
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - suite: unit
            path: tests/unit
          - suite: integration
            path: tests/integration
          - suite: e2e
            path: tests/e2e
    steps:
      - name: Configure Git Default Branch
        run: git config --global init.defaultBranch main
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: "pip"
      - name: Install
        run: make install-ci
      - name: Run tests with coverage data
        env:
          COVERAGE_FILE: .coverage.${{ matrix.suite }}
        run: python -m pytest ${{ matrix.path }} --cov=app --cov=engine --cov=core --cov=adapters --cov-report=
      - name: Upload coverage data
        uses: actions/upload-artifact@v7
        with:
          name: coverage-data-${{ matrix.suite }}
          path: .coverage.${{ matrix.suite }}
          include-hidden-files: true
          if-no-files-found: error

  coverage-gate:
    name: Main Releasability / Coverage Gate (Combined)
    needs: [test-suites]
    runs-on: ubuntu-latest
    steps:
      - name: Configure Git Default Branch
        run: git config --global init.defaultBranch main
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: "pip"
      - name: Install
        run: make install-ci
      - name: Download coverage artifacts
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh run download ${{ github.run_id }} --pattern 'coverage-data-*' --dir coverage-data
          find coverage-data -mindepth 2 -type f -name '.coverage.*' -exec cp {} coverage-data/ \;
      - name: Enforce coverage floor
        run: |
          python -m coverage combine coverage-data
          python -m coverage report --fail-under=${{ env.COVERAGE_FAIL_UNDER }}
      - name: Upload coverage summary
        uses: actions/upload-artifact@v7
        with:
          name: main-releasability-coverage-data
          path: |
            .coverage
            coverage-data
          include-hidden-files: true
          if-no-files-found: error

  docker-build:
    name: Main Releasability / Validate Docker Build
    needs: [coverage-gate]
    runs-on: ubuntu-latest
    steps:
      - name: Configure Git Default Branch
        run: git config --global init.defaultBranch main
      - uses: actions/checkout@v6
      - name: Build Docker image
        run: make docker-build
